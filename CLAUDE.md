# CLAUDE.md — Webnua Platform

> This file is read at the start of every Claude Code session. It is the standing
> instruction set for this project. Keep it concise — bloat costs context every session.
> **When something goes wrong, fix the instance AND add a line here so it never recurs.**

---

## What this project is

The Webnua platform — **one application, two roles, role-based access:**

- **Client view** — what a Webnua customer (a business owner) sees: their funnels, tickets, integrations, websites, settings.
- **Admin/operator view** — what the Webnua team sees: all clients, the Proof Page pipeline (audit → build → generate), internal automations, the cross-client integrations matrix, ticket inbox.

These are **not two apps.** They are one codebase, one component library, one design system, with routing and layouts that branch by role. A large share of components are **shared** between the two views. Treat shared-by-default as the assumption; role-specific is the exception that needs justifying.

---

## Agency / sub-account model (the operator's two tiers)

The operator view is a **two-tier agency model** (GoHighLevel-style). The agency does two kinds of work, separated by *what* is being managed:

- **Agency level** — HQ. Cross-sub-account policy: default rules, capability floors, integration defaults, seat limits (later: plan catalog + billing). Done in **agency mode** (no sub-account selected). No drilldown.
- **Sub-account level** — one client. Their funnels, automations, campaigns, leads, integrations, profile. Reached by picking the sub-account from the sidebar `AdminClientPicker` → **sub-account mode**.

Mode is **state-driven** (`useIsAgencyMode()` from `lib/workspace/workspace-stub.tsx`), not URL-driven. Surfaces that exist at both levels **dispatch on the mode** — the settled pattern (`/dashboard`, `/website`, `/settings/access`; `/settings/*` joins it in Cluster 8). URL stays clean.

**Three-layer resolution.** Every agency-level setting resolves down three layers:

1. **Platform defaults** — code constants (`CLIENT_DEFAULTS`, section registry…).
2. **Agency policy** — what the agency sets HQ-wide.
3. **Sub-account** — inherits Layer 2; may **override** it. Override-anywhere model: every policy key is overridable per sub-account, flagged `inherited` vs `overridden`.

Effective value = resolve down the stack. Generalises the existing capability pattern (role defaults + grants). Built in **Cluster 8 · Session 2** as `lib/agency/` — agency policy store + per-sub-account override store + a typed `resolvePolicy` resolver returning `{ effectiveValue, source: 'agency' | 'override', agencyValue }`.

**Policy keys — the closed typed union.** A policy key is something that flows through the resolver. This is the *complete* set — the resolver is typed to this union, **NOT** `Record<string, unknown>`:

| Key | Value type | Cluster 8 status |
|---|---|---|
| `defaultClientCapabilities` | `Capability[]` | wired (S3/S4) |
| `integrationDefaults` | integration provider config | wired (S3/S4) |
| `defaultSeatLimit` | `number \| null` | wired (S3/S4) — resolves the misplaced seat-limit |
| `brandDefaults` | brand tokens (font / accent) | typed now, wired later |
| `automationDefaults` | `Record<string, boolean>` on/off flags | typed now, wired later |
| `pricingDefaults` | currency + buffer rules | typed now, wired later |

Deferred keys are named in the union *now* so Session 2's resolver is shaped right. Don't add a key outside this list without amending it here first — "we'll discover them as we go" is the grab-bag warning sign.

**NOT a policy key — structured templates (deferred, needs its own design).** Agency-provided *default funnel structures, default automation flow copy, default review-request templates* are nested structured data, not flat policy. "Override-anywhere" means something different there (override a whole template? a field? fork it?). Bigger shape — out of Cluster 8, and explicitly **not** shoehorned into the policy resolver.

**Webnua-as-sub-account — deferred, kept drop-in-ready.** The agency's own marketing account (Webnua) is structurally identical to a client sub-account but is *not* in the sidebar picker yet (deliberate). Cluster 8 must not introduce anything that makes the future drop-in harder — sub-account surfaces stay shape-agnostic about *which* client is active; no `clientId === 'webnua'` special-casing.

**Settings classification.** `/settings/*` becomes mode-dispatched in Cluster 8. Tab homes:

- **Agency-level** (agency mode): Workspace (agency identity), Team (agency operators), Access (cap policy + drill), Defaults, Integration defaults, Default seat limit, API + webhooks, Danger zone. (Plan catalog → later billing cluster.)
- **Sub-account-level** (sub-account mode / client role): Profile, Integrations (per-account keys/overrides), Notifications, Billing (assigned plan), Team (client teammates + seats), Danger.
- **User-level** (orthogonal axis — the signed-in person, not a workspace): Login + security. Help is global/static.

---

## Stack — these are decided. Do not introduce alternatives.

- **Framework:** Next.js (App Router) + React + TypeScript
- **Styling:** Tailwind CSS, themed to the prototype's design tokens (see Design System below)
- **Primitives:** shadcn/ui — the canonical primitive layer. Button, Input, Dialog, Card, Badge, etc. come from here.
- **Backend (intended, building later):** Supabase. Frontend is built now; build the data layer with Supabase's shape in mind (role-based row access, the audit→proof-page record relationship). Do not invent a placeholder data pattern that will be ripped out.
- **TypeScript:** strict mode, from line one. No `any` without a written reason.

If a task seems to need a library not listed here, **stop and ask** before adding it. Every dependency is a decision.

---

## The cardinal rule: check before you build

**Before creating ANY component, hook, util, or type — search the codebase for an existing one.**

- Grep `src/components/` and `src/lib/` for anything that does this or is close.
- If something close exists: **extend it or compose with it.** Do not create a parallel version.
- If you're unsure whether something exists: **search, don't assume.** An unnecessary search is free; a duplicate component is technical debt.
- If a genuinely new component is needed, check whether it belongs in `ui/`, `shared/`, or a route folder (see Structure) before creating it.

Duplicate components for the same concern are the #1 failure mode this project is guarding against. One canonical Button. One canonical FindingCard. One canonical Stepper.

---

## Project structure — where things live

```
/reference          → the HTML prototypes + spec docs. READ-ONLY. Never import from here.
/src
  /app              → Next.js App Router. Layouts branch by role here.
    /(client)       → client-role routes + client layout
    /(admin)        → admin-role routes + admin layout
    /(auth)         → login / role resolution
  /components
    /ui             → shadcn primitives + low-level building blocks. Canonical. Shared by everyone.
    /shared         → composed components used by BOTH client and admin views
    /client         → composed components used ONLY by the client view
    /admin          → composed components used ONLY by the admin view
  /lib              → utils, helpers, hooks, types, constants
  /lib/types        → shared TypeScript types — the single source of truth for data shapes
```

**Placement rules:**
- A component used by both roles → `components/shared/`. Default here.
- A component used by one role → `components/client/` or `components/admin/`.
- A primitive (does one small thing, no business logic) → `components/ui/`.
- If a `client/` component starts being needed in `admin/`, **move it to `shared/`** — don't copy it.

---

## The prototypes — how to use them

Two HTML prototypes live in `/reference`. They are the **visual and behavioural specification.** They are NOT source to port.

- **Design tokens → extract verbatim.** Colours, fonts, radii, spacing from the prototype CSS go into the Tailwind config. Pure extraction.
- **Primitives → build fresh, once.** Match the prototype's styling, build the canonical shadcn-based version.
- **Composed components → build fresh from primitives**, using the prototype as the visual + behaviour spec.
- **The prototype HTML → never imported into `src`.** It is read-only reference. Look at it to know what to build; never copy from it.

Do not "convert the HTML screen by screen." That produces one-off components. Build the token layer, then primitives, then compose.

---

## Design system

The look comes from the prototype. The tokens (lift these into `tailwind.config` and the CSS variables):

- **Colours (Webnua palette — source of truth):** paper `#f5f1ea`, paper-2 `#ebe5d9`, paper-3 `#e0d8c8`, ink `#0a0a0a` (with ink-soft `#2a2a28`, ink-mid `#4a4a45`, ink-quiet `#6e685c`), rust `#d24317` (with rust-deep `#8a3815`, rust-light `#e8743b`, rust-soft `#f4dccd` — the Perth landing page uses `#d24317`; the tools used `#d45b1a`, **standardise on `#d24317`**), good `#1e6b3a`, warn `#c44444`, info `#2d7d8a`, plum `#6b4ea6` (category swatch — campaigns), amber `#c8941e` (category swatch — reviews), rule `#c9c0b0`.
- **Two utility vocabularies, one bright line.** `globals.css` also exposes shadcn role aliases (`primary`, `secondary`, `background`, `foreground`, `card`, `popover`, `muted`, `accent`, `destructive`, `border`, `input`, `ring`) pointing at the Webnua palette. These are NOT a parallel palette — they're a role layer used internally by shadcn-derived primitives. **Bright line:** shadcn-derived files (anything pulled from `npx shadcn add ...`) keep the shadcn role vocabulary they ship with — don't fight upstream. Everything Webnua-authored — custom primitives in `ui/` like `Eyebrow`/`StatusDot`, and all of `shared/`, `client/`, `admin/` — uses the Webnua palette names (`bg-paper`, `text-ink`, `border-rule`, `text-rust`, etc.). The test is "did this file come from shadcn or did we write it" — no judgment call. **One sanctioned exception:** `bg-card` — the white-surface alias (`#ffffff`). Webnua-authored files may use `bg-card` for white card surfaces, because the Webnua palette has no white token (`paper` / `paper-2` / `paper-3` are all off-white). Until a dedicated `bg-surface` token is introduced, `bg-card` IS the canonical white surface in Webnua-authored code — it is the only shadcn role token allowed outside shadcn-derived files. (If you reach for any other role token — `bg-primary`, `text-foreground`, `border-ring`, etc. — that's the bright-line violation.)
- **Fonts:** Inter Tight (display + body), JetBrains Mono (eyebrows, data, technical labels).
- **Feel:** light theme — paper backgrounds, ink text, rust accent. Heavy uppercase Inter Tight for display headings, tight letter-spacing. Mono eyebrow tags. Generous section rhythm.

When a design decision isn't covered by a token, **ask rather than inventing** — design drift is hard to undo later.

---

## Workflow rules — how we work together

1. **Plan before code.** For anything beyond a trivial change, explain the approach and list the files you'll touch *before* writing. Wait for confirmation on anything non-obvious.
2. **One screen or one feature per session** where possible. Small, focused scope. Large unfocused sessions are where redo/undo happens.
3. **Don't refactor unrelated code while doing something else.** Fixing a bug? Fix the bug. See something else messy? Mention it, don't touch it.
4. **Respect what's working.** Do not "improve," reformat, or restructure code that wasn't part of the task. If existing code looks wrong but works, ask before changing it.
5. **If a change touches more than ~5 files, stop and confirm** the approach before proceeding.
6. **Commit at every working state.** A clean git history is the undo button. Suggest a commit whenever something works.
7. **No silent scope expansion.** If the task turns out bigger than it looked, say so and re-confirm — don't just keep going.

---

## Code conventions

- **Components:** one component per file, named export, file named after the component (`FindingCard.tsx`).
- **Component internal layout:** consistent order — types/props at top, the component, helpers below. Predictable structure makes debugging navigation, not archaeology.
- **Types:** shared data shapes live in `src/lib/types`. Don't redefine the same shape in two places — import it.
- **Naming:** descriptive and specific. `ProofPageStepper` not `Stepper2`. No numbered suffixes ever — a numbered suffix means a duplicate that should have been a reuse.
- **State:** local state for local concerns. Lift only when genuinely shared. Don't reach for global state by default.
- **Error handling:** [decide the pattern on first real use, then document it HERE so it's consistent everywhere].
- **`useSyncExternalStore` snapshots MUST be reference-stable.** `getSnapshot` cannot return a fresh array/object each call (no `.filter()`/`.map()`/`{...}`/`.sort()` inline) — React then sees the store "change" every render and spins into an infinite loop that crashes the route in production. The stub stores (`publish-stub`, `audit-stub`) cache their snapshots keyed on the raw localStorage string. Any new store accessor read through `useSyncExternalStore` must do the same.
- **No dead code.** Don't leave commented-out blocks "just in case" — git remembers.

---

## What NOT to touch

- `/reference` — read-only, ever.
- Generated files, lockfiles, `.next/`.
- The `@theme` block in `src/app/globals.css` (the Webnua design tokens — palette, fonts, radii, shadows) and `tsconfig` strict settings — these are deliberate. Tailwind v4 is CSS-first, so tokens live in `@theme`, not `tailwind.config.*`. Changing them needs a conversation.
- Any config that's clearly intentional — ask first.

---

## Known component inventory

> Keep this list current. When a shared component is built, add it here so future sessions
> find it instead of rebuilding it. This is the map that prevents duplication.
> Format: **`Name`** — purpose — variants/sizes/notable props.

### `ui/` — primitives

**shadcn-derived (keep shadcn role vocabulary; don't fight upstream):**

- **`Button`** (`button.tsx`) — canonical action element. Themed to prototype: `h-10 px-[22px]` default, `rounded` (8px), `font-bold`. Variants: `default` (rust/primary), `secondary` (paper-2 with border), `outline` (white with border), `ghost` (transparent, muted text), `link`, `destructive`. Sizes: `sm`, `default`, `lg`, `icon`. Supports `asChild` via Radix `Slot`.
- **`Input`** (`input.tsx`) — single-line text input. `h-10`, white card bg, 14px×10px padding, `rounded-md`. Inherits `bg-card`/`border-input` from alias layer. Standard HTML input props; supports `aria-invalid` styling.
- **`Textarea`** (`textarea.tsx`) — multi-line text input. Same shape as Input but `min-h-20`, `font-mono`, `field-sizing-content` (auto-grows).
- **`Select`** + `SelectTrigger` / `SelectContent` / `SelectItem` / `SelectGroup` / `SelectLabel` / `SelectSeparator` / `SelectValue` / `SelectScrollUpButton` / `SelectScrollDownButton` (`select.tsx`) — Radix-backed dropdown. Trigger has `sm` / `default` sizes. Content opens below by default (popper, start-aligned). Use for any dropdown with discrete options.
- **`Dialog`** + `DialogTrigger` / `DialogContent` / `DialogHeader` / `DialogFooter` / `DialogTitle` / `DialogDescription` / `DialogClose` / `DialogOverlay` / `DialogPortal` (`dialog.tsx`) — Radix-backed modal. **`DialogContent` accepts `size`: `sm` (480px) / `default` (560px) / `lg` (920px).** White card surface, `rounded-2xl`, `p-10`, paper backdrop. Close button auto-renders top-right (suppress with `showCloseButton={false}`).
- **`Card`** + `CardHeader` / `CardTitle` / `CardDescription` / `CardContent` / `CardFooter` / `CardAction` (`card.tsx`) — white-on-paper surface panel (`.auto-card` in proto). `rounded-xl`, `border-border`, `shadow-card`, `py-6`. Use for any bordered content surface; compose with the sub-parts.
- **`Badge`** (`badge.tsx`) — small pill label, `rounded-full`. Variants: `default` (rust filled), `secondary` (paper-2), `muted` (paper-2 + quiet text — most common in proto), `destructive`, `outline`. Supports inline children including `StatusDot` for dot-prefixed status pills.
- **`Tabs`** + `TabsList` / `TabsTrigger` / `TabsContent` (`tabs.tsx`) — Radix-backed tabs. `TabsList` has `default` (filled pill) and `line` (underline) variants. Default orientation horizontal; vertical supported via `orientation="vertical"`.
- **`Tooltip`** + `TooltipProvider` / `TooltipTrigger` / `TooltipContent` (`tooltip.tsx`) — Radix-backed tooltip. **Wrap the app (or section) in `TooltipProvider`** for tooltips to function. Ink-on-paper styling with arrow.

**Webnua-authored (use Webnua palette tokens; bright line in design-system above):**

- **`BrandMark`** (`BrandMark.tsx`) — canonical "Webnua ◆" wordmark: rotated rust diamond + Inter Tight wordmark, `font-extrabold tracking-[-0.03em]`. Sizes: `sm` / `default` (sidebar size) / `lg` (auth screen). Color is inherited via `text-*` on the parent — set `text-paper` on ink backgrounds, `text-ink` on paper. No business logic. **`SidebarBrand` composes this**; don't duplicate the glyph elsewhere.
- **`Eyebrow`** (`eyebrow.tsx`) — mono uppercase tag used as section/eyebrow label across both prototypes (`.sidebar-section-label`, `.modal-tag`, etc.). JetBrains Mono, `text-[10px]`, `tracking-[0.14em]`, `font-bold`, uppercase. Tones: `rust` (default, brand), `ink`, `quiet`. Optional `bullet` prop renders a leading colored dot via `::before`.
- **`StatusDot`** (`status-dot.tsx`) — 6px colored circle used as inline status/severity indicator (matches `.status-dot` in proto). Tones: `good` (default), `warn`, `info`, `rust`, `quiet`. Composable inside `Badge`/`Eyebrow` for dot-prefixed pills.
- **`Switch`** (`switch.tsx`) — bespoke 44×24 toggle, Radix Switch under the hood. Matches `.toggle` in proto exactly: rule-bg off / good-bg on, 18px white knob slides 20px on activate. Optional `label` prop renders a mono uppercase label adjacent (quiet off, good on — peer-data driven). Use for any boolean on/off control.

### `shared/` — app shell + cross-role composed components

- **`AppShell`** (`AppShell.tsx`) — top-level grid wrapper for the authed app. `grid-cols-[280px_1fr] min-h-svh bg-paper`. Takes `sidebar` slot + `children` (the main area). The 280px sidebar + 1fr main matches both prototypes exactly.
- **`Sidebar`** (`Sidebar.tsx`) — ink-bg, sticky-to-viewport, scrollable column. Structure only; children are passed in (brand, sections, items, footer). Compose via `ClientSidebar`/`AdminSidebar` rather than using directly.
- **`SidebarBrand`** (`SidebarBrand.tsx`) — sidebar-header composition: `BrandMark` (rendered `text-paper` on the ink sidebar bg) + a mono meta line below. Prop: `meta` (e.g. `"Client workspace"` / `"Operator console"`). For the plain wordmark on other surfaces, use `BrandMark` directly (e.g. the auth screen).
- **`SidebarSectionLabel`** (`SidebarSectionLabel.tsx`) — mono uppercase eyebrow used between sidebar groups (`// Your business`, `// Overview`, etc.). Renders `// {children}` styled paper-on-ink quiet.
- **`SidebarItem`** (`SidebarItem.tsx`) — nav row inside the sidebar. Icon + label + optional badge. Wraps `next/link`. Auto-detects active state from `usePathname`. Active state = left rust rail + lifted bg + rust-light icon. Takes a `NavItem` (`{ label, href, icon, badge? }`).
- **`SidebarUser`** (`SidebarUser.tsx`) — bottom-of-sidebar user card. Avatar circle + name + role meta. Props: `initial`, `name`, `role`.
- **`Topbar`** (`Topbar.tsx`) — page-level top bar inside `main`. Slots: `breadcrumb` (left), `middle` (centred, optional — admin step-tracker), `search` (centred, optional — a `GlobalSearchInput`; added Cluster 6 · 2, mutually exclusive with `middle`), `actions` (right, optional). Sticky to the main area with rule border.
- **`TopbarBreadcrumb`** (`Topbar.tsx`) — helper that renders `trail / trail / current` in mono uppercase. Pass as the `breadcrumb` slot of `Topbar`.
- **`PagePlaceholder`** (`PagePlaceholder.tsx`) — placeholder body block used by Phase-4 stub routes and any not-yet-built page. Eyebrow + title + description + a dashed-border "wiring only" hint card. Replace with real page content as each route is built.
- **`PageHeader`** (`PageHeader.tsx`) — canonical page-top block: rust mono `eyebrow` (string, rendered through `Eyebrow tone="rust" bullet`) + 40px Inter Tight `title` (ReactNode, so callers pass `<>Your <em>clients</em>.</>` for rust em-highlight) + optional `subtitle` (ReactNode, `<strong>` segments render ink-bold). Matches the `.screen-title-block`/`.greeting-block` pattern from both prototypes. Default `mb-8` rhythm.
- **`StatCard`** (`StatCard.tsx`) — single white-card stat tile: mono quiet `label` + 30px Inter Tight `value` (ReactNode, supports `<em>` for rust highlight) + optional `trend` line. `trendTone: 'good' | 'quiet'` (default `good`). Matches `.client-stat-card` (admin) and `.c-landing-stat` (client) 1:1. Compose in a `grid grid-cols-4 gap-3.5` row — no `StatRow` wrapper, the grid is too thin to abstract. **Cluster 6 · 1b** added an optional `chart` slot (ReactNode, rendered below the trend — additive, never replaces value/trend) for hosting a `MiniTrendBars` sparkline.
- **`MiniTrendBars`** (`MiniTrendBars.tsx`) — small bar sparkline: `data: number[]` (oldest → newest) + optional `labels` (default `3W / 2W / LW / NOW`). Bars scale to the series max; the final (newest) bar is rust-highlighted, the rest sit in the rule colour. Built for the overview hub's vs-prior stat cards (`StatCard`'s `chart` slot).
- **`DevRoleSwitcher`** (`DevRoleSwitcher.tsx`) — **STUB.** Floating bottom-right pill that shows the active stubbed user, lets you switch between the three users (Craig admin / Mark client / Anna view-only), cycle the view-as override (admin sessions only — click cycles `none → mark → anna → none`), open the `/dev/capabilities` matrix, or sign out. When view-as is active, the user pill shows `craig (admin) [as mark]` and the cycle button highlights rust-light. Mounted inside `(client)` + `(admin)` layouts + every shared-route layout. Delete with the rest of the auth stub.
- **`CapabilityGate`** (`CapabilityGate.tsx`) — wraps any control with capability-aware presentation. Props: `capability: Capability`, `mode: 'hide' | 'disable' | 'request'`, `children`, optional `disabledExplainer` (overrides the default `CAP_EXPLAINER` tooltip text), optional `requestContext: { pageId?, sectionId?, fieldKey? }`, optional `onRequestChange`. Has the cap → renders children. Lacks + `hide` → renders null. Lacks + `disable` → renders children inert (opacity-55, pointer-events-none) with a `Tooltip` showing the explainer. Lacks + `request` → renders `<RequestChangeAffordance>` with the cap's `requestLabel`. **`request` mode auto-falls-back to `hide` when the cap's `requestLabel` is empty** (currently: `viewBuilder`, `approve`) — deliberately so a labelless cap doesn't render a broken affordance.
- **`RequestChangeAffordance`** (`RequestChangeAffordance.tsx`) — hover overlay surfaced by `CapabilityGate mode="request"`. Children render visually-present-but-inert (opacity-55, pointer-events-none); a rust-soft/rust-bordered button absolutely-positioned on top reveals on `group-hover` / `focus-visible` showing `{label} →` in mono uppercase. Fires the `onClick` callback (in 1a: the stub just logs; real ticket creation wires up in a later session). Reusable standalone — anywhere you want the "hover to reveal request-change affordance over an inert control" pattern.
- **`WorkspaceContextBanner`** (`WorkspaceContextBanner.tsx`) — small pill that reinforces which workspace context the operator is in. Agency mode → paper-2 pill, `◆ All clients · birds-eye`. Sub-account mode → rust-soft pill with pulsing dot, `Sub-account · {clientName}`, plus an inline `← Back to agency` `Button` (suppress with `hideReturnButton`). Used on context-aware page headers (currently just `/settings/access`) so the operator always knows where they are even with the sidebar scrolled out of view. Returns `null` until hydrated to avoid SSR mismatch. Optional `agencyLabel` override.
- **`FilterChips`** (`FilterChips.tsx`) — `'use client'` rust-active / paper-inactive filter chip strip with optional count badges. Generic `{ id, label, count? }` chip type. Single-select; supports uncontrolled (`defaultActiveId`) or controlled (`value` + `onChange`). Mono `label` prop (e.g. `// CLIENT`) sits to the left of the chip row. Extracted in Phase 5 · Cluster 3 from the previously-separate `LeadFilterChips` + `CalendarClientFilterBar` (both deleted) once the third surface (`/automations` admin filter) made the third data point. Distinct from `shared/ChipSelector` — that's a single-select **pick** (ink-active, no badges); this is the filter-chip vocabulary (rust-active, optional count). Used by: admin leads inbox, admin calendar, admin automations.

### `shared/settings/` — settings + integrations screen pair (Phase 5 sessions A + B)

> **Architectural note — settings lives outside the role route groups.** Routes whose URL space is **shared** between roles can't live inside `(client)` or `(admin)` — Next.js refuses two route groups resolving to the same URL. Three such shared route trees exist at the top level: **`app/dashboard/`** (admin home + client home), **`app/settings/`** (the entire `/settings/*` tab space), and **`app/tickets/`** (`/tickets`, `/tickets/[id]`, `/tickets/new`). Each has its own `layout.tsx` ('use client', reads `useRole()`, picks `ClientSidebar` vs `AdminSidebar`, also mounts the `DevRoleSwitcher`). For `/dashboard` the page itself is a `'use client'` dispatcher with `_admin-content.tsx` / `_client-content.tsx` siblings — **plus `_hub-content.tsx`**: the admin branch dispatches a second time on `useIsAgencyMode()` (Cluster 6 · 1a), agency mode → `_admin-content` (the cross-client roster), sub-account mode → `_hub-content` (the single-client overview hub, Screen 20). For settings, role-specific tab slugs (`/profile`, `/workspace`, `/team`, `/notifications`, `/security`, `/help`, `/defaults`, `/api`, `/danger`, `/integration-defaults`, `/seats`) are reached only from the correct role+mode's nav, so their pages render statically without re-checking role; shared-slug tab pages (`/settings/integrations`, `/settings/billing`) are `'use client'` and dispatch on `useRole()` — per-role bodies live as `_admin-content.tsx` / `_client-content.tsx` siblings (underscore = naming convention, not a Next.js private rule). **Cluster 8 · Session 4a — `/settings/*` is workspace-mode-aware.** The nav set is resolved at runtime by `useSettingsNav()` (role + `useIsAgencyMode()`); the settings `layout.tsx` carries a guard that bounces an operator who reaches a wrong-mode tab by direct URL (agency-only: workspace/defaults/integration-defaults/seats/api; sub-account-only: profile/notifications) back to the mode-aware `/settings` index redirect. No tab page passes a static nav anymore. The resolver-backed sub-account *content* (per-account integration overrides, seat-limit resolution) lands in Session 4b. Admin-only URLs that aren't shared still live inside `(admin)` — e.g. `/clients/new/*` (the onboarding wizard) and `/integrations` (the cross-client matrix). When real auth ships, the shared-route layouts become the place to check the JWT role and 404/redirect on mismatched access; nothing else moves.

- **`SettingsShell`** (`settings/SettingsShell.tsx`) — wraps `PageHeader` + `SettingsContextBanner` + the two-column settings layout (`220px` nav left + `1fr` panel right, `gap-7`, `items-start`). Used by every tab page under `app/settings/*` (see the architectural note below — settings lives **outside** the `(client)`/`(admin)` route groups because the URL space is shared between roles). Props: `eyebrow`, `title`, `subtitle`, `children` (the right-column panel content). **No `items` prop** (Cluster 8 · Session 4a) — the nav resolves itself from role + workspace mode. Provides the page padding (`px-10 py-10`) — each tab page renders `Topbar` separately above it.
- **`SettingsNav`** (`settings/SettingsNav.tsx`) — vertical tab nav rendered as `next/link`s. Active state from `usePathname` (matches exact `href` or any deeper path under it). Active style = ink-bg + paper text; inactive = ink-quiet text on paper, hover bumps to paper-2 + ink. **Resolves its own items via `useSettingsNav()`** (Cluster 8 · Session 4a) — no `items` prop. `useSettingsNav` (`lib/nav/use-settings-nav.ts`) dispatches on role + `useIsAgencyMode()`: client → `clientSettingsNav`; operator + agency mode → `adminSettingsNav` (the agency HQ set); operator + sub-account mode → `subAccountSettingsNav`.
- **`SettingsContextBanner`** (`settings/SettingsContextBanner.tsx`) — `'use client'` wrapper mounting `WorkspaceContextBanner` on every settings page via `SettingsShell`. Operators only — renders null for client-role users (no agency/sub-account axis). Replaces the per-page manual banner mounts.
- **`SettingsSection`** (`settings/SettingsSection.tsx`) — vertically-stacked section block inside a `Card`. Props: `heading` (ReactNode, supports `<em>` for rust highlight — covers both the plain `settings-section-h` and the `*-content-h` rust-em variants from the prototype), `description` (ReactNode, `<strong>` renders ink-bold), `children`. Auto-separates from the next section with `border-b border-paper-2 pb-7 mb-7`, and removes the border + bottom rhythm when it's the last child (`last:border-b-0 last:mb-0 last:pb-0`).
- **`SettingsFieldRow`** (`settings/SettingsFieldRow.tsx`) — single label/value row inside a `SettingsSection`. Props: `label` (string), `sub` (optional string under label), `value` (ReactNode — wrap a span in `className="mono"` for the JetBrains Mono treatment), `action` (optional ReactNode — typically a "Edit ✎" / "Request change ✎" inline affordance). Grid `220px 1fr`, dotted bottom rule between rows, no rule on the last row.
- **`IntegrationCard`** (`settings/IntegrationCard.tsx`) — the integration row used on both `/settings/integrations` pages. Logo block (56×56, brand-tinted) + name + status pill (inline with name on both roles — converged on the better layout) + description + optional `meta` line + action button. Props: `name`, `description` (ReactNode), `status: 'connected' | 'warning' | 'missing' | 'partial'`, optional `statusLabel` to override the default ("Connected"/"Needs reauth"/"Not connected"/"Partially connected"), `logo: { initial, tone? }` where `tone` is `'gbp' | 'meta' | 'ga' | 'gads' | 'stripe' | 'generic'` (defaults to generic = ink-bg / rust-light glyph), optional `meta` (ReactNode — colour shifts to warn on `warning` status), and `action: { label, href?, variant? }`. Re-exports `IntegrationStatus`, `IntegrationLogoTone`, `IntegrationAction`, `IntegrationCardProps`.
- **`IntegrationProgressHero`** (`settings/IntegrationProgressHero.tsx`) — the ink-bg rust-tagged progress band at the top of the client `/settings/integrations` page. Props: `tag` (rust mono pill string), `title` (ReactNode, `<em>` = rust highlight, 26px headline), `subtitle` (ReactNode, `<strong>` = paper-bold), `connected` + `total` (numeric — drives the big mono fraction + the rust→rust-light gradient fill), `remainingLabel` (ReactNode for the right-of-bar caption). For the admin cross-client matrix's different hero shape (stat tiles, not a single fraction), see `admin/integrations/IntegrationMatrixHero`.
- **`SettingsPanel`** (`settings/SettingsPanel.tsx`) — extracted from the inline `<Card className="py-7"><CardContent className="px-8">` pattern (parked-decision trigger fired on second-use). White-card surface that holds a sequence of `SettingsSection` children with the correct vertical rhythm. Single `className` knob; no other props. Every tab page uses this; no tab page should re-inline the `Card` + `CardContent` composition.
- **`NotificationRow`** (`settings/NotificationRow.tsx`) — single notification preference row used on the client Notifications tab. Props: `label`, optional `sub`, `channels: NotificationChannel[]` (e.g. `['sms','email','push']` — declares which channels are valid for this notification), `active: NotificationChannel[]` (which are currently on). Renders each channel as a dot+label pill — good-green when on, ink-quiet when off. Re-exports `NotificationChannel` type.
- **`BillingPlanCard`** (`settings/BillingPlanCard.tsx`) — ink-bg plan card used at the top of both client + admin `/settings/billing`. Props: `tag` (rust-light mono string), `name` (ReactNode, `<em>` = rust-light highlight, 26px headline), `meta` (ReactNode, `<strong>` = paper-bold), `action` (ReactNode — typically a `Button`). The action slot is open so each role can use its own button styling (client uses a quiet secondary "Talk to Craig"; admin uses the rust primary "Change plan").
- **`InvoiceList`** (`settings/InvoiceList.tsx`) — bordered table of invoice rows used on both billing tabs. Takes `invoices: Invoice[]` where `Invoice = { id, date, description, amount, status: 'paid' | 'pending' | 'failed' }`. Includes a mono header row + a per-status colour-coded status pill + a rust "PDF ↓" affordance per row. Re-exports `Invoice`.
- **`SecurityRow`** (`settings/SecurityRow.tsx`) — bordered row used on the client Login + security tab (credentials + 2FA sections). Props: `heading`, optional `status: { tone: 'verified' | 'warn', label? }` (renders an inline dot-prefixed pill, good-green for verified, rust for warn/recommended), `description` (ReactNode), `action: { label, variant? }` (rendered through `Button`). Re-exports `SecurityRowStatus`.
- **`SessionRow`** (`settings/SessionRow.tsx`) — single active-session row in Login + security. Props: `icon` (emoji glyph), `device`, optional `isCurrent` (renders the rust "THIS DEVICE" pill and suppresses the revoke action), `meta` (mono ink-quiet metadata), `when` (right-aligned mono timestamp). Dotted bottom rule between rows, no rule on the last.
- **`HelpFaqItem`** (`settings/HelpFaqItem.tsx`) — collapsible FAQ entry on the Help tab. Uses native `<details>` for state (no JS), so it works in server components and degrades gracefully. Props: `question`, optional `answer` (ReactNode — items without answers render as question-only, matching the prototype's "expand to see answer" UX), `defaultOpen`. The `+`/`−` glyph toggles via `group-open:` Tailwind variants.
- **`TeamRow`** (`settings/TeamRow.tsx`) — admin Team-tab member row. 5-column grid: avatar (rust-gradient circle) + name & email + role & sub-role + status pill (good for active, rust for pending — dot-prefixed) + actions (mono action chips, with `tone: 'danger'` flipping the hover to warn-bg).
- **`ApiKeyRow`** (`settings/ApiKeyRow.tsx`) — admin API + webhooks tab. 4-column grid: name + masked token, created mono meta, used mono meta, revoke action. Mono token uses JetBrains Mono. The created/used meta slots are `ReactNode` so callers can pass `<>Created <strong>21 Mar 2026</strong></>` for the ink-bold treatment.
- **`WebhookEventRow`** (`settings/WebhookEventRow.tsx`) — single webhook-delivery log row. 3-column mono grid: time (quiet) + event name (rust bold) + status (right-aligned good for `ok`, warn for `failed`). `statusLabel` defaults to `✓ 200 OK` / `✗ Failed` but is overridable.
- **`DangerRow`** (`settings/DangerRow.tsx`) — warn-tinted destructive-action row on the Danger zone tab. Props: `heading`, `description` (ReactNode), `action: { label, solid?, tone? }`. `solid: true` renders a filled destructive `Button`; otherwise `tone: 'neutral'` (ink-bordered, for non-destructive actions like Export) or `tone: 'destructive'` (warn-bordered, the default). Border + bg colour-coded across the whole row.
- **`CapabilityToggleGrid`** (`settings/CapabilityToggleGrid.tsx`) — admin-only `/settings/access` grid. Renders one card per client user (operators are excluded — they have workspace-wide access through their role); inside each card, one row per website the user has access to; inside each row, a wrapping flex of `<Switch>`-with-label cells (one per capability). Role-default caps (currently just `viewBuilder` for clients) render as always-on disabled switches — clients can't be revoked below their role floor. Props: `users: CapabilityToggleGridUser[]` (`{ id, displayName, email, role, websites: CapabilityGridWebsite[], capabilities: Set<Capability> }`), `onToggle: (userId, websiteId, capability, enabled) => void`. `CapabilityGridWebsite` is a minimal local display shape (`{ id, clientName, domain }`) — decoupled from the full `Website` type so the grid doesn't pull in brand/version/page-tree fields it never reads. Callers (e.g. `/settings/access`) project from `Website` to this shape. Stateless — the parent owns the grant store via `setUserGrant` on the user-stub context.
- **`ForcePublishLog`** (`settings/ForcePublishLog.tsx`) — read-only audit log shown below the cap grid on `/settings/access`. 3-column grid (timestamp / event narrative / version id). Each row: mono timestamp, ink-bold actor + page + rust client name in the body, italic ink-mid reason quote, mono version id. Empty state renders a dashed-border card describing what the log will hold once force-publish actions begin firing. Props: `entries: ForcePublishEntry[]`.
- **`AccessClientRosterRow`** (`settings/AccessClientRosterRow.tsx`) — single row in the `/settings/access` **agency-mode** client roster. 5-col grid (logo / name+meta / user count / caps-granted summary / drill-in button). Empty clients (no users) get a dimmed logo + paper-2 swatch + ghost "Open →" button; populated clients get rust-light glyph on ink + secondary "Drill in →" button. The button calls `onDrillIn(clientId)` which the page wires to `useWorkspace().setActiveClientId` — clicking it switches workspace context and re-renders the page in sub-account mode.

### `shared/tickets/` — tickets inbox + detail (Phase 5 sessions A + B)

> **Architectural note — tickets lives outside the role route groups, same reason as settings.** Both roles need `/tickets` and `/tickets/[id]`, so `app/tickets/` lives at the top level with its own `layout.tsx` (`'use client'`, reads `useRole()`, picks `ClientSidebar` vs `AdminSidebar`, mounts `DevRoleSwitcher`). The inbox page (`app/tickets/page.tsx`) and the detail page (`app/tickets/[id]/page.tsx`) are both `'use client'` dispatchers — per-role bodies live as `_admin-content.tsx` / `_client-content.tsx` siblings (same `_`-prefixed convention as `/settings/integrations`). `/tickets/new` is a client-only placeholder route (`PagePlaceholder`) reachable from the client inbox's "+ New ticket" CTA — the submit-form arrives in a later session. The `[id]` segment matches any non-`new` slug; for the stub layer it renders a single canonical detail regardless of the actual id (matches the prototype, which only fleshed out TKT-0247).

**Inbox (session A):**

- **`TicketsHero`** (`tickets/TicketsHero.tsx`) — ink-bg rust-tagged hero band at the top of both `/tickets` pages. Props: `tag` (mono pill string), `title` (ReactNode, `<em>` = rust highlight, 30px headline), `subtitle` (ReactNode, `<strong>` = paper-bold), `right` (ReactNode slot — client passes a "+ New ticket" `Button`; admin passes a row of `TicketsHeroStat`s). Standalone — NOT extracted into an `InkHero` primitive (see parked decisions). Sibling export **`TicketsHeroStat`** renders one of the admin hero's right-aligned numbers + mono label, with `tone: 'warn' | 'rust' | 'neutral'` controlling the `<em>` colour.
- **`TicketTabsBar`** (`tickets/TicketTabsBar.tsx`) — `'use client'` pill-tab strip with optional counts. Active tab = ink-bg paper-text rounded-full + count chip on paper/15; inactive = ink-quiet text + count chip on ink/8. Props: `tabs: TicketTab[]` + `defaultActiveId?`. Local `useState` active state — fine for the stub layer; lift when real filtering wires up.
- **`CategoryTile`** (`tickets/CategoryTile.tsx`) — colored mono-glyph square used in the client-variant `TicketRow` as the left-cell category indicator. 6 categories × 6 colors (website→rust, marketing→info, campaigns→plum, reviews→amber, billing→good are all Webnua palette tokens; `other` still uses inline `#5a5a5a` — flag if a third use lands and we'll tokenise that too). `size: 'sm' | 'md'` (md is the row size; sm available for tighter contexts).
- **`pills.tsx`** (`tickets/pills.tsx`) — four small pill primitives exported together so all status/category/urgency colour decisions live in one file: **`StatusPill`** (open/in_progress/blocked/done with `reviewAware` flag — when true, `in_progress` + `awaiting === 'client'` renders as a rust-outlined "Review" pill; optional `label` overrides the default text for e.g. "Open · awaiting your reply" in the detail header), **`UrgencyPill`** (rush/soon/none), **`CategoryPill`** (admin row's pill-style category cell), **`AttentionPill`** (rust-filled "Reply needed" / "Draft ready"). Webnua-authored — uses Webnua palette tokens directly (not `Badge`, which keeps shadcn role vocab).
- **`TicketRow`** (`tickets/TicketRow.tsx`) — the single canonical row, discriminated by `variant: 'client' | 'admin'`. Both variants are `next/link` rows with shared "needs attention" treatment (left rust rail + faint rust bg) — converged from the prototype's `.attention` (client) and `.unread` (admin) variants per the agreed unification. Client variant: `auto 1fr auto auto auto` grid — `CategoryTile` + title+preview (with inline `AttentionPill` when `awaiting === 'client'`) + category label + `StatusPill reviewAware` + age. Admin variant: `20px 180px 1fr 110px 120px 110px 80px` grid — checkbox + client mini-card (tone-tinted avatar + name + mono meta) + title+preview + `CategoryPill` + `StatusPill` + `UrgencyPill` + age. Re-exports `TicketRowProps`, `ClientTicketRowProps`, `AdminTicketRowProps`.

**Detail (session B):**

- **`TicketDetailLayout`** (`tickets/TicketDetailLayout.tsx`) — 2-column grid wrapper for the detail page: `1fr 320px`, items-start, gap-4.5. Wraps `main` in a bordered white card (rounded-[14px], border-ink/8); the right `side` column is a plain flex-col so each `RailCard` carries its own surface. Used by both role detail-content files.
- **`TicketDetailHeader`** (`tickets/TicketDetailHeader.tsx`) — sits at the top of the detail main card. Renders a `next/link` back button + pills row (slot — caller composes `TicketIdLabel` + `CategoryPill` + `StatusPill` + `UrgencyPill`) + 24px ink title (ReactNode, `<em>` = rust) + optional `meta` line (ReactNode, `<strong>` = ink-bold; admin uses this slot to inline a client mini-avatar + "From X" line). Sibling export **`TicketIdLabel`** renders the TKT-0247 mono semibold label — kept inline because it's just decorated text, not a pill.
- **`TicketThreadMessage`** (`tickets/TicketThreadMessage.tsx`) — single message bubble: 36px avatar + bubble with author header (name + role chip + time) + body. `author: 'client' | 'operator'` toggles the surface (paper-2 + ink for client, ink + paper for operator). `children` is the bubble body — pass `<p>...</p>` paragraphs. Same component handles both prototypes' message shapes (the client `.ctd-msg` and admin `.td-msg` markup converged 1:1).
- **`TicketReply`** (`tickets/TicketReply.tsx`) — `'use client'` reply composer: optional mono `label` ("// Reply to Craig" on client; admin omits), optional `chips` (click-to-fill quick-reply buttons above the textarea — client only), `Textarea` (auto-resizing, paper card on paper-2 bg), tools row + `Button` send. `tools: { icon, title }[]` lets each role supply its own toolbar (client: attach/image/voice; admin: attach/link/template). Defaults to a single attach tool if omitted. The chip-to-textarea population is local state — wire to real send when backend lands.
- **`TicketStatusCard`** (`tickets/TicketStatusCard.tsx`) — discriminated by `mode: 'display' | 'pick'`. **Display** renders a static `<em>Open</em> · awaiting your reply` headline (statusLabel is ReactNode so the caller wraps the highlighted word in `<em>` for rust-light) + an ink-bg dot + optional description paragraph. **Pick** renders an interactive list of `{ status, label }` options with a rust-light `✓` on the active row; local `useState` tracks active (lift when real status mutation wires up). Both modes use `RailCard tone="dark"` underneath. (The card wrapper + property row are the shared `RailCard` / `RailPropertyRow` — see the `shared/` rail-primitives entry below.)
- **`TicketActionRow`** (`tickets/TicketActionRow.tsx`) — single button-as-row with leading icon glyph + label. Bordered ghost button (`border-ink/10 → border-rust` on hover). Rows auto-space with `mt-1.5` (first row resets via `first:mt-0`). Used for the side-panel "Quick actions" lists; each role passes its own action set.

### `client/` — client-shell-only composed components

- **`ClientSidebar`** (`ClientSidebar.tsx`) — thin composition: `SidebarBrand` + `ClientWorkspaceCard` + `clientNav` rendered through `SidebarSectionLabel` + `SidebarItem` + `ClientSupportCard` + `SidebarUser`. Pulls data from `src/lib/nav/client-nav.ts`.
- **`ClientWorkspaceCard`** (`ClientWorkspaceCard.tsx`) — rust-tinted single-workspace identity block that sits below the brand in the client sidebar. Props: `initial`, `name`, `status`. The status renders with a small good-green dot prefix.
- **`ClientSupportCard`** (`ClientSupportCard.tsx`) — "// Managed by …" support block above the user footer. Takes a `contact: { label, org, description, ctaLabel, ctaHref }` prop — the contact is config, not hardcoded, because the support contact will change.

### `client/dashboard/` — client dashboard (Phase 6 · Cluster 7 · Screen 1)

> The client home — client `/dashboard`. Like the operator hub it is **pure assembly**: `app/dashboard/_client-content.tsx` composes these four dashboard-specific components with reused ones (`PageHeader`, `StatCard`, `MiniTrendBars`, `ActivityFeed`, and the shared `FunnelConversionBars`). The route dispatcher (`app/dashboard/page.tsx`) renders this for client-role users; admin role dispatches to the agency roster / hub.

- **`ClientDashboardHero`** (`dashboard/ClientDashboardHero.tsx`) — the urgent ink-hero at the top of the client dashboard (Screen 1). Built on the `lib/ink-hero.ts` recipe. Big 84px rust-light callback count + label + a sub-line **composed from the structured `callouts`** (`{ name, age, note }[]`) — never a stored sentence + an inbox CTA `Button`. Takes `hero: DashboardUrgentHero`. The `threshold` field drives the mono tag (`overdue` → `// NEEDS YOU NOW`).
- **`DashboardQueueCard`** (`dashboard/DashboardQueueCard.tsx`) — a queue card: header (heading + count `Badge` + link) over a list of avatar rows (inner `QueueItemRow`). Used twice on the dashboard — follow-ups due and today's jobs. Each `DashboardQueueItem` carries an avatar `initial` + optional `avatarTone` (`good`/`rust`), title, sub, optional inline `tag` (`urgent`/`done`/`next` colour key), time, and optional `href` (whole row links). Takes `queue: DashboardQueue`.
- **`LandingSnapshotCard`** (`dashboard/LandingSnapshotCard.tsx`) — the landing-page snapshot: card header (domain + status meta) over a 4-up grid of reused `StatCard`s. Takes `snapshot: LandingSnapshot`.
- **`FunnelSummaryBand`** (`dashboard/FunnelSummaryBand.tsx`) — ink-bg strip below the funnel bars. Composes plain prose from the structured `ClientFunnelSummary` (weak-point transition + drop count + operator note + health note) + a rust mono CTA. **Sibling of the operator hub's `HubInsightBand`, not a reuse** — that component consumes a structured `FunnelInsight` (severity + suggested-action); the client framing is plainer and carries an operator-note slot instead. Two clean siblings beat one component with conditional slots.

### `shared/funnels/` — conversion-bar funnel

- **`FunnelConversionBars`** (`funnels/FunnelConversionBars.tsx`) — six horizontal conversion bars (Landing visits → … → Reviewed), each a gradient bar scaled by `pct` + count + pct. Header carries a reused `FunnelPeriodToggle`. Distinct from `FunnelFlow` (3-step cards) and `CampaignTrendChart` (grouped bars). Takes `funnel: HubFunnelConversion`. **Promoted from `admin/hub/` to `shared/` in Cluster 7** (second-surface trigger — the operator hub Screen 20 and the client dashboard Screen 1 both use it).

### `admin/` — admin-shell-only composed components

- **`AdminSidebar`** (`AdminSidebar.tsx`) — thin composition: `SidebarBrand` + `Overview` section + `AdminClientPicker` + `Workspace` section + `AdminWorkspaceBlock` + `SidebarUser`. Pulls data from `src/lib/nav/admin-nav.ts` + `admin-clients.ts`.
- **`AdminClientPicker`** (`AdminClientPicker.tsx`) — collapsible client switcher in the admin sidebar. **Wired to the workspace context** (see `lib/workspace/workspace-stub.tsx`): closed state shows the active sub-account OR the agency-mode "All clients ◆" identity; open state lists an "All clients · agency birds-eye" entry first (rust-active when in agency mode), then every client (rust-active when that's the active sub-account), then "Add new client" + "View all clients". Clicking a client calls `setActiveClientId`; clicking the agency entry calls `clearActiveClient`. Takes `clients: AdminClient[]` (no `activeClientId` prop — pulled from context).
- **`AdminWorkspaceBlock`** (`AdminWorkspaceBlock.tsx`) — **static block**, not a selector. There's only one workspace ("Webnua") for now. Renders `// Workspace` label + workspace name above the user footer. Grow into a real selector if multi-workspace lands.
- **`ContinueSetupHero`** (`ContinueSetupHero.tsx`) — admin-dashboard "mid-setup" hero card. White surface with rule border, `rounded-3xl`, `px-10 py-9`, two-column grid (content + right-aligned CTA). Props: `tag` (rust mono eyebrow), `title` (ReactNode, supports `<em>` for rust), `description`, `meta` (`ReactNode[]` — segments are separated by `·` and any `<strong>` renders ink-bold), `ctaLabel`, `ctaHref` (rendered through `Button asChild` + `next/link`). Lives in the operator dashboard; admin-only — no client analogue.
- **`ClientListRow`** (`ClientListRow.tsx`) — admin-dashboard live-client row. White card surface, `rounded-lg`, 6-column grid (`32px 1fr 120px 100px 120px 80px`). Composes a 32×32 ink-bg avatar tile with a rust-light glyph, name + meta lines, a status-dot pill (`good`=live, `rust`=setup), `leadsPerWeek` (rust number), `spend`, and a rust "Open →" affordance. Whole row is a `next/link`. Props: `{ id, initial, name, meta, status: 'live'\|'setup', leadsPerWeek, spend, href }`. Re-export `ClientStatus` type. Will be reused on the future "all clients" admin list.

### `admin/hub/` — single-client overview hub (Phase 6 · Cluster 6 · Session 1b)

> The hub is admin sub-account `/dashboard` (Screen 20). It is **pure assembly** — `_hub-content.tsx` composes these hub-specific components with reused ones (`WorkspaceContextBanner`, `RailCard`, `CalendarTodayPanel`, `ActivityFeed`, `StatCard`, `MiniTrendBars`, `FunnelPeriodToggle`, and the now-shared `FunnelConversionBars`). The client dashboard (Screen 1) is the parallel Cluster 7 build — see `client/dashboard/` below.

- **`ClientHubHero`** (`hub/ClientHubHero.tsx`) — the hub's ink hero. First consumer of the `lib/ink-hero.ts` recipe. Renders the rust status tag + client name + identity facts + composed management line + the big lead-focal block (count + breakdown + CTA) + a 4-tile 7-day stat strip. Takes `hero: HubHero`.
- **`OperatorActionBar`** (`hub/OperatorActionBar.tsx`) — the `// OPERATOR ACTIONS` bar at the top of the hub. Operator-only chrome — mono label + action chips (Edit page / Manage automations / Edit ad campaign / Billing / Impersonate) + a "viewing as operator" note. Each chip is an `OperatorAction` typed by `kind` (vision §7). Takes `actions: OperatorAction[]`.
- **`HubInsightBand`** (`hub/HubInsightBand.tsx`) — thin ink-bg insight strip at the foot of the hub. Composes its sentence from `FunnelInsight` parts (severity lead + reasoning + action). Built as a thin sibling rather than reusing `CampaignManagedBand` — that component's 56px-icon-tile shape is chunkier than this one-line strip, so reuse would have fought (evaluate-at-build outcome). Takes `insight: FunnelInsight` + `cta`.

### `admin/integrations/` — cross-client integrations matrix (Phase 5 session B)

Components for the operator-only `/integrations` screen — workspace-wide view across every client. Sits at `(admin)/integrations`, separate from the per-workspace `/settings/integrations` panel.

- **`IntegrationMatrixHero`** (`integrations/IntegrationMatrixHero.tsx`) — ink-bg hero with rust-tagged pill + 28px headline + subtitle + N right-aligned stat tiles. Props: `tag` (mono string), `title` (ReactNode, `<em>` = rust), `subtitle` (ReactNode), `stats: MatrixHeroStat[]` where each stat is `{ num: ReactNode, label: string, tone?: 'good' | 'warn' | 'bad' | 'neutral' }`. `num` is a ReactNode so callers can pass `<em>14</em>` to colour-key the number against the tone. Different shape from the client-side `IntegrationProgressHero` (stat tiles vs single fraction) — kept as a sibling component on purpose.
- **`IntegrationMatrix`** (`integrations/IntegrationMatrix.tsx`) — the table itself. Header bar with title + filter chips, then a `220px + repeat(N, 1fr) + 100px` grid of column headers and per-client rows. Each cell is a coloured circle (`connected` = good check, `warning` = warn `!`, `missing` = dashed-border `×`). The Setup column holds a mini fraction + progress bar tinted by progress level (good = full, warn = ≥50%, bad = <50%). Avatars use `MatrixClientAvatarTone` (`voltline`/`freshhome`/`keyhero`/`flowline`/`generic`) — colours come from the prototype's per-client brand swatches. Re-exports `MatrixCellStatus`, `MatrixIntegrationColumn`, `MatrixClientRow`, `MatrixClientAvatarTone`, `MatrixFilter`.
- **`IntegrationMatrixActionCard`** (`integrations/IntegrationMatrixActionCard.tsx`) — the right-rail action cards under the matrix ("Needs your attention", "Critical gaps"). Props: `heading`, `badge: { label, tone: 'warn' | 'info' }`, `description` (ReactNode), `items: ActionItem[]`, optional `tone: 'attention'` to flip to the warn-tinted gradient background. Each `ActionItem` is `{ id, clientInitial, clientTone?, text, cta }` — `clientTone` is a Tailwind bg class (e.g. `'bg-rust'`) for the avatar tile. Re-exports `ActionItem`.

### `admin/tickets/` — admin-only ticket-inbox extras (Phase 5 session A)

- **`AdminTicketsFilterBar`** (`tickets/AdminTicketsFilterBar.tsx`) — `'use client'` filter-chip strip + search input. Right-side of the admin inbox toolbar; pairs with `TicketTabsBar` on the left in a flex `justify-between` row. Props: `active: string[]` (rust-filled chips with `×`) + `available: string[]` (paper-2 outlined chips like "+ Category" / "+ Client"). Search input uses `Input` with a `⌕` placeholder. Stubbed interactivity — chips don't filter yet, search input is local-state only.

### `shared/builder/` — generic wizard primitives (Phase 5)

> **Architectural note — generic by design.** These are role-agnostic wizard primitives. The admin client-onboarding flow (`/clients/new/*`) uses them now; the universal page builder will reuse them later. Don't bake admin-isms in. Anything funnel-specific lives in `admin/onboarding/` instead.

- **`BuilderLayout`** (`builder/BuilderLayout.tsx`) — two-column wizard shell: `480px` form panel (white-card, `rounded-xl border border-rule`) + `1fr` preview panel (sticky `top-[88px]`, `overflow-hidden`, same chrome). Props: `form` (left content), optional `preview` (right content), `className`. When `preview` is omitted the form fills the row — used by the full-width steps (automations, review, published).
- **`BuilderStepHeader`** (`builder/BuilderStepHeader.tsx`) — wizard step header: rust mono `eyebrow` (string, e.g. `// Voltline · Step 1 of 6`) + 40px Inter Tight `title` (ReactNode, `<em>` = rust) + optional `subtitle` (ReactNode, `<strong>` = ink-bold). Same shape as `PageHeader` but spaced for sequential step rhythm (`mb-7` instead of `mb-8`). Kept as a sibling because the prototype's `screen-title-block` has different vertical rhythm + the step number lives in the eyebrow.
- **`BuilderFooterActions`** (`builder/BuilderFooterActions.tsx`) — bottom-of-form Back/Continue row. Props: `progress` (mono uppercase label, ReactNode, `<strong>` = ink) + `actions` (ReactNode — typically two `Button`s) + `bordered: boolean` (default `true` — adds the `mt-9 border-t pt-5` rhythm; pass `false` when the footer sits outside a form panel).
- **`BuilderField`**, **`BuilderInput`**, **`BuilderTextarea`**, **`BuilderFormSection`**, **`BuilderFormRow`** (`builder/BuilderField.tsx`) — the form-row toolkit. `BuilderField` wraps label + input + optional helper; supports an optional `hint` slot on the same line as the label (right-aligned, rule-coloured) — used by the "Website (optional · for AI to scrape)" pattern. `BuilderInput` / `BuilderTextarea` carry a `variant: 'default' | 'ai'` knob — `'ai'` swaps to the rust-soft fill + rust border for AI-drafted fields (matches `.form-input.with-ai`). `BuilderFormSection` is a section block with a dotted-rule separator (auto-suppressed on last). `BuilderFormRow` is a `grid-cols-2 gap-3` pair-row for two side-by-side fields.
- **`AIPill`** (`builder/AIPill.tsx`) — small `✦ AI-DRAFTED` chip used inline inside `BuilderField` labels. Rust-on-rust-soft, mono `text-[9px]`, the ✦ glyph lives in the `::before` so the children slot is free for any label override (`Will AI-draft` / `AI-drafted` / `AI-suggested · edit prices`).
- **`PreviewPanelBar`** (`builder/PreviewPanelBar.tsx`) — `'use client'` chrome bar at the top of any builder preview pane. Props: `domain` (renders the `● Live · {domain}` rust pulsing pill) + `defaultDevice: 'desktop' | 'mobile'`. Internal `useState` toggles between Desktop/Mobile tabs — stubbed visual state for now, lift to a callback when the preview actually responsive-renders.

### `admin/onboarding/` — client onboarding (funnel build) flow (Phase 5)

> **Architectural note — the wizard lives at `/clients/new/<step>`** under the `(admin)` route group (Session 7 reshaped it — see `builder-design.md §5`). 8 routes: `/basics`, `/idea`, `/offer`, `/trust`, `/draft`, `/automations`, `/review`, `/published` (and `/clients/new` redirects to `/basics`). Per-step URLs keep the back button and review-card "Edit ✎" jumps working natively. The flow is launched from the "+ Add new client" button on `/dashboard` (the admin home) AND from the Voltline mid-setup hero's "Continue setup →" CTA (both wired to `/clients/new/basics`). Steps 1-4 are single-column forms; the trust step triggers generation; Step 5 (`/draft`) mounts the wizard-frame editor walk-through (`WizardSectionEditor`); Steps 6-8 go full-width.

- **`ReframeOptionCard`** (`onboarding/ReframeOptionCard.tsx`) — `'use client'` selectable option card used on the Big Idea step. Props: `tag` (mono label string) + `text` (ReactNode, `<em>` = rust) + `reason` (string) + `selected: boolean` + `onSelect: () => void`. Selected state swaps the border to 2px rust + rust-soft bg + adds the rust-filled SELECTED pill in the tag row.
- **`JobsMenuEditor`** (`onboarding/JobsMenuEditor.tsx`) — the white-card jobs grid editor (Trust + jobs step). Props: `jobs: JobsMenuItem[]`. Auto-computes the flat/quote split for the header label. Each row uses an internal `JobRow` (`auto 90px 100px 24px` grid: name + price input + FLAT/QUOTE flag pill + drag handle). Type-tinted pills: `flat` = good-on-good-soft, `quote` = info-on-info-soft. Footer carries the rust "⊕ Add job to menu" affordance. Stubbed — rows aren't actually draggable, the price inputs are `readOnly`.
- **`AutomationCard`** — **moved to `shared/automations/AutomationCard.tsx`** in Phase 5 · Cluster 3 · Session A (the same component, no shape change — just relocated so the upcoming `/automations` surfaces can share it). Onboarding still consumes it from the new path. See the `shared/automations/` inventory entry below for full props.
- **`ReviewCard`** (`onboarding/ReviewCard.tsx`) — the white review-grid card used on the Review + publish step. Props: `heading` (string) + `editHref` (string — the per-step `stepHref()` of the section being reviewed; rendered as a rust "Edit ✎" link) + `details: { label, value }[]` (each row = mono uppercase label + Inter Tight value, `<em>` = rust). Optional `children` slot for non-standard bodies — the "Automations" review card uses this to render its 2-up automation-status grid.
- **`PublishCTACard`** (`onboarding/PublishCTACard.tsx`) — the ink-bg rust-glow publish CTA card at the bottom of the Review step. Props: `title` (ReactNode, `<em>` = rust-light) + `description` (ReactNode, `<strong>` = paper-bold) + `ctaLabel` (string) + `ctaHref` (string — the next step, typically `/clients/new/published`). Renders the CTA through `next/link` with the rust glow shadow.
- **`PublishedSuccessHero`** (`onboarding/PublishedSuccessHero.tsx`) — published-state hero: 96px good-tinted ✓ icon + 36px headline + subtitle + URL row with copy/open chips. Props: `title` (ReactNode, `<em>` = rust) + `description` (string) + `url` (string) + optional `scheme: string` (default `'https://'`). URL row is a single inline block with the scheme dimmed + LIVE good pill + ink "Copy link" chip + rust "Open ↗" chip.
- **`NextStepCard`** (`onboarding/NextStepCard.tsx`) — single "what happens next" card used in the 3-up grid below the success hero. Props: `num` (mono rust eyebrow string like `// 01 · NEXT 24 HOURS`) + `title` + `description`.

### `lib/onboarding/` — wizard state + stub data

- **`types.ts`** — `OnboardingStepSlug` (`basics | idea | offer | trust | draft | automations | review | published`), `ONBOARDING_STEPS` (7 fill-in steps — `published` is a post-publish state, excluded) + `ONBOARDING_TOTAL_STEPS` (7) + `stepHref(slug)` + `getStepNumber(slug)` helpers. Plus the form-shape types: `BusinessBasics`, `ReframeOption`, `OfferDetails`, `TrustSignal`, `JobsMenuItem`, `Automation` + `AutomationStep` (`channel: 'sms' | 'email'`), `NextStep`. (The `FunnelPreviewState` / `Funnel*Config` types were deleted in Session 7 alongside `FunnelLandingPreview`.)
- **`voltline-build.tsx`** — the wizard's Q&A-input + automations stubs: `voltlineBasics`, `voltlineReframes` (3 options + `voltlineSelectedReframeId = 'time'`), `voltlineOffer`, `voltlineTrust`, `voltlineJobs` (10 jobs · 8 flat-rate · 2 quote-only), `voltlineAutomations` (4 with full step config — 3 enabled, no-show recovery off), `voltlineNextSteps`. `.tsx` because reframes / automation bodies carry inline `<em>` / `<strong>` JSX. The generated funnel *content* lives in `lib/funnel/data-stub.tsx` (Session 7 · §5.4) — this file holds the wizard's *inputs*, not its output. Reshape into Supabase reads when the backend lands.

### `lib/nav/` — nav + sidebar data (single source of truth for nav)

- **`types.ts`** — `NavItem` (`{ label, href, icon, badge? }`) + `NavSection` + `SettingsNavItem` (`{ label, href, icon }`, no badge — settings tabs don't carry one in either prototype).
- **`client-nav.ts`** — `clientNav` (sections + items), `clientWorkspace`, `clientSupport` (used as the `contact` prop on `ClientSupportCard`), `clientUser`.
- **`admin-nav.ts`** — `adminOverviewNav`, `adminWorkspaceNav` (Workspace section — Leads / Tickets / Websites / **Funnels** (Session 7) / Internal automations / Campaigns / Reviews / Integrations / Calendar / Settings, in that order), `adminWorkspace`, `adminUser`.
- **`admin-clients.ts`** — `adminClients` (stubbed client list of 4 clients: Voltline / FreshHome / KeyHero / NeatWorks). The previous `adminActiveClientId` const was removed — active client now lives in the reactive `WorkspaceProvider` context, not a hardcoded module export. Replace `adminClients` with backend data when wired.
- **`client-settings-nav.ts`** — `clientSettingsNav` — 7 tabs (Integrations / Profile / **Team** / Notifications / Billing / Login + security / Help). The Team tab (Cluster 6 · client invite) is net-new — the client prototype has no team surface; it mirrors the operator `/settings/team` for symmetry.
- **`admin-settings-nav.ts`** — `adminSettingsNav` — the operator's **agency-mode** (HQ) set, 10 tabs (Workspace / Team / Access / Integrations / Billing / Defaults / Integration defaults / Default seat limit / API + webhooks / Danger zone). Integration defaults + Default seat limit (Cluster 8 · Session 3) are the agency-policy surfaces.
- **`sub-account-settings-nav.ts`** — `subAccountSettingsNav` — the operator's **sub-account-mode** set (drilled into one client), 7 tabs (Profile / Access / Team / Integrations / Notifications / Billing / Danger zone). Access appears here AND in `adminSettingsNav` — the one genuinely bi-modal surface.
- **`use-settings-nav.ts`** — `useSettingsNav()` — resolves the settings nav for the current role + workspace mode (client → `clientSettingsNav`; operator + agency → `adminSettingsNav`; operator + sub-account → `subAccountSettingsNav`). `SettingsNav` consumes this; no tab page hard-codes a nav set.

### `lib/dashboard/` — per-role dashboard stub data

- **`admin-dashboard.tsx`** — operator-dashboard stub data: `dashboardGreeting` (eyebrow), `dashboardStats` (4-tile stat row, `value` is `ReactNode` so the `<em>34</em>` rust highlight on "leads this week" lives in the data), `midSetupClient` (the Voltline continue-setup hero), `liveClients` (the live-client list rows). `.tsx` because the stat values are JSX. Re-shape into Supabase reads when the backend lands. This is the **agency-mode** `/dashboard` content.
- **`hub-types.ts`** — the single-client overview hub's typed schema (admin Screen 20). Vision §7 discipline: every field maps to a column or computed/templated value, nothing needs an LLM. Key shapes: `OperatorAction` (typed by `kind`), `ManagementState` (`{ primary, clientLastActiveDays, operatorName }` — never composed prose), `HubHero` + `HubLeadFocal` + `HubHeroStat`, `HubContextCard`, `HubActivityEvent` (typed by `kind`), `HubWeeklyStat`, `HubFunnelConversion` + `HubFunnelStep`, `FunnelInsight` (`{ severity, target, suggestedAction, reasoning }`), and the top-level `ClientHub`. `schedule` reuses the calendar `CalendarTodayPanel` type.
- **`client-hub.ts`** — `freshhomeHub`: the canonical FreshHome single-client hub stub (Screen 20), conforming to `hub-types.ts`. Decorative for the stub layer (renders for any active sub-account client); real backend keys the read to `activeClientId`. Plain `.ts` — schema-pure strings/numbers, no JSX.
- **`client-dashboard-types.ts`** — the client dashboard's typed schema (client Screen 1). Same vision-§7 discipline as `hub-types.ts`. **Reuses `HubWeeklyStat` and `HubFunnelConversion` verbatim** — the client "this week" trend + funnel bars are structurally identical to the hub's. New shapes for concepts that diverge structurally: `DashboardUrgentHero` (`{ count, threshold, label, callouts, cta }` — `callouts: { name, age, note }[]`), `DashboardQueue` + `DashboardQueueItem` + `QueueItemTag`, `LandingSnapshot` + `LandingSnapshotStat`, `ClientFunnelSummary` (`{ weakPoint, operatorNote, healthNote, cta }` — the client-framed sibling of `FunnelInsight`), and the top-level `ClientDashboard`.
- **`client-dashboard.ts`** — `voltlineDashboard`: the canonical Voltline client-dashboard stub (Screen 1 — Mark's home), conforming to `client-dashboard-types.ts`. Plain `.ts` — schema-pure, the components compose all prose. Reshape into Supabase reads when the backend lands.

### `lib/settings/` — per-tab stub data for `/settings/*`

- **`client-profile.ts`** — `clientProfileBusiness` + `clientProfileManagedByWebnua` field-row lists for the client Profile tab.
- **`admin-workspace.ts`** — `adminWorkspaceFields` + `adminWorkspacePlanFields` for the admin Workspace tab (the Workspace ID row carries `mono: true` so the value renders in JetBrains Mono).
- **`client-integrations.tsx`** — `clientIntegrationsHero` (props for `IntegrationProgressHero`) + `clientIntegrations` (the 5 integration cards Voltline sees on `/settings/integrations`). `.tsx` because description / meta / hero copy carry inline `<em>` / `<strong>` JSX.
- **`admin-integrations.tsx`** — `adminConnectedIntegrations` (the 6 workspace-level integrations Webnua Perth has connected — Resend, Twilio, Meta Ads, GBP, Vercel, Anthropic) + `adminAvailableIntegrations` (Stripe + Xero). `.tsx` for the same JSX-in-description reason.
- **`client-notifications.ts`** — `clientNotifications: NotificationGroup[]` (Leads / Bookings / Reviews / Summary emails — each with channel set + per-row default state) + `clientQuietHours` (enabled flag + window string).
- **`client-billing.tsx`** + **`admin-billing.tsx`** — plan card props + payment method + invoice list per role. Client billing has `clientBillingIncluded` (the "what's included" grid); admin doesn't. `.tsx` because plan headlines carry `<em>` highlights.
- **`client-security.ts`** — `clientSecurityCredentials`, `clientSecurityTwoFactor`, `clientSecuritySessions` for the Login + security tab.
- **`client-help.tsx`** — `clientHelpFaqs` (7 FAQ entries, only the first carries an answer + `defaultOpen: true` by default, matching the prototype) + `clientHelpRecentSupport`.
- **`admin-team.ts`** — `adminTeamMembers` + `adminTeamPermissions` for the Team tab.
- **`admin-defaults.ts`** — `adminDefaultsAutomations` (4 toggleable defaults) + `adminDefaultsBranding` + `adminDefaultsPricing` for the Defaults tab.
- **`admin-api.tsx`** — `adminApiKeys`, `adminWebhookEndpoint`, `adminWebhookEvents` for the API + webhooks tab.
- **`admin-danger.tsx`** — `adminDangerWorkspace` (4 entries) + `adminDangerClient` (1 entry) for the Danger zone tab.

### `lib/integrations/` — cross-client integrations matrix data

- **`admin-matrix.tsx`** — `adminMatrixHero`, `adminMatrixFilters`, `adminMatrixColumns` (the 5 integration column defs: GBP / Meta / GA4 / G.Ads / Stripe), `adminMatrixRows` (4 client rows × per-integration status cells + per-client progress), `adminMatrixAttention` + `adminMatrixGaps` (action-card items). `.tsx` because hero stats and action-item text carry inline JSX. Reshape into Supabase reads when the backend lands.

### `shared/website/` — website hub + section editor shell (Phase 6 · Cluster 5 · Session 3 → 3.5)

> **Architectural note — `/website` is shared between roles.** Lives at top-level `app/website/` (moved out of `(client)/` in Session 3) with its own `layout.tsx` ('use client', reads `useRole()`, picks `ClientSidebar` vs `AdminSidebar`, mounts `DevRoleSwitcher`). The hub page (`app/website/page.tsx`) renders header/footer/nav cards + page grid for the active workspace's website. The page editor lives at `app/website/[pageId]/page.tsx`; two reserved sibling routes `app/website/header/page.tsx` and `app/website/footer/page.tsx` take precedence over `[pageId]` and mount the editor in singleton mode (design doc §2.6). The agency-level cross-client matrix lives separately at `app/(admin)/websites/` (admin-only).

> **Architectural note — `Website` ≠ `Funnel`.** Per design doc §2.0, a website (multi-page, shared chrome) and a funnel (linear sequence, no chrome) are independent artefacts that share the section registry and the client's brand but nothing else. A client's home page is **not** their funnel's landing page. Session 3.5 pinned the website page types to `'home' | 'about' | 'services' | 'contact' | 'generic'` and added website-level singletons (`header`, `footer`) plus a flat `nav: NavLink[]` capped at 6 items. Funnel types (`Funnel`, `FunnelStep`, `FunnelStepType`) live shape-only in `lib/funnel/types.ts` until Session 7 wires the funnel editor.

- **`PageGridCard`** (`website/PageGridCard.tsx`) — single page card on the website hub. Top row: page-type pill (`home`=rust / `about`=ink / `services`=good / `contact`=info / `generic`=paper-2) + slug. Middle: page title + section count ("4 of 7 sections on"). Footer: rust "Open editor →" CTA on paper-2 bar. The whole card is a `next/link` to `/website/[pageId]` — always visible regardless of capabilities (view-only users still need to inspect pages); gating happens inside the editor.
- **`VersionHistoryCard`** (`website/VersionHistoryCard.tsx`) — ink-bg list of recent versions for a website. Same visual chrome as `client/funnels/FunnelHistoryCard` (rust radial-gradient flourish, dotted-bottom-bordered rows) but reads the Session 2 `Version` model. Each row: status pill (LIVE = rust fill for the currently-live one; DRAFT = paper/12 on ink; PENDING = warn-soft; archived = rust/18 on rust-light) + notes + mono timestamp. Props: `title`, `subtitle`, `versions: Version[]`, `currentPublishedId: string | null` (drives which row shows the rust LIVE pill). **Session 8** added the rollback affordance: pass `websiteId` and every non-current published / archived row gets a "↺ Restore" button calling `restoreVersionAsDraft` (behind a `window.confirm`). Restore is disabled while a pending submission is in flight (a warn banner explains). `allowRestore` (default true when `websiteId` is set) opts a read-only surface out.
- **`PagePreviewPane`** (`website/PagePreviewPane.tsx`) — right pane of the section editor. Renders every enabled section's `<Preview>` (from the section registry) vertically stacked at max-width 760px in a paper-2 canvas. Disabled sections are excluded. Empty pages render a dashed-border placeholder. Clicking a preview tile selects the section (Session 4 will open a fields drawer; for Session 3 selection is visual only — outlines the tile with `ring-2 ring-rust`). Props: `sections`, `brand`, optional `onSelectSection` + `selectedSectionId`.
- **`SectionRailRow`** (`website/SectionRailRow.tsx`) — single row in the editor's left rail. `auto + 1fr + auto` grid: drag-handle glyph (`⋮⋮`, `editLayout`-gated via `<CapabilityGate mode="disable">`), section type label + one-line content summary, enable `<Switch>` (`editLayout`-gated via `<CapabilityGate mode="hide">`). Selected rows get a rust border + rust-soft tint; disabled sections opacity-down. Optional `singleton: boolean` (used by the header/footer editors) suppresses the drag handle, the enable switch, and the numbered index — singletons are always-on and unreorderable. The `summarizeSection` helper picks the first non-empty among `headline`/`title`/`priceLabel`/`tag`/`label` keys to render a meaningful summary line; falls back to the section type's description.
- **`SectionListRail`** (`website/SectionListRail.tsx`) — left rail of the editor. Discriminated `mode: { kind: 'page', title } | { kind: 'singleton', label }`. **Page mode**: `// SECTIONS` eyebrow + page title + on-count, multi-row list, `+ Add section` footer button (`editSections`-gated). **Singleton mode**: `// WEBSITE-LEVEL · {LABEL}` eyebrow + "singleton · wraps every page" meta, the single row passes `singleton={true}` to the `SectionRailRow`, the footer is replaced with a dashed-border explainer card instead of the Add button.
- **`EditorToolbar`** (`website/EditorToolbar.tsx`) — top bar of the section editor. Discriminated `mode: { kind: 'tabs', tabs, activeTabId, backHref? } | { kind: 'breadcrumb', label, backHref? }`. **Tabs**: one pill per `EditorToolbarTab` (`{ id, label, href }`), ink-active for the current; `backHref` defaults to `/website` for website editing and is passed `/funnels/[id]` for funnel-step editing. **Breadcrumb**: static mono uppercase line (used by `/website/header` and `/website/footer`). Middle slot: `AutosaveIndicator` (when `autosave` prop is provided — Session 5 (A) wired this against the real `useAutosave` hook; omitted in static demos). Right slot: View live ↗ + the publish action following design-doc §3.3 (Publish → / Submit for review → / hidden). Session 5 props: `autosave?` (status/lastSavedAt/onRetry), `publishDisabled?` (hides both publish actions — used by the Lane B editor lock AND by the funnel-step editor where publish isn't wired yet), `onPublish?` / `onSubmitForReview?` (real handlers replacing the visual-stub buttons from Session 3), `publishMenu?` (slot for the `ForcePublishMenu` chevron — chunk F). **Session 7** swapped the `website: Website` prop for a plain `domain: string` so the toolbar can mount over either a website OR a funnel without coupling to either model.
- **`AutosaveIndicator`** (`website/AutosaveIndicator.tsx`) — Session 5 (A). The mono pill in the toolbar middle slot. Five states from the `useAutosave` hook result: `idle` (no edits yet — static good dot, "Autosaved"), `pending` (within the 500ms debounce window — rust pulse, "Editing…"), `saving` (brief — rust pulse, "Saving…"), `synced` (settled — static good dot, "Saved Ns ago" with a 5s re-tick interval so the relative text stays current without parent rerenders), `failed` (button — warn dot, "Save failed · retry ↻" calling `onRetry`).
- **`SectionEditor`** (`website/SectionEditor.tsx`) — composes the toolbar + rail + preview pane + fields panel and owns the editor's local state (selected section id, locally-mutated section list). **Session 7** added a third mode discriminator and moved `website` from a top-level prop onto each website-bound mode entry, so the editor takes a single `mode` prop:
  - `{ kind: 'page', website, pages, page }` — website page editor; page tabs in toolbar.
  - `{ kind: 'singleton', website, section, label }` — website header / footer editor; breadcrumb in toolbar.
  - `{ kind: 'funnelStep', funnel, steps, step }` — funnel-step editor; step tabs prefixed `01 ·`, `02 ·`, `03 ·`. Publish + Lane B hidden in this mode (funnel publish mechanics land in a later session). Autosave still wires through with a funnel-keyed `DraftSlot`.

  Brand resolves through the client either way: website mode reads `website.clientId`, funnel mode reads `funnel.clientId`, both via `getBrandForClient` (one brand per client, design doc §2.3). Layout is **3-column when a section is selected** (`300px rail | 1fr preview | 400px fields`); reverts to 2-column when nothing is selected. **Singleton mode auto-selects** the only section so the fields panel is visible immediately. **Session 5 wiring (website modes):** the editor feeds every mutation through `useAutosave`, rehydrates initial state from `loadDraftSections`, and calls `publishDraft` / `submitForApproval` on the toolbar action handlers, routing back to `/website` on success. **Lane B lock**: `useUserPendingSubmission(websiteId, user.id)` returns the submitter's pending submission if one exists; when present the `WebsiteEditorPendingBanner` mounts above the toolbar, the rail + preview opacity-65 + pointer-events:none, the fields panel hides, autosave pauses, and the `ForcePublishMenu` is hidden via `publishMenu hidden={locked}`. Operators never see the lock — it only lights up for the user who submitted. The lock short-circuits in funnel mode (`websiteId` arg is `null`).
- **`WebsiteEditorPendingBanner`** (`website/WebsiteEditorPendingBanner.tsx`) — Session 5 (D). Warn-tinted top banner that mounts above the editor for a Lane B submitter while their submission is pending operator review. Renders the submitted-at timestamp + the fields-changed/sections-changed summary from `submission.diff` + two affordances: "Open ticket →" (deep-links to `/tickets` so the submitter can see the queue position) and "Recall submission ↩" (calls `publish-stub.recallSubmission` after a `window.confirm`; the submitter's draft slots are untouched so recall resumes editing exactly where they left off). `reviewerName` defaults to `'Craig'` (the one admin stub user); when real auth ships this comes from the workspace operator-of-record.
- **`ForcePublishMenu`** (`website/ForcePublishMenu.tsx`) — Session 5 (F). Break-glass "skip approval" affordance — chevron menu to the right of the rust "Publish →" button, admin-only via call-site `role === 'admin'` check (design doc §2.4: NOT a separate capability). Two-stage confirm: (1) menu open, (2) reason-required dialog. The confirm dialog uses `Dialog size="default"` (560px), warn-tinted header, required `Textarea` reason, warn "Force publish ⚡" CTA stays disabled until the reason field has non-whitespace content. Confirm calls `publishDraft({ force: { reason } })` which both publishes AND writes a `ForcePublishEntry` to the audit overlay. `hidden` prop returns null when set (used by `SectionEditor` to drop the menu when the publish button itself is disabled — Lane B lock).
- **`SectionFieldsPanel`** (`website/SectionFieldsPanel.tsx`) — third column of the editor, only visible when a section is selected. Header shows the section type's label + description + a Close × button; body scrolls and mounts the registry's `Fields` component for the selected section. Per-field capability gating happens INSIDE the Fields component (every section's Fields uses `CopyField`/`MediaField`, both of which wrap inputs in `<CapabilityGate mode="request">` so view-only users see the request-change affordance on hover). The panel itself is just chrome. `hideClose` prop suppresses the Close × in singleton mode (no other section to deselect to). Force-remounts when the selected section changes (`key={section.id}`) so per-field state like `CopyField`'s AI-variant index resets cleanly.
- **`WizardSectionEditor`** (`website/WizardSectionEditor.tsx`) — Session 7. The editor's **wizard-frame mode** (design doc §5.2) — mounted by the onboarding wizard's Step 5 (`/clients/new/draft`). Walks the generated funnel's sections one at a time, flattened across every funnel step. No `EditorToolbar`, no rail; capabilities locked to `{editCopy, editMedia, useAI}` via `<CapabilityOverrideProvider>`; a `BuilderFooterActions` footer drives the walk (Back / Continue — `onExitBack` / `onExitForward` route to the neighbouring wizard steps); height-contained, not `h-svh`. Autosave persists to the same per-funnel-step `DraftSlot` the funnel editor uses. **Built as a sibling component, NOT a fourth `SectionEditor` mode discriminator** — the three existing modes share helpers keyed on a `{website}`/`{funnel}` shape the per-step walk doesn't fit; keeping the working 3-mode editor untouched avoided regression risk. Inner `WizardStepPane` (keyed by step id) owns one step's sections so a step boundary is a clean remount.

### `shared/website/` (cont'd) — preflight + review surface (Phase 6 · Cluster 5 · Session 8)

> **Architectural note — `/website/review` is the publish gate.** Session 8 (design doc §7) makes the review surface the single place a website publishes from. The editor's `EditorToolbar` no longer publishes directly: when its `reviewHref` prop is set (`SectionEditor` passes `/website/review` for website modes), the two lane buttons collapse into one **"Review & publish →"** link. The review surface runs preflight, blocks publish on hard fails, and owns the actual `publishDraft` / `submitForApproval` calls following the §3.3 capability lane. Funnel-step mode leaves `reviewHref` unset (funnel publish isn't wired — see parked decisions); the `ForcePublishMenu` break-glass path stays direct (it deliberately skips both approval and review). `/website/review` joins `header` / `footer` as a reserved static sibling that takes precedence over the `[pageId]` dynamic route.

- **`DomainStatusIndicator`** (`website/DomainStatusIndicator.tsx`) — Session 8. Small status pill for a website's primary domain + SSL state. UI ONLY — DNS / cert work is a backend concern (design doc §8: domain management is V2). `sslStatus` drives the dot colour: `live` = good, `pending` = warn pulse, `error` = warn. Renders the primary domain in mono + an alias count when `domain.aliases` is non-empty. Mounted on the `/website` hub and the `/website/review` surface.
- **`PreflightChecklist`** (`website/PreflightChecklist.tsx`) — Session 8. Renders a `PreflightReport`: a summary row of pass / warn / fail count chips + the list of warn + fail result rows (passing rules collapse to the green count). Each issue row carries the rule title + message + an optional rust "Fix →" deep-link to the offending editor. `allClear` reports render a green "all clear" banner instead of the list. Presentation only — publish gating lives on the review surface.
- **`PageReviewCard`** (`website/PageReviewCard.tsx`) — Session 8. One card per page on the review surface. Page-type pill + slug + section count + a lightweight section-stack glyph "thumbnail" (rust bars for enabled sections, paper-2 for disabled) + a per-page preflight roll-up footer (worst status of that page's results). Whole card is a `next/link` to the page editor. Real visual thumbnails wait for a render-to-image backend.

> **Architectural note — funnels are top-level AND shared between roles** (Session 7 relocation). The prototype framed funnels as client-only (Screen 23) and admin interactions via a different page-builder flow, but the platform converged on a single funnel editor that both roles use — admins drive funnel changes day-to-day for clients on managed plans, and the editor's per-field capability layer covers per-user gating. `/funnels` therefore lives at the **top level** (`app/funnels/`) alongside `/website`, `/tickets`, `/leads`, etc., NOT inside `(client)/`. Routes: `/funnels` (context-aware list — agency mode renders the cross-client roster; sub-account / client renders that client's funnels), `/funnels/[id]` (analytics detail with an operator-facing `Edit funnel →` CTA), `/funnels/[id]/edit/[stepId]` (funnel-step editor — Session 7). The funnel detail's back link reads "← back to funnels" and points to `/funnels`. **Sidebar:** both the client nav (`lib/nav/client-nav.ts`) AND the admin nav (`lib/nav/admin-nav.ts`) carry a top-level `Funnels` item alongside `Website` / `Websites`. The website hub itself lives at the shared route `app/website/` (Phase 6 · Cluster 5 · Session 3); see the `shared/website/` inventory above.

- **`FunnelHero`** (`funnels/FunnelHero.tsx`) — ink-bg banner with rust radial-gradient flourish + a paired `FunnelAggCard` on the right. Props: `back: { label, href }` (renders as `← {label}` mono link), `tag` (rust-light mono pill with 🔒 glyph), `title` (ReactNode, `<em>` = rust-light), `subtitle` (ReactNode, `<strong>` = paper-bold), `meta: FunnelHeroMeta[]` (mono `LABEL` + paper-bold value pairs, e.g. `BUILT 14d ago`), `versionLabel` (optional rust-light pill), `actions: { viewLiveLabel, viewLiveHref, requestChangeLabel, requestChangeHref, editFunnelLabel?, editFunnelHref? }` (ghost View live + primary Request-change + optional Session 7 `Edit funnel →` button rendered when caller passes both `editFunnel*` strings — gate upstream on edit caps), `agg: FunnelAggCardProps`. Grid is `1.4fr 1fr`. **Standalone — not extracted into a shared `InkHero` primitive (see parked decisions; this is the 4th ink-hero data point).**
- **`FunnelAggCard`** (`funnels/FunnelAggCard.tsx`) — translucent-on-ink metric tile used inside `FunnelHero`. Props: `label` (mono eyebrow), `live` (renders the pulsing good-dot LIVE indicator), `metrics: [FunnelAggMetric, FunnelAggMetric]` (2-up grid; `num` is ReactNode so `<em>412</em>` highlights rust-light, plus mono label + optional trend in good-green), `bottom?: { left, right }` (border-topped revenue row).
- **`FunnelFlow`** (`funnels/FunnelFlow.tsx`) — white card containing the 3-step row + 2 arrows + period toggle in its header. Props: `title` (ReactNode, `<em>` = rust), `steps: FunnelStep[]`, `arrows: FunnelArrow[]` (rendered between consecutive steps via `flatMap`; arrow `n` sits between step `n` and `n+1`), `periods: FunnelPeriod[]`, `defaultPeriod?: FunnelPeriod`. Grid is `1fr 100px 1fr 100px 1fr` — widened from the prototype's 40px so the pct pill + wrapped drop-off captions fit cleanly without overflowing into neighbouring step cards. Currently rendering 3 steps; the column template would need to extend for 4+ (out of scope for the stub layer).
- **`FunnelStepCard`** (`funnels/FunnelStepCard.tsx`) — per-step paper-bg card. `step: FunnelStep` does all the work; the `tone` field (`'first' | 'middle' | 'last'`) drives the colour treatment: `first` = rust-soft gradient + rust accent; `last` = good-soft gradient + good accent; `middle` = plain paper + rust accent. Composes `FunnelStepThumbnail` for the visual gist. Hover lifts -translate-y-0.5 + shadow-card.
- **`FunnelStepThumbnail`** (`funnels/FunnelStepThumbnail.tsx`) — small 64px-tall CSS "preview gist" of each step. `variant: 'landing' | 'schedule' | 'thanks'`. Pure visual, no business logic. Already used by both the detail view's step cards AND the funnels list row, so it's a real shared-within-funnels primitive — kept in `client/funnels/` because both consumers are client-only.
- **`FunnelArrow`** (`funnels/FunnelArrow.tsx`) — between-steps cell: rust-soft conversion-rate pill on top, line + `→` glyph in the middle, mono drop-off caption below (`<strong>` renders in warn-red for emphasis on the drop count).
- **`FunnelPeriodToggle`** (`funnels/FunnelPeriodToggle.tsx`) — `'use client'` pill toggle for time-range (7D / 14D / 30D / 90D). Local `useState` active state — fine for the stub layer; lift to a callback or URL state when real period filtering wires up. Optional `onChange` is plumbed but the stub doesn't need it.
- **`FunnelInsightsCard`** (`funnels/FunnelInsightsCard.tsx`) — white-surface "// Insights · what we see" list. Props: `title` (ReactNode, `<em>` = rust), optional `subtitle`, `items: FunnelInsight[]`. Sibling export **`FunnelInsightRow`** renders one row — a 28px tone-tinted icon glyph + body (with `<em>` = rust, `<strong>` = ink-bold) + mono meta line. Severity tones: `warn` (warn-tinted), `good` (good-tinted), `info` (rust-soft).
- **`FunnelHistoryCard`** (`funnels/FunnelHistoryCard.tsx`) — ink-bg version history list with a rust radial-gradient flourish. Props: `title` (ReactNode, `<em>` = rust-light), optional `subtitle`, `items: FunnelVersion[]`, `ctaLabel`, `ctaHref` (rendered through `Button asChild` + `next/link`, full-width below the list). Sibling export **`FunnelHistoryItem`** renders one row — version pill (`current` = rust-filled, others = rust-light on rust/18) + body text + mono meta line + right-aligned mono timestamp. **Not** a `RailCard` — the heading rhythm and the version-pill column make it its own shape.

### `lib/funnels/` — funnels stub data + types

- **`types.ts`** — single source of truth for funnel shapes: `FunnelStepThumbVariant` (`landing` | `schedule` | `thanks`), `FunnelStepTone` (`first` | `middle` | `last`), `FunnelStep`, `FunnelArrow`, `FunnelPeriod` + `FUNNEL_PERIOD_LABEL`, `FunnelInsight` + `FunnelInsightTone`, `FunnelVersion`, `FunnelAggMetric` + `FunnelAggBottom`, `FunnelHeroMeta`, and the top-level `FunnelDetail` that ties it all together (back link, hero, agg, flow config, steps, arrows, insights, history).
- **`client-detail.tsx`** — `voltlineFunnel`: the single canonical Voltline "$99 emergency call-out" funnel stub matching client Screen 23 (412 → 74 → 14, three insights, three versions). `.tsx` because titles / subtitles / metric numbers / insight bodies all carry inline `<em>` / `<strong>` JSX. Reshape into Supabase reads when the backend lands.

### `lib/tickets/` — tickets stub data + types

- **`types.ts`** — single source of truth for ticket shapes: `TicketStatus = 'open' | 'in_progress' | 'blocked' | 'done'` (admin's vocabulary is the stored one — client's `Review` is derived view-time via `StatusPill reviewAware` when status is `in_progress` and `awaiting === 'client'`), `TicketUrgency`, `TicketCategory`, `TicketAwaiting` (`'operator' | 'client' | null` — drives both the client row's attention pill and the derived `Review` label), `TicketTab`, plus the `CATEGORY_LABEL` / `STATUS_LABEL` / `URGENCY_LABEL` maps.
- **`client-tickets.tsx`** — `clientTicketsHero` (props for `TicketsHero`), `clientTicketTabs` (Active / Needs your reply / Done / All), `clientTickets: ClientTicketRow[]` (7 rows — Mark's queue from the prototype). `.tsx` because previews carry inline `<strong>Craig:</strong>` / `<strong>You:</strong>` JSX.
- **`admin-tickets.tsx`** — `adminTicketsHero` (props for `TicketsHero` with 4 stat tiles in `right`), `adminTicketTabs` (All / Open / In progress / Blocked / Done), `adminTicketFilters` (`active` + `available` chip lists), `adminTickets: AdminTicketRow[]` (10 rows across 4 clients from the prototype). Re-exports `AdminTicketClientTone` (`voltline` / `freshhome` / `keyhero` / `flowline` / `generic`) — drives the row's client-avatar tint.
- **`client-detail.tsx`** — `clientTicketDetail`: the single canonical TKT-0247 detail stub for the client view. Carries `statusLabel` (string for the header pill) + `statusHeadline` (ReactNode with `<em>Open</em>` for the side card — two fields because the same data renders two ways, neither derivable from the other without losing the em-scope decision the prototype made). Plus thread messages, reply config (label / placeholder / 4 quick-chips), status description, properties (read-only), and actions (Call Craig / Edit / Add file / Cancel). Re-exports `TicketDetailMessage`, `TicketDetailProperty`, `TicketDetailAction` so the admin stub can reuse the shapes.
- **`admin-detail.tsx`** — `adminTicketDetail`: same TKT-0247 from the operator side. Differs from the client stub by including `client` (initial/name/tone) for the inline mini-avatar in the header meta, a pre-populated `defaultValue` reply body (the draft Craig has staged), `statusOptions` (4 selectable rows for the status picker), `editable` flags on properties, and admin-only actions (Open Voltline website / Create page draft / Add internal note / Convert to subtasks). Imports the message/property/action shapes from `client-detail.tsx`.

### `shared/leads/` — lead inbox + detail + conversation (Phase 5 · Cluster 1)

> **Architectural note — leads lives outside the role route groups**, same reason as settings / tickets / dashboard. Routes: `/leads` (inbox), `/leads/[id]` (detail), `/leads/[id]/conversation` (conversation). `app/leads/layout.tsx` (`'use client'`, reads `useRole()`, picks `ClientSidebar` vs `AdminSidebar`, mounts `DevRoleSwitcher`). Each page is a `'use client'` dispatcher with `_client-content.tsx` / `_admin-content.tsx` siblings. The detail + conversation pages reuse `TicketDetailLayout` directly (same `1fr 320px` shape). For the stub layer, the `[id]` slug is decorative — both roles render their single canonical Sarah Davies stub regardless of the actual id.

- **`LeadsHero`** (`leads/LeadsHero.tsx`) — ink-bg rust-tagged hero used by the admin inbox. Props: `tag` (mono pill string), `title` (ReactNode, `<em>` = rust), `subtitle` (ReactNode, `<strong>` = paper-bold), optional `right` slot. Visually same chrome as `TicketsHero` (the 5th ink-hero data point — see parked decisions; still hold-pattern, no extraction).
- **`LeadTabsBar`** (`leads/LeadTabsBar.tsx`) — `'use client'` pill-tab strip with counts. Same shape as `TicketTabsBar` but takes the leads-typed `LeadTab[]` instead. Kept separate from `TicketTabsBar` to keep type boundaries clean — if a third surface needs the same pattern, extract to a generic `TabsBar` primitive.
- **`LeadRow`** (`leads/LeadRow.tsx`) — single canonical row, discriminated by `variant: 'client' | 'admin'`. Both variants share the `unread` → left rust rail + faint rust bg attention treatment. Client variant: `44px + 1fr + auto + auto + 70px` grid — avatar + name+preview (with suburb prefix in mono) + `LeadStatusPill` + optional `LeadUrgencyPill` + age. Admin variant: `36px + 180px + 1fr + 110px + 90px + 100px` grid — tone-tinted avatar + name+`LeadClientPill` + preview + `LeadStatusPill` + age + tone-coloured activity meta (`good`/`rust`/`quiet`).
- **`LeadDetailHeader`** (`leads/LeadDetailHeader.tsx`) — top-of-detail row: 48px avatar + name + comma-joined meta parts (phone bolded via `<strong>`, suburb, email) + optional `clientPillLabel` (admin uses this for the "FreshHome" pill). `rightActions` slot is open for future per-role overflow.
- **`LeadStatusSwitcher`** (`leads/LeadStatusSwitcher.tsx`) — `'use client'` mono-uppercase segmented control: `// STATUS` label + 5 status buttons (New / Contacted / Booked / Completed / Lost). Active = ink-bg paper-text. Local `useState` — lift when status mutation wires to backend.
- **`LeadTimeline`** + **`LeadTimelineEventRow`** (`leads/LeadTimeline.tsx`) — activity-timeline list: heading + `N EVENTS · NEWEST FIRST` mono meta on the right + vertically-stacked events. Each `LeadTimelineEvent` carries a `dot` type (`sms-in` / `sms-out` / `form` / `status` / `email` / `scheduled-sms` / `scheduled-email`), `meta` (ReactNode), optional `body` + `snippet` (rendered in a tinted bordered block matching dot type), optional right-aligned `rightTime`, and `pending: true` for opacity-dimmed scheduled future events. `auto: true` adds the inline `⤿ AUTO` pill in the meta row.
- The leads side-rail uses the shared **`RailCard`** (with the `rows` shorthand) + **`RailPropertyRow`** — see the `shared/` rail-primitives entry below. (The former `LeadRailCard` / `LeadRailRowItem` were merged into `RailCard` in Phase 6 · Cluster 6.)
- **`LeadQuickActions`** + **`LeadQuickActionItem`** (`leads/LeadQuickActions.tsx`) — vertical button stack used inside the `// QUICK ACTIONS` rail card. Each `LeadQuickAction` is `{ icon, label, primary?, href? }` — `primary: true` renders rust-filled (used by Call Sarah back now / Open conversation); others render bordered ghost. When `href` is set the item is a `next/link`; otherwise a `<button>`.
- **`LeadStatusPill`** / **`LeadUrgencyPill`** / **`LeadClientPill`** (`leads/pills.tsx`) — three small pills exported together. `LeadStatusPill` tones: `new` (rust) / `contacted` (info) / `booked` (good) / `completed` (ink) / `lost` (quiet); optional `label` override. `LeadUrgencyPill` tones: `asap`/`today` (warn) / `soon` (quiet); `none` renders nothing. `LeadClientPill` is a colour-tinted client-name pill driven by `LeadClientTone` (`voltline` / `freshhome` / `keyhero` / `neatworks` / `flowline` / `generic`) — used in the admin row to attribute leads to their client.
- **`ConversationHeader`** (`leads/ConversationHeader.tsx`) — `'use client'` top-of-conversation row: avatar + name + meta + optional channel-tabs (All / SMS / Email) + optional `actions` (call / chevron / overflow glyphs). Local `useState` for active channel — visual stub for the stub layer.
- **`ConversationThread`** + **`ConversationMessageRow`** + **`ConversationDayDivider`** (`leads/ConversationThread.tsx`) — message-thread wrapper. Days are rendered with a centred mono day-divider pill; each `ConversationMessage` is rendered with a kind-discriminated bubble: `incoming` (paper card on left), `outgoing` (ink on right), `auto` (rust-tinted on right with inline `⤿ AUTO` meta), `system` (centred paper-2 mono pill). Bubble metadata (channel / timestamp / `DELIVERED` flag / `metaPrefix` like "SARAH" / "LISA") is rendered in mono uppercase below the bubble. **Not** built on `TicketThreadMessage` because the meta-row vocabulary (AUTO tag, DELIVERED, channel) diverges enough that composition wouldn't simplify the call site.
- **`ConversationComposer`** (`leads/ConversationComposer.tsx`) — `'use client'` reply composer at the bottom of the conversation card. Two channel-control modes: `channels: ['SMS', 'Email']` renders a top pill-tab strip (client variant); `channelToggle: 'SMS ↕'` renders an inline pill button to the left of the textarea (admin variant — single-line, matches the prototype's compact admin row). Optional `helpers` row beneath the textarea renders rust-light affordance chips with a right-aligned `Spell check ✓`. Send button is rust-filled.

### `lib/leads/` — leads stub data + types

- **`types.ts`** — single source of truth for lead shapes: `LeadStatus` (`new | contacted | booked | completed | lost`), `LeadUrgency`, `LeadClientTone`, `LEAD_STATUS_LABEL` + `LEAD_URGENCY_LABEL` maps, `LeadTab`, `LeadFilterChip`, inbox-row shapes (`ClientLeadRow`, `AdminLeadRow`), `LeadTimelineEvent` + `LeadTimelineDot`, `LeadRailRow` + `LeadRailCard` + `LeadQuickAction`, the top-level `LeadDetail`, and the conversation shapes (`ConversationBubbleKind`, `ConversationMessage`, `ConversationDay`, `ConversationChannelTab`, `ConversationQuickReply`, `LeadConversation`).
- **`client-leads.tsx`** — `clientLeadsHero` (props for `PageHeader`), `clientLeadsTabs` (5 tabs · New / Contacted / Booked / Lost / All), `clientLeads: ClientLeadRow[]` (8 rows from client Screen 2 — Voltline's inbox), `voltlineLeadDetail` (Sarah Davies full 4-event timeline + 2 rail cards + 4 quick actions), `voltlineConversation` (Mark↔Sarah 5-message SMS thread + composer config). `.tsx` because titles / subtitles / metric numbers / timeline bodies all carry inline `<em>` / `<strong>` JSX.
- **`admin-leads.tsx`** — `adminLeadsHero` (props for `LeadsHero`), `adminLeadsClientFilters` (5 chips · All clients + 4 clients), `adminLeadsTabs` (6 tabs · New / Contacted / Booked / Completed / Lost / Spam), `adminLeads: AdminLeadRow[]` (8 rows from admin Screen 15 — Webnua Perth's cross-client inbox), `freshhomeLeadDetail` (Sarah Davies via FreshHome with 5 timeline events incl. scheduled future ones + 3 rail cards), `freshhomeConversation` (Lisa↔Sarah 7-message thread + status-change system row + quick-reply rail). `.tsx` for the same JSX-in-fields reason.

### `shared/calendar/` + `admin/calendar/` — week calendar (Phase 5 · Cluster 2 · Session 1)

> **Architectural note — calendar lives outside the role route groups**, same reason as settings / tickets / leads / dashboard. Single shared route `app/calendar/` with `'use client'` layout (reads `useRole()`, picks `ClientSidebar` vs `AdminSidebar`, mounts `DevRoleSwitcher`) + `'use client'` dispatcher `page.tsx` + `_client-content.tsx` / `_admin-content.tsx` siblings. The grid itself is identical in both roles; the admin variant adds a client-filter row, a colour legend, and a today-summary panel.

- **`CalendarGrid`** (`shared/calendar/CalendarGrid.tsx`) — the week-grid surface. White card with a `70px + repeat(6, 1fr)` header (day labels) and body (time column + 6 day columns). Each day is 12 hour-slots tall, 50px per hour (= 600px). Renders `BookingPill`s positioned by `top`/`height` px from the booking data, plus an optional `CalendarNowLine` on the today column. Takes `week: CalendarWeek` and renders. Tone of the booking pills comes from the data (`tone?: CalendarClientTone`) — client view leaves it unset (defaults to rust); admin view passes `freshhome` / `keyhero` / `voltline` / `neatworks` and gets per-client tinting.
- **`BookingPill`** (`shared/calendar/BookingPill.tsx`) — single positioned booking. Absolute-positioned inside its day column (`top: Npx; height: Npx`). Renders mono time / Inter Tight title / customer line, all paper-on-tone. Renders as a `next/link` when `href` is set; otherwise a click-target `<div>` (will lift to a real callback when the booking detail wires up in Session 2). `tone: CalendarClientTone` controls the background fill — `voltline` → `bg-rust`, the others use inline hex tokens (`#4a7ba6` / `#8a5cb8` / `#2d8a4e`) matching the prototype.
- **`CalendarNowLine`** (`shared/calendar/CalendarNowLine.tsx`) — the pulsing rust horizontal line + leading dot + "NOW · 10:35" pill at a given `top` px inside the today column. Pointer-events disabled — purely visual.
- **`CalendarToolbar`** (`shared/calendar/CalendarToolbar.tsx`) — white-card control strip: prev/next nav buttons + period label (`Week of <em>May 11</em> — May 16, 2026`, ReactNode so `<em>` segments render rust) on the left, `CalendarViewTabs` on the right.
- **`CalendarViewTabs`** (`shared/calendar/CalendarViewTabs.tsx`) — `'use client'` Day/Week/Month pill toggle. Local `useState` active state; optional `onChange` (not wired for the stub — Day/Month views ship in a later session).
- **`CalendarLegend`** (`admin/calendar/CalendarLegend.tsx`) — admin-only horizontal legend row: per-client colour swatches in a paper-2 mono uppercase bar, with an optional right-aligned `meta` ReactNode (e.g. `28 bookings · 11 today`). Tone → swatch colour mapping is the same as `BookingPill` (single source of truth lives in each component for now; if a third surface uses the same tone vocab, extract to `lib/calendar/tones.ts`).
- **`CalendarTodayPanel`** (`admin/calendar/CalendarTodayPanel.tsx`) — admin-only summary card below the grid. Heading + meta line (`<strong>` = rust) + list of `CalendarTodayJob` rows. Each row is a 5-column grid (`100px 36px 1fr auto auto`) with a colour-coded left border (per-client tone), mono time, single-letter client logo on ink, title + customer, status pill (`completed` → good-soft; `scheduled`/`in_progress` → rust-soft), and a rust "Open →" affordance. Renders as a `next/link` when `href` is set. **Cluster 6 · 1b** added two optional props: `action` (header-right ReactNode, e.g. an "Open calendar →" link) and `hideClientLogo` (drops the logo column → 4-col grid — used by the single-client overview hub, where a per-row client logo is redundant).

### `lib/calendar/` — calendar stub data + types

- **`types.ts`** — single source of truth for calendar shapes: `CalendarClientTone` (`voltline | freshhome | keyhero | neatworks | generic`), `CalendarBookingStatus` + `CALENDAR_BOOKING_STATUS_LABEL`, `CalendarBooking` (id / time / title / customer / `top` + `height` px / optional tone + href), `CalendarDay` (id / name / num / `isToday` / `bookings` / optional `nowTopPx` + `nowLabel`), `CalendarWeek` (corner label / period label ReactNode / time slots / days), plus admin extras (`CalendarClientFilter`, `CalendarLegendItem`, `CalendarTodayJob`, `CalendarTodayPanel`) and the top-level `ClientCalendar` + `AdminCalendar`.
- **`client-calendar.tsx`** — `voltlineCalendar`: Mark's Voltline week (Mon 11 – Sat 16 May 2026) from client Screen 4. 17 bookings across 6 days, Wed = today with a 10:35 now-line. `.tsx` because the period label + hero copy carry inline `<em>` / `<strong>` JSX.
- **`admin-calendar.tsx`** — `adminCalendar`: Webnua Perth cross-client week from admin Screen 13. 23 bookings tinted across 4 clients, the 5-chip client filter, the 4-item legend, and the 4-row today panel (one completed, one in-progress, two scheduled). `.tsx` for the same JSX-in-copy reason.

### `shared/bookings/` — booking detail (Phase 5 · Cluster 2 · Session 2)

> **Architectural note — bookings live outside the role route groups**, same reason as calendar. Single shared route `app/bookings/[id]/` with `'use client'` layout (reads `useRole()`, picks `ClientSidebar` vs `AdminSidebar`, mounts `DevRoleSwitcher`) + `'use client'` dispatcher `page.tsx` + `_client-content.tsx` / `_admin-content.tsx` siblings. The `[id]` slug is decorative for the stub layer (both roles render their single canonical booking regardless of the actual id — Voltline's "Ceiling fan + RCD replacement" on the client; FreshHome's "Fortnightly clean · 3-bed" on the admin). Reached from any `BookingPill` with `href` set on the calendar grid.

- **`ClientBookingHero`** (`bookings/ClientBookingHero.tsx`) — **ink-bg** hero for client Screen 8. `1fr auto` grid: left half carries the rust-light mono `tag` + 26px Inter Tight `title` (ReactNode, `<em>` = rust-light) + meta line (`{ customer, suburb, price, duration }`, `·`-separated; first segment renders ink-bold via the customer prop). Right half is a rust-bg paper-text dot pill rendering `statusLabel` (e.g. `Scheduled · 2.5h away`). Inverse chrome to `AdminBookingHero` — kept as a separate component because the structures invert (ink surface vs white card with tone left-border).
- **`AdminBookingHero`** (`bookings/AdminBookingHero.tsx`) — **white-card** hero for admin Screen 18. `tone: CalendarClientTone` drives a 6px-wide left border tint matching the calendar booking pill colours. Renders mono `timeRow` (ReactNode, `<strong>` = ink) + 28px Inter Tight `jobTitle` + customer row (name strong + phone + suburb + rust-soft client pill) + an open `actions` slot (caller supplies the `Button` row — typically Mark complete / Reschedule / Open lead / Cancel booking).
- **`BookingSection`** (`bookings/BookingSection.tsx`) — section block with a mono `// HEADING`. `variant: 'card' | 'inline'`. **Card** (default, admin): standalone white card with its own border + padding (`px-6.5 py-5.5`). **Inline** (client): plain block separated from the next section by a paper-2 bottom rule (`border-b border-paper-2 pb-5.5 mb-5.5`), auto-suppressed on `:last-child`. Use inline when stacking sections inside an outer wrapper card.
- **`BookingJobGrid`** (`bookings/BookingJobGrid.tsx`) — 2-column key/value grid. `cells: BookingJobCell[]` (`{ label, value }`, value is ReactNode so `<em>` renders rust). `surface: 'paper' | 'plain'`. **Paper** (client): each cell sits in its own paper-bg tile (`rounded-lg bg-paper px-3.5 py-3`). **Plain** (admin): cells are inline label/value pairs with no tile surface (`gap-x-6 gap-y-4`).
- **`BookingNotesBox`** (`bookings/BookingNotesBox.tsx`) — paper-bg notes container with a rule-coloured 3px left border. Renders children at 14px ink-soft, with `<strong>` segments switching to bold ink. Identical chrome in both roles.
- **`BookingHistoryRow`** (`bookings/BookingHistoryRow.tsx`) — discriminated by `variant: 'compact' | 'grid'`. **Compact** (client): a single-line paper-bg pill with `<strong>{date}</strong> · {body}`; `body` is ReactNode so the price + review meta inline. **Grid** (admin): `100px 1fr 80px auto` grid with mono date / title / price / status (`done` = good-green; optional `statusLabel` override for star ratings).
- **`BookingActionBtn`** (`bookings/BookingActionBtn.tsx`) — client-rail stacked icon+label button. `tone: 'primary' | 'secondary' | 'danger'`. **Primary** = rust-filled paper-text (Mark job complete). **Secondary** = paper bg + rule border, hovers to ink-fill (Call / Reschedule / etc.). **Danger** = warn text on paper, hovers to warn-fill (Cancel booking). Renders as `next/link` when `href` is set, else a `<button>`. `mb-2 last:mb-0` auto-spaces within a stack.

### `lib/bookings/` — booking detail stub data + types

- **`types.ts`** — single source of truth for booking shapes: `BookingJobCell` (label/value), `BookingRailRow` (label/value/`accent`), `BookingHistoryItemCompact` (date + ReactNode body), `BookingHistoryItemGrid` (date / title / price / `status: 'done'|'scheduled'` / optional `statusLabel`), `ClientBookingActionTone` (`primary | secondary | danger`), `ClientBookingAction` + `ClientBookingActionGroup`, plus the top-level `ClientBookingDetail` and `AdminBookingDetail` (the latter carries `tone: CalendarClientTone` so the hero border tint pairs with the calendar pill colour).
- **`client-booking.tsx`** — `voltlineBooking`: Liam Reilly's "Ceiling fan + RCD replacement" stub from client Screen 8. Customer block + 4 job-grid cells + notes paragraph + 2-row history + 2 action groups (On arrival / Manage, 3 buttons each) + the "// NEXT" tail explainer. `.tsx` because titles / values / notes / next-note all carry inline `<em>` / `<strong>` JSX.
- **`admin-booking.tsx`** — `freshhomeBooking`: Anna Larsen's "Fortnightly clean · 3-bed" stub from admin Screen 18. Hero (eyebrow / title / subtitle) + FreshHome tone + time row + 8 job-grid cells + customer notes + 4-row history (last carries 5★ rating) + 3 rail data sets (customer value 4 rows, location address, automations 2 rows). `.tsx` for the same JSX-in-fields reason.
- **`recurring-setup.tsx`** — `voltlineRecurring`: Emma Petrov's "Building inspection · electrical" recurring stub from client Screen 16. Customer header + 4 frequency options (`fortnightly` default) + 7 day options (`thu` default) + time + service/price + 4-row preview (Thu 14 May → Thu 25 Jun) + ink summary bar copy + bottom footnote. Re-exports `RecurringSetup`, `RecurringFrequencyOption`, `RecurringDayOption`, `RecurringPreviewRow`. `.tsx` for the same JSX reason.
- **`new-booking-modal.tsx`** — two stubs that share the same shape so the modal works for both roles. **`freshhomeNewBooking`** (admin Screen 21): includes the optional `summary` banner (`"Booking for FreshHome"`) because operators choose which client they're booking for. **`voltlineNewBooking`** (client calendar entry): omits `summary` (single-business context, nothing to pick). Both use the same service-agnostic service set (`standard / quick / quote / emergency / recurring / one_off / custom`) and generic notes copy so the modal reads as universal for any service business. Re-exports `NewBookingModalData` (where `summary` is now optional), `NewBookingServiceOption`, `NewBookingToggleField`. `.tsx` because the headline + footer copy carry inline `<strong>` JSX.
- **`conflict-modal.tsx`** — `voltlineConflict`: stub for the client Screen 17 conflict modal triggered from the recurring "Save + check conflicts" CTA. Carries the warn-coloured tag + title/subtitle (with `<strong>Thu 25 June</strong>` highlighted), `attempted` + `existing` 2-card pair (Emma's would-be 10am visit vs Banner's existing 11am job), explainer paragraph, 4 numbered options (option 1 marked recommended), and the save label. Re-exports `ConflictModalData`, `ConflictCardSide`, `ConflictOption`. `.tsx` for JSX in title/subtitle/explainer.
- **`reschedule-modal.tsx`** — two stubs for the reschedule modal: **`freshhomeReschedule`** (admin booking-detail Reschedule button — Petrov fortnightly, FreshHome flavour) and **`voltlineReschedule`** (client booking-detail Reschedule action — Liam Reilly, Voltline flavour). Both carry rust tag + title (em=rust) + subtitle, `was` vs `now` compare pair, 6 reason chips, SMS preview tokens, 2 toggles, footer info, save label. Re-exports `RescheduleModalData`, `RescheduleCompareSide`, `RescheduleReasonOption`, `ReschedulePreviewToken`, `RescheduleToggleField`.

### `shared/bookings/` — recurring setup + new-booking modal (Phase 5 · Cluster 2 · Session 3)

> **Architectural note — these two screens have different home shapes.** Client Screen 16 (recurring setup) is a **full page**, not a modal — lives at `app/(client)/recurring/new/page.tsx` since it's client-only (no admin equivalent in the prototype). Admin Screen 21 (new booking) is a **Dialog-based modal** invoked from a "+ New booking" button on the admin calendar — no route of its own. The shared primitive that unifies both surfaces is `ChipSelector` (extracted to `shared/`) — used by the recurring day picker (`mono` variant), the modal service chips (`pill` variant), and any future single-select pill row.

- **`RecurringCustomerHeader`** (`bookings/RecurringCustomerHeader.tsx`) — 56px paper-2 avatar circle + 22px Inter Tight customer name + 2-line meta. `<strong>` segments render ink-bold (contact details + previous-booking emphasis). Sits at the top of the recurring-setup white card, with a paper-2 bottom-border separator.
- **`FrequencyGrid`** (`bookings/FrequencyGrid.tsx`) — `'use client'` 4-column option grid for the recurring frequency picker. Each card carries `{ id, name, meta }`; selected state = rust border + rust-soft bg + rust meta line; hover = rust border only. Local `useState` active state — lift to `value`/`onChange` when real persistence wires up.
- **`RecurringPreviewList`** (`bookings/RecurringPreviewList.tsx`) — paper-bg card listing the next-N visits. Mono eyebrow at the top (`<strong>` = rust) + `120px 1fr 90px` rows (mono date / time + visit sub / rust right-aligned price). Dotted bottom rules between rows, auto-suppressed on the last.
- **`RecurringSummaryBar`** (`bookings/RecurringSummaryBar.tsx`) — ink-bg summary footer with the configured frequency on the left (line 1 — `<strong>` = paper-bold) + secondary detail line + 22px rust-light `totalLabel` line, and a rust CTA button on the right. Renders the CTA as `next/link` when `ctaHref` is set, else as a click button.
- **`NewBookingModal`** (`bookings/NewBookingModal.tsx`) — `'use client'` booking modal used by both roles. Built on the canonical `Dialog`/`DialogContent` (size `lg` = 920px) with a custom header / scrollable body / footer rhythm. **Viewport-safe**: `max-h-[calc(100vh-4rem)]` + `overflow-y-auto` on the body so tall content scrolls inside the modal rather than overflowing the screen. Header: rust mono tag pill + 24px rust-em title + subtitle + custom paper-2 close button. Body: **optional** summary banner (admin only — ink-bg single-letter logo + headline + muted detail) + customer Input + date/time split row + service chip row (uses `ChipSelector` pill variant) + price/duration split + notes Textarea + 3-col row (assigned-to Input + 2 `ToggleField`s). Footer: muted info text (`<strong>` = ink) + Cancel/Save buttons. Stubbed — closes on either Cancel or Save without persisting.
- **`AddBookingButton`** (`bookings/AddBookingButton.tsx`) — `'use client'` rust primary button that opens `NewBookingModal`. Takes `data: NewBookingModalData` so each role passes its own stub (admin → `freshhomeNewBooking`; client → `voltlineNewBooking`). Mounted on both the admin calendar (alongside the client filter bar) and the client calendar (alongside the `+ Recurring` link). Replaces the earlier admin-only `admin/calendar/AddBookingButton.tsx`.

### `shared/` — new generic primitive added in Session 3

- **`ChipSelector`** (`shared/ChipSelector.tsx`) — `'use client'` generic single-select pill row. `options: ChipOption<T>[]` (`{ id: T, label }`) + `defaultId`/controlled `value`/`onChange`. `variant: 'pill' | 'mono'`. **Pill** (default) — Inter Tight 13px pill chip, ink-active on white card bg, hover-rust border. Used by the new-booking modal's service-chip row. **Mono** — small mono uppercase pill stretched flush via `flex-1`, ink-active on paper bg, hover-rust border. Used by the recurring day-picker row. Different from `shared/FilterChips`, which is a **multi-toggle filter** chip row (rust-active with count badges); `ChipSelector` is a single-select pick (ink-active, no badges). The two patterns share visual chrome but are semantically distinct — both intentionally kept separate.

### `shared/bookings/` — conflict + reschedule modals (Phase 5 · Cluster 2 · Session 4)

Two Dialog-based modals that complete the booking-edit flows. Both reuse the same body+footer rhythm pattern as `NewBookingModal` (custom `p-0 gap-0` on `DialogContent` + manual header/body/footer sections with paper footer + paper-2 dividers + custom paper-2 close button). Both are entry-pointed by a small `'use client'` trigger component that owns the modal `open` state.

- **`ConflictCard`** (`bookings/ConflictCard.tsx`) — single comparison card inside the conflict modal. `tone: 'attempted' | 'existing'` drives the colour: attempted = rust-soft bg + rust border + rust tag; existing = warn-soft bg + warn border + warn tag. Body is mono tag + 15px ink-bold `time` + 12px ink-soft `detail` line.
- **`ConflictOptionRow`** (`bookings/ConflictOptionRow.tsx`) — numbered option row in the conflict modal's option list. 28px col + 1fr text grid. 22px ink-bg/rust-light digit circle + (`<strong>` title block + 11px sub line). `selected` OR `recommended` flips the row to rust-soft bg + rust border. Stub layer: `onClick` swaps the selected row.
- **`ConflictModal`** (`bookings/ConflictModal.tsx`) — `'use client'` Dialog (size `default` = 560px) for client Screen 17. Header: warn-coloured tag + 22px rust-em title + subtitle (`<strong>` = ink). Body: 56px warn-soft ⚠ icon + 2-up `ConflictCard` grid + explainer paragraph + N `ConflictOptionRow`s with local `useState` selection. Footer: 3 buttons (Back / Cancel / Save). When `onSaveHref` is passed, the Save button renders as a `<Button asChild>` linking to that URL (used by the recurring page to navigate to `/calendar` on save); otherwise it closes the modal.
- **`RescheduleCompareCard`** (`bookings/RescheduleCompareCard.tsx`) — single compare card inside the reschedule modal. `tone: 'was' | 'now'`: was = paper-bg + rule border + `opacity-65` (dim previous state); now = rust-soft bg + rust border + rust tag (highlighted new state). Body is mono tag + 22px ink time + 14px ink-soft day line. Larger time font than `ConflictCard` per prototype.
- **`RescheduleSmsPreview`** (`bookings/RescheduleSmsPreview.tsx`) — SMS preview card: white card surface with a rust-mono eyebrow (leading rust dot) + a paper-bg/left-good-border SMS body. Body is rendered from `tokens: ReschedulePreviewToken[]` — each `type: 'text'` is plain copy, each `type: 'var'` renders the rust-tinted mono-styled variable highlight. Token-array shape lets the data declare which spans are variables without inline JSX.
- **`RescheduleModal`** (`bookings/RescheduleModal.tsx`) — `'use client'` Dialog (size `lg` = 920px) for admin Screen 23 + the client-side equivalent. Same viewport-safe header / scrollable body / footer rhythm as `NewBookingModal`. Body: 2-up `RescheduleCompareCard` grid + a reason `ChipSelector` (pill variant) + `RescheduleSmsPreview` + 2-col toggle row (per `RescheduleToggleField`, local `useState` per row). Footer: muted info text (`<strong>` = ink) + Cancel / Save buttons.
- **`RescheduleBookingButton`** (`bookings/RescheduleBookingButton.tsx`) — `'use client'` button + `RescheduleModal` pair. Takes `data: RescheduleModalData` and a `variant: 'button' | 'action-row'`. **`button`** (default, admin hero) renders a secondary `Button`. **`action-row`** (client booking rail) renders a `BookingActionBtn`-style ghost row with leading icon glyph — drops in for the client's "Reschedule" entry in the MANAGE action group. Both variants share the same modal open state.

### `shared/automations/` + `admin/automations/` — `/automations` list page (Phase 5 · Cluster 3 · Session A)

> **Architectural note — automations lives outside the role route groups**, same reason as settings / tickets / leads / calendar / dashboard. Single shared route `app/automations/` with `'use client'` layout (reads `useRole()`, picks `ClientSidebar` vs `AdminSidebar`, mounts `DevRoleSwitcher`) + `'use client'` dispatcher `page.tsx` + `_client-content.tsx` / `_admin-content.tsx` siblings. The two role bodies render **different shapes** (client = flat list of stats cards; admin = filter chips + workspace stats + grouped per-client mini-rows) — intentionally not converged, because they're genuinely different surfaces (personal flows vs cross-client roster). Session B will add `app/automations/[id]/` for the admin-only editor; that page is admin-only by role-gate inside the dispatcher.

- **`AutomationCard`** (`shared/automations/AutomationCard.tsx`) — **moved from `admin/onboarding/` in Cluster 3 · Session A** (no shape change). `'use client'` toggle-driven card with an expandable trigger + step list. Props: `automation: Automation` (`lib/onboarding/types`). Switch (with ON/OFF label) toggles enabled state; when on, renders the ink-bg trigger row + numbered step list. Each step row has a numbered ink circle (rust-light digit), channel pill (`sms` = good, `email` = info), mono delay, "Edit copy ✎" link, the copy box (with `[data-slot=var]` token-styled variables and a rust-soft "editing" tint when `step.isEditing`), and an optional mono meta bar. Currently consumed only by the onboarding wizard's Step 5; the Session B editor will compose adjacent primitives rather than reusing this card directly (different inputs / step actions).
- **`AutomationStatTile`** (`shared/automations/AutomationStatTile.tsx`) — single stat tile used inside `AutomationStatsCard`. Mono uppercase label + 22px Inter Tight value (ReactNode — `<em>` renders rust). Paper-bg `rounded-lg` surface; designed to sit in a `grid grid-cols-4 gap-2.5` row.
- **`AutomationStatsCard`** (`shared/automations/AutomationStatsCard.tsx`) — `'use client'` client-Screen-5 card: header (tag + title + description) + `Switch` (ON/OFF label) + a 4-tile stats grid below. Disabled state hides the stats grid entirely (paper-2 bg + 65% opacity). Stats render via `AutomationStatTile`. Different shape than `AutomationCard` — this card is **read-only with stats**, no expanded trigger/step body; the onboarding card is **structural with toggle**, no stats. Both coexist.
- **`AutomationInfoBanner`** (`shared/automations/AutomationInfoBanner.tsx`) — rust-soft tinted info banner with a rust `i` glyph + ReactNode body (`<strong>` = ink-bold). Used at the top of the client `/automations` page to explain "Webnua writes and manages these automations for you." New small primitive; if a second "this surface is managed by your operator" banner lands elsewhere on the client side, reuse this rather than re-inlining the chrome.
- **`AutomationFlowMini`** (`admin/automations/AutomationFlowMini.tsx`) — admin-Screen-10 compact per-client row inside a group. 6-column grid (`36px logo + 1fr name+flow + 3× 90px metric + 56px toggle`). Tinted logo from `AutomationClientTone` (`voltline`/`freshhome`/`keyhero`/`neatworks`/`flowline`/`generic` — same colour vocabulary as `BookingPill`, with `flowline` added for future use). When `flow.href` is set AND the flow is enabled, renders as `next/link` (whole row clicks through to the editor at `/automations/[id]`). The `Switch` is wrapped in a `stopPropagation` div so toggling doesn't trigger navigation. Disabled rows render with `paper-2` background + opacity and don't click through.
- **`AutomationGroup`** (`admin/automations/AutomationGroup.tsx`) — admin-Screen-10 group container. White card with a paper-2 header (title + ink/8 count badge + `<strong>`-bolded meta) + a stack of `AutomationFlowMini` rows separated by paper-2 borders. Driven by `AutomationGroup` data (`title`, `countBadge`, `meta`, `flows[]`).

### `lib/automations/` — automations stub data + types

- **`types.ts`** — single source of truth for automation shapes. List page (Session A): `AutomationClientTone` (mirrors calendar tones + adds `flowline`), `AutomationStat`, `AutomationStatsCard` (client card), `AutomationFlowMini` (admin mini-row), `AutomationGroup` (admin group container), `AutomationsHero`, `AutomationsWorkspaceStat`, `ClientAutomations`, `AdminAutomations`. Editor (Session B): `AutomationEditorChannel`, `AutomationEditorTrigger`, `AutomationEditorStep` (id / number / channel / delay / name / optional subject / body ReactNode with `[data-slot=var]` highlights / footerMeta / variables[] / `isEditing`), `AutomationVariable`, `AutomationPerformanceMetric` (with `tone: 'default' | 'accent' | 'good'`), `AutomationTestSendData`, and the top-level `AutomationEditor`.
- **`client-automations.tsx`** — Voltline 4-card stub for client Screen 5: hero + info banner ReactNode + 4 cards (3 enabled with 4-tile stats each, 1 disabled with no stats). `.tsx` because descriptions / hero subtitle / banner all carry inline `<em>` / `<strong>` JSX.
- **`admin-automations.tsx`** — Webnua Perth workspace-wide stub for admin Screen 10: hero + 5-chip client filter set (All clients / FreshHome / KeyHero / NeatWorks / Voltline) + 4 workspace stats (active flows / sent 7d / delivery / reply rate) + 4 groups (Instant confirm / 24h follow-up / Review loop / No-show recovery) with cross-client mini-rows. Each enabled row has an `href` to `/automations/[id]` for the upcoming editor wire-up. `.tsx` because group meta + stat values carry inline `<em>` / `<strong>` JSX.
- **`admin-editor.tsx`** — FreshHome 24-hour follow-up stub from admin Screen 17: page header copy + trigger config + 3 editor steps (step 2 in editing state with `subject` for the email; step 1 SMS / step 3 SMS) + 7 available variables + test-send modal config (with phone-bar / SMS preview / variables-filled line / options row / footer info) + performance metrics (Triggered / Replies (rust-em) / → Booked (good-em)) + footer actions (back / disable / save with the "auto-saved 8s ago" mono progress line). Drives `/automations/[id]` for admin. `.tsx` for the same JSX-in-fields reason.

---

### `admin/automations/` — automation editor + test-send (Phase 5 · Cluster 3 · Session B)

> **Architectural note — the editor lives at `/automations/[id]`** inside the shared `/automations` route tree (Next won't allow it inside `(admin)` simultaneously). The page is a `'use client'` dispatcher that role-gates internally: admin renders the editor; client falls back through `router.replace('/automations')` since there's no client editor. The `[id]` slug is decorative for the stub layer — the single canonical FreshHome 24h follow-up renders regardless of id. Admin Screen 10's `AutomationFlowMini` rows already carry `href` values pointing here from Session A.

- **`AutomationEditorLayout`** (`automations/AutomationEditorLayout.tsx`) — `1fr 340px` two-column wizard shell (canvas + sticky right rail). Canvas is wrapped in a white card surface (`rounded-xl border border-rule bg-card px-7 py-6.5`); the rail is a plain `sticky top-[100px]` flex-col. **3rd `1fr + fixed-rail` detail layout** alongside `TicketDetailLayout` + the inline lead-detail shape — see parked decisions for layout-shell consolidation.
- **`AutomationTriggerBox`** (`automations/AutomationTriggerBox.tsx`) — ink-bg trigger row with the rust ⚡ icon + `// TRIGGER` mono label + the trigger name + a rust-light "Change trigger →" affordance. `1fr` middle column holds the trigger summary.
- **`AutomationStepConnector`** (`automations/AutomationStepConnector.tsx`) — small 2px rule-coloured vertical line, centred between the trigger and each step. Pure decorative, `aria-hidden`.
- **`AutomationEditorStep`** (`automations/AutomationEditorStep.tsx`) — `'use client'` editable step. Header is a 6-col grid: numbered ink circle (rust-light digit) + channel pill (`sms` = good, `email` = info) + delay pill (mono with ◷ glyph) + editable name input + ↕ reorder + × delete (icon buttons via the inner `StepIconBtn`). Body: optional subject input (email only) + the message body in a tinted card with `[data-slot=var]` highlighting. `isEditing: true` flips the header bg to rust-soft, the body border to rust + rust-glow shadow, and tints the footerMeta rust. Footer: the meta string + the variable chip row (always leads with a "+ Insert variable" rust-soft chip). Inputs are local state — wire to backend when real save lands.
- **`AutomationAddStep`** (`automations/AutomationAddStep.tsx`) — dashed-border `+ Add another step (SMS / Email / Wait)` affordance. Hover-flips to rust border + rust-soft fill.
- **`AutomationVariableList`** (`automations/AutomationVariableList.tsx`) — right-rail card listing the 7 available variables. Each row: mono rust `{code}` + ink-quiet description + rust "+" affordance. Hover-tinted rust-soft.
- **`AutomationTestSendCard`** (`automations/AutomationTestSendCard.tsx`) — `'use client'` right-rail card that owns the modal `open` state. Heading + description + full-width ink button (hover-flips to rust). Mounts `AutomationTestSendModal` underneath.
- **`AutomationPerformanceCard`** (`automations/AutomationPerformanceCard.tsx`) — right-rail card with N rows of `label : value`. Per-row value tint: `accent` = rust, `good` = good-green, `default` = ink-bold. Dotted bottom rules between rows.

### `shared/automations/` — test-send modal (Phase 5 · Cluster 3 · Session B)

- **`AutomationTestSendModal`** (`automations/AutomationTestSendModal.tsx`) — `'use client'` Dialog (size `default` = 560px) for admin Screen 22. Same viewport-safe header / scrollable body / footer rhythm as `NewBookingModal` (`p-0 gap-0` on `DialogContent`, custom paper-2 close button, paper footer with paper-2 top border). Body: "Send to" readonly Input + SMS preview card (blue-tinted phone-frame surface with the white SMS bubble + the variables-filled mono line below) + paper test-options row (info text + Switch-to-email secondary button). Footer: Twilio Twilio-credit info text (`<strong>` = ink) + Cancel / Send buttons (both close the modal — wire to real send when backend lands).

### `shared/` — editor-mode footer actions (Phase 5 · Cluster 3 · Session B)

- **`EditorFooterActions`** (`EditorFooterActions.tsx`) — sibling to `BuilderFooterActions` (the wizard footer). Built separately because the interaction model differs: editor footers carry an "auto-saved" mono progress indicator + a 3-button action set (Back / Disable / Save) rather than the wizard's 2-button Back/Continue. No top border by default — the canvas above already carries its own card border; centred `mt-7` gap separates the rows. Used by the automation editor today; future editors (e.g. page builder) should reuse it. See parked decisions for the "revisit when a third footer-actions variant appears" rule.

### `shared/search/` — global search (Phase 6 · Cluster 6 · Session 2)

> **Architectural note — `/search` is a shared route.** `app/search/` has its own `layout.tsx` ('use client', `useRole()`, picks sidebar, mounts `DevRoleSwitcher`). `page.tsx` is a `'use client'` dispatcher that picks the role-scoped stub and renders the shared `SearchResultsView` — no `_admin-content` / `_client-content` siblings, because the two role bodies are structurally identical (same view, different data). Operators search across every client (admin Screen 35); a client searches their own account. `page.tsx` wraps the body in `<Suspense>` because it reads `?q=` via `useSearchParams`.

- **`GlobalSearchInput`** (`search/GlobalSearchInput.tsx`) — `'use client'` rounded-pill search field (⌕ icon + input + decorative `⌘K` hint). Enter routes to `/search?q=…`. Lives in the `Topbar` `search` slot (mounted on the overview hub now; full operator-surface rollout is a flagged follow-on) and at the top of `/search` itself.
- **`SearchResultRow`** (`search/SearchResultRow.tsx`) — single result row, a `next/link`: kind-tinted 40px avatar tile (initials for people, glyph otherwise) + title + meta (both run through `highlightMatch`) + `→`. `KIND_TILE` maps the six `SearchResultKind`s to tone classes.
- **`SearchResultGroup`** (`search/SearchResultGroup.tsx`) — one per-kind group: mono label + ink count chip header, then a bordered card of `SearchResultRow`s.
- **`SearchResultsView`** (`search/SearchResultsView.tsx`) — the shared `/search` body: an ink result-count summary strip + the per-kind groups. Role-agnostic — operator and client pass their own `SearchResults`.

### `lib/search/` — global search data + types

- **`types.ts`** — `SearchResultKind` (`lead | booking | review | conversation | customer | client`), `SearchResult` (`{ id, kind, avatar, title, meta, href }` — each result a typed entity with a real detail `href`), `SearchResultGroup`, `SearchResults`.
- **`highlight.tsx`** — `highlightMatch(text, query)` → ReactNode, wrapping case-insensitive query matches in a rust `<mark>`.
- **`admin-search.ts`** — `adminSearchResults`: the operator cross-client stub (Screen 35, query "sarah" — leads / bookings / reviews / conversations across all clients).
- **`client-search.ts`** — `clientSearchResults`: the client single-business stub (query "emma", Voltline's own account — no cross-client, no `client` kind).

### `shared/` — rail-card primitives (Phase 6 · Cluster 6)

> The canonical white-card side-rail pattern. Extracted in Cluster 6 (mechanical rename) from the previously-separate `TicketSideCard` / `TicketPropertyRow` (tickets + bookings) and `LeadRailCard` / `LeadRailRowItem` (leads) — all four deleted, no re-export shims. Used by the tickets, leads, and bookings detail rails, and the single-client overview hub.

- **`RailCard`** (`RailCard.tsx`) — right-rail card wrapper. Props: `heading` (mono uppercase string, e.g. `// Status`), `tone: 'light' | 'dark'` (default light = white card with `border-ink/8`; dark = ink-bg paper-text — used by `TicketStatusCard`), optional `rows: RailRow[]` shorthand (each row renders as a mono-labelled `RailPropertyRow`), `children` (for bespoke bodies — status pickers, action lists). `rounded-[14px] px-5 py-[18px]`. `RailRow` (`{ label, value, accent?, tone? }`) is structurally compatible with `lib/leads/types.ts` `LeadRailRow` — feature types stay put (same pattern as `FilterChips`).
- **`RailPropertyRow`** (`RailPropertyRow.tsx`) — single label/value row inside a `RailCard`. Props: `label` (string) + `value` (ReactNode) + `editable` (boolean — appends a mono `✎` glyph) + `tone: 'good' | 'quiet' | 'default'` (value colour) + `accent` (boolean — rust value, overrides `tone`) + `labelStyle: 'plain' | 'mono'` (default `plain` — quiet sentence-case label for tickets/bookings; `mono` — mono uppercase label, used by the leads rail rows). `space-between` flex, dotted bottom rule between rows, suppressed on the last. Tighter than `SettingsFieldRow` because the 320px side panel can't carry settings' 220px label column.

### `shared/` — activity feed (Phase 6 · Cluster 6 · Session 1a)

> The canonical flat activity-log card. Extracted at the third-surface trigger (the single-client overview hub) from the previously-sibling `CampaignActivityCard`. **`LeadTimeline` is deliberately NOT consolidated here** — a timeline (dot-spine, scheduled-future pending events, tinted snippet quotes, gapped rows) is a different structural concern from a flat feed; folding it in would re-create the optional-slot grab-bag. The two coexist by design.

- **`ActivityRow`** (`ActivityRow.tsx`) — one feed row. `row: ActivityRowData` does the work; `ActivityRowData` is **event-shaped** per vision §7 — `{ id, icon, tone, actor?, body, detail?, time, href? }`, a typed attributable event, not rendered prose. `tone: ActivityTone` is a colour-keyed vocabulary (`info | amber | good | rust`) — consumers map their own domain categories onto these slots. `grid-cols-[32px_1fr_auto]`: tone-tinted 32px icon tile + body (rust-bold `actor` + `body` + quiet `detail` block) + right-aligned mono `time`. Dotted bottom rule between rows. Renders as a `next/link` when `href` is set.
- **`ActivityFeed`** (`ActivityFeed.tsx`) — white-card wrapper: `title` (ReactNode, `<em>` = rust) + optional `sub` (`<strong>` = ink-bold) + optional `action` (header-right ReactNode, e.g. an "All →" link — added Cluster 6 · 1b) + a stack of `ActivityRow`s from `items: ActivityRowData[]`. `rounded-xl border border-rule bg-card px-6 py-5.5`. Used by client `/campaigns` (via the `CampaignActivityCard` adapter) and the single-client overview hub.

### `shared/reviews/` + `client/reviews/` — `/reviews` page (Phase 5 · Cluster 4 · Session A)

> **Architectural note — reviews lives outside the role route groups**, same reason as settings / tickets / leads / calendar / automations / dashboard. Single shared route `app/reviews/` with `'use client'` layout (reads `useRole()`, picks `ClientSidebar` vs `AdminSidebar`, mounts `DevRoleSwitcher`) + `'use client'` dispatcher `page.tsx` + `_client-content.tsx` / `_admin-content.tsx` siblings. Client renders a single-business reviews surface (client Screen 6) + the negative-review intercept modal (prototype admin Screen 24 — relocated to client side, see prototype-disagreement note below); admin renders a cross-client per-client-card grid (admin Screen 12). The two role bodies share `ReviewSummaryHeader` (sized down inside the per-client card) and `ReviewItem` (with `variant: 'full' | 'compact'`) — single canonical components, no role-specific duplicates.
>
> **Prototype-disagreement note — negative-review alert is client-side, not admin.** The platform prototype mounts the 2★ alert modal on admin Screen 24 ("Call Jamie immediately to apologise" framed as the operator's action). In practice the business owner is the one with the customer relationship and the one who'd actually call back; the operator's role here is to be informed and to adjust ad copy if a pattern emerges. The modal lives in `shared/reviews/` (reusable shape) but its only mount today is on client `/reviews` via `client/reviews/NegativeReviewAlertButton`. If a future operator-facing "FreshHome just got a 2★" surfacing lands in Cluster 6, build that as a sibling — don't shoehorn it into this modal.

- **`ReviewSummaryHeader`** (`reviews/ReviewSummaryHeader.tsx`) — big rating block (Inter Tight rating value with `<em>` rust on the ★ glyph + stars-row + mono meta line). `size: 'lg' | 'sm'` controls scale (56px headline on client Screen 6; 26px headline inside admin per-client cards). `align: 'left' | 'right'` so the same component can sit left-aligned in the client summary and right-aligned in the admin per-client card header. Single canonical — don't sibling per-surface.
- **`ReviewDistributionBars`** (`reviews/ReviewDistributionBars.tsx`) — 5★→1★ horizontal bar chart driven by `ReviewDistributionRow[]` (stars / count / pct). Rust-fill on paper-2 track, mono labels. Used by client `/reviews` today; lives in `shared/` so admin can reuse if a future single-client admin drill-down appears.
- **`ReviewCallout`** (`reviews/ReviewCallout.tsx`) — ink-bg "Asked, not begged." motivational card on the right of the client summary row. ReactNode `headline` (`<em>` = rust-light on ink) + sub line + a rust-light mono link.
- **`ReviewItem`** (`reviews/ReviewItem.tsx`) — single canonical Google review entry, discriminated by `variant`. **`full`** (client Screen 6) = `44px avatar + 1fr body + 100px stars + 80px age` grid with name + job + quote + filled-star row + mono age. **`compact`** (admin per-client cards) = `1fr text + auto right-aligned stars-and-age` with `<strong>Name</strong> · "quote"` and right-aligned `★★★ · age`. Same data shape, two layouts.
- **`ReviewMiniStat`** (`reviews/ReviewMiniStat.tsx`) — small stat tile inside admin per-client cards (NEW · 7D / 5-STAR / REQUEST → REVIEW). 20px Inter Tight value + mono uppercase label. Smaller than `StatCard`, intentionally — sits inside a 3-up grid inside an already-bordered card.
- **`ReviewClientCard`** (`reviews/ReviewClientCard.tsx`) — admin per-client card. White card composing logo + name+meta header row + right-aligned `ReviewSummaryHeader` (size `sm` + align `right`) + 3-up `ReviewMiniStat` grid + `// RECENT` block of `ReviewItem` (compact). Discriminated by `card.kind` — `connected` renders the full content; `empty` (Voltline pre-launch) renders an opacity-dimmed card with description + "Connect Google Business" CTA.
- **`NegativeReviewActionRow`** (`shared/reviews/NegativeReviewActionRow.tsx`) — numbered suggested-action row inside the negative-review modal. 3-column grid: 24px ink/rust-light digit circle + 1fr title+sub body + auto right-aligned "Recommended" pill. `recommended: true` flips the row to rust-soft bg + rust border + shows the pill. Structurally close to `shared/bookings/ConflictOptionRow` but adds the visible pill column — parked-decision: extract a generic numbered-option-row shared primitive on the third instance.
- **`NegativeReviewModal`** (`shared/reviews/NegativeReviewModal.tsx`) — `'use client'` Dialog (size `default` = 560px). Same viewport-safe header / scrollable body / footer rhythm as `NewBookingModal` (`p-0 gap-0` on `DialogContent`, custom paper-2 close button, paper footer with paper-2 top border). Header: warn-tinted tag (with leading warn dot) + 24px rust-em title + subtitle. Body: centered 56px warn-soft `!` icon + warn-tinted quote block (stars + italic quote + mono meta with `<strong>` ink emphasis) + numbered `NegativeReviewActionRow` list. Footer: muted footer info (`<strong>` = ink) + Dismiss / Call buttons (both close the modal — wire to real call/dismiss when backend lands). Lives in `shared/` (reusable shape); currently only mounted on client `/reviews`.
- **`NegativeReviewAlertButton`** (`client/reviews/NegativeReviewAlertButton.tsx`) — `'use client'` trigger button + modal-open state owner. Stub-only entry point: the real build fires the modal from a low-rating webhook the moment Google surfaces a sub-4★ review, but the stub layer mounts a "⚠ Show 2★ alert" secondary `Button` next to the client `/reviews` `PageHeader`. Delete the button (and the data import) when the webhook trigger lands; keep `NegativeReviewModal` either way.

### `lib/reviews/` — reviews stub data + types

- **`types.ts`** — single source of truth for review shapes: `ReviewItem` (data shape used by both row variants), `ReviewDistributionRow`, `ReviewSummaryData`, `ReviewMiniStat`, `ReviewClientCardData` (discriminated `kind: 'connected' | 'empty'`), `ReviewCalloutData`, `ClientReviewsPage`, `AdminReviewsPage`, and the negative-review modal shapes (`NegativeReviewActionData`, `NegativeReviewQuoteData`, `NegativeReviewModalData`).
- **`client-reviews.tsx`** — `voltlineClientReviews`: Voltline's first-14-days reviews surface (client Screen 6). Summary (4.9★ / 7 REVIEWS · 14 DAYS) + 5★-only distribution + ink "Asked, not begged" callout + 7-review list. `.tsx` because titles / subtitles / callout headline carry inline `<em>` / `<strong>` JSX.
- **`admin-reviews.tsx`** — `adminReviews`: Webnua Perth cross-client stub (admin Screen 12). Hero + 5-chip workspace filter (All clients + FreshHome / KeyHero / NeatWorks — Voltline excluded because not yet connected) + 4 workspace stats (`StatCard`-driven) + 4 per-client cards (3 connected + 1 empty Voltline placeholder). `.tsx` for the same JSX-in-fields reason.
- **`client-negative-modal.tsx`** — `voltlineNegativeReview`: Voltline 2★ Jamie K. stub (reframed from the prototype's KeyHero/admin-side copy — see prototype-disagreement note above). Quote + 4 suggested actions (action 1 recommended; action 4 = "Flag to Craig at Webnua for ad messaging review") + footer "first negative review in your first 14 days" reassurance.

### `shared/campaigns/` + `admin/campaigns/` — `/campaigns` page (Phase 5 · Cluster 4 · Session B)

> **Architectural note — campaigns lives outside the role route groups**, same reason as reviews / settings / tickets / etc. Single shared route `app/campaigns/` with `'use client'` layout + `'use client'` dispatcher + `_client-content.tsx` / `_admin-content.tsx` siblings. The two role bodies render **different shapes** — client is a single-campaign deep dive (managed band + active hero card + 4-week trend chart + activity log + change CTA); admin is a flat cross-client roster (filter chips + 4 workspace stats + N campaign rows + Meta Ads info bar). Intentionally not converged — they're genuinely different surfaces (single-campaign view vs multi-campaign roster), and forcing a shared shell would only produce a grab-bag of optional slots.

- **`CampaignManagedBand`** (`campaigns/CampaignManagedBand.tsx`) — ink-bg reassurance band at the top of client `/campaigns`. 3-column grid: 56px rust-tinted icon tile + tag/title/sub body (`<em>` = rust-light, `<strong>` = paper-bold) + ghost "Text Craig" CTA on the right. Standalone — distinct from `AutomationInfoBanner` (rust-soft chip) and from `BillingPlanCard` (plan-meta + headline + action). If a second ink-bg "managed by your operator" band lands elsewhere on the client side, revisit extraction.
- **`CampaignMetricTile`** (`campaigns/CampaignMetricTile.tsx`) — single metric tile inside the active-campaign hero. 10px mono uppercase label + 32px Inter Tight value (`<em>` = rust) + sub trend line with `trendTone: 'good' | 'warn' | 'quiet'`. Sized larger than `StatCard` (32px vs 30px) AND lives inside a parent card with no border of its own — the 1px paper-2 gap between tiles comes from the parent's grid background trick.
- **`CampaignPlainEnglish`** (`campaigns/CampaignPlainEnglish.tsx`) — rust-soft tinted "In plain English" explainer card. 2-column grid: 32px rust circle with `i` glyph + ReactNode body (`<em>` = rust bold, `<strong>` = ink-bold). Sits below the metric grid inside the active-campaign hero.
- **`CampaignHeroCard`** (`campaigns/CampaignHeroCard.tsx`) — active-campaign hero on client `/campaigns`. White card composing header row (pulsing-good-dot eyebrow + 22px name with `<em>` rust + meta line + good-soft status pill) + 4-tile `CampaignMetricTile` grid with 1px paper-2 separators + a `CampaignPlainEnglish` block in the footer. Single-campaign surface — admin has no equivalent.
- **`CampaignTrendChart`** (`campaigns/CampaignTrendChart.tsx`) — CSS-only 4-week grouped-bar chart (leads vs spend). Hand-rolled with a 60px y-axis column + a 1fr bars area; each week column renders two stacked-end bars (leads filled rust / spend paper-2 outlined) scaled against `leadsMax` / `spendMax`. Bar labels float above their bars; the "current" week gets a rust glow shadow + rust week label. No chart library — the prototype shape is small enough that hand-rolling is cleaner than pulling in a dep; revisit if a second chart shape lands elsewhere.
- **`CampaignActivityCard`** (`campaigns/CampaignActivityCard.tsx`) — "What Webnua's done lately" activity log on client `/campaigns`. **As of Cluster 6 · Session 1a this is a thin domain adapter** over the shared `ActivityFeed` (see the `shared/` activity-feed entry above): it maps the campaign activity-category vocabulary (`creative`/`audience`/`budget`/`tune`) onto `ActivityFeed`'s colour-keyed tone slots and shapes `CampaignActivityItem` into `ActivityRowData`. Keeps its `{ data, className }` API so `/campaigns` is unchanged; `CampaignActivityItem` stays uncoupled from the row shape.
- **`CampaignChangeCard`** (`campaigns/CampaignChangeCard.tsx`) — bottom "Want to change something?" CTA card on client `/campaigns`. White card + body text + N right-aligned `Button`s. Buttons render through the canonical `Button` primitive (`primary: true` → rust default; otherwise secondary).
- **`CampaignSparkline`** (`campaigns/CampaignSparkline.tsx`) — small inline SVG sparkline used on admin `/campaigns` rows. Rust stroke with a soft rust fill below the line; dashed flat-line when `points` is omitted (pre-launch / pending rows). Lives in `shared/` because it's generic enough to be reusable.
- **`CampaignStatusPill`** (`admin/campaigns/CampaignStatusPill.tsx`) — mono uppercase pill used on admin `/campaigns` rows. `active` = good-green with pulsing dot; `paused` / `pending` = ink-quiet with static dot. `label` overrides the status-keyed default. Distinct vocabulary from the existing tickets/leads status-pill families — kept as its own component; revisit if a third campaign-status surface appears.
- **`CampaignClientRow`** (`admin/campaigns/CampaignClientRow.tsx`) — admin-only cross-client roster row. 7-column grid: 36px ink-bg logo with rust-light glyph + 1.4fr name+meta + 110px `CampaignStatusPill` + three 100/110px metric cells (each with a ReactNode `value` for rust accent + small mono `sub`) + 100px `CampaignSparkline`. `row.dimmed` opacity-fades the whole row (used for Voltline pre-launch).

### `lib/campaigns/` — campaigns stub data + types

- **`types.ts`** — single source of truth for campaign shapes. Client side: `CampaignActivityIconTone` (`creative | audience | budget | tune`), `CampaignMetric`, `CampaignManagedBandData`, `CampaignHeroData`, `CampaignTrendWeek` + `CampaignTrendChartData`, `CampaignActivityItem` + `CampaignActivityData`, `CampaignChangeCardData`, top-level `ClientCampaignsPage`. Admin side: `AdminCampaignStatus` (`active | paused | pending`), `AdminCampaignRow`, top-level `AdminCampaignsPage` (hero + filters + stats + rows + footer info bar).
- **`client-campaigns.tsx`** — `voltlineClientCampaigns`: Voltline $99 emergency call-out single-campaign deep dive (client Screen 18). Managed band + active hero (4 metrics + plain-English explainer) + 4-week leads-vs-spend trend + 5-item activity log + change CTA. `.tsx` because titles / metric values / activity bodies all carry inline `<em>` / `<strong>` JSX.
- **`admin-campaigns.tsx`** — `adminCampaigns`: Webnua Perth cross-client roster (admin Screen 11). Hero + 5-chip workspace filter (All clients + FreshHome / KeyHero / NeatWorks / Voltline pre-launch) + 4 workspace stats (SPEND 7D / LEADS 7D / AVG CPL / AVG ROAS) + 4 rows (3 active + 1 pending Voltline pre-launch) + bottom "Open Meta Ads ↗" info bar. `.tsx` for the same JSX-in-fields reason.

### `lib/auth/` — user + capability layer (Phase 6 · Cluster 5 · Session 1a)

The spine of the cluster 5 builder access model. See `reference/builder-design.md` §1 for full rationale. One editor renders for everyone; capabilities decide which controls are live. Role still drives sidebar shape (which nav set / workspace block); capabilities are the finer-grained layer inside the editor.

- **`capabilities.ts`** — single source of truth for the cap layer. 13 capabilities (`viewBuilder`, `editCopy`, `editMedia`, `editSEO`, `editLayout`, `editSections`, `editTheme`, `editPages`, `useAI`, `publish`, `approve`, `rollback`, `manageDomain`). Exports `ALL_CAPABILITIES`, `ADMIN_DEFAULTS` (= all caps), `CLIENT_DEFAULTS` (= just `viewBuilder`), `CAPABILITY_LABEL` (`Record<Capability, string>` — the affirmative display name per cap, e.g. `editCopy → 'Edit page copy'`; the SoT for labelling a capability in any human-facing UI — added Cluster 6 · team invite), plus `Role`, `CapabilityGrant` (`{ userId, websiteId | '*', capabilities }` — shape stays preset-compatible per design doc §1.4 forward-compat note), and `User` (`{ id, displayName, email, role, clientId: string | null, capabilities: Set<Capability> }` — `clientId` is the user's home client business, null for operators; `capabilities` is **derived** from role defaults + grants every render). Force-publish is **not** a separate cap — break-glass discipline is UI + audit log layered on top of `publish`, gated at the call site by `role === 'admin'`.
- **`explainers.ts`** — `CAP_EXPLAINER: Record<Capability, { short, requestLabel }>` — the per-cap human strings surfaced by `<CapabilityGate>` (tooltip in `disable` mode, affordance label in `request` mode). Empty `requestLabel` opts a cap out of `request` mode entirely (currently `viewBuilder` + `approve`). Hardcoded "your operator" / "your account" — see the parked decision in Open decisions about an `{operatorLabel}` token for multi-operator deploys.
- **`user-stub.tsx`** — **STUB.** localStorage-backed React provider + hooks. Four hardcoded stub users (Craig admin / Mark@Voltline editor / Liam@Voltline view-only / Anna@FreshHome view-only — two Voltline users so the sub-account view exercises the multi-user-per-client case). Each `StubUserDef` carries a `clientId` (operators have `null`) and `accessibleWebsiteIds: string[]` (references into the website data layer) so the workspace context can resolve "which users belong to this client". Three storage keys: `webnua.dev.active-user-id` (current session), `webnua.dev.grants` (per-user grant overrides edited from `/settings/access`), `webnua.dev.view-as-user-id` (admin-only impersonation override). Helpers: `getUserDefsForClient(clientId)`, `getClientUserDefs()`. The previously-duplicated `StubWebsite` type + `STUB_WEBSITES` const + `findWebsite`/`getWebsitesForClient` helpers were removed in Session 2; website data now lives canonically in `lib/website/data-stub.tsx`. The two helpers are re-exported from `user-stub.tsx` for any caller transitioning import paths but new code should import directly from `@/lib/website/data-stub`. Exports `<UserProvider>`, `useUser()` → `User | null` (actual user, **not** the view-as override — so role-shape decisions don't flip), `useUserContext()` (full context — `user`, `viewAsUser`, `effectiveCapabilities`, `allUsers`, `setUserId`, `clearUser`, `setViewAsUserId`, `setUserGrant`, `resetGrants`), `useCapabilities()` → `Set<Capability>` (**effective** set: view-as user's caps when the override is active, else the user's), `useCan(cap)`, `useCanAny(...caps)`, `useCanAll(...caps)`, `useIsViewingAs()`, plus a derived `useRole()` that preserves the legacy `{ role, hydrated, setRole, clearRole }` shape exactly — every consumer of the old `role-stub` continues to work unchanged. **Session 7** added `<CapabilityOverrideProvider>` — wraps a subtree to lock the effective capability set to a fixed list; `useCapabilities()` consults it before the user's resolved set (used by the wizard-frame editor to lock onboarding to `{editCopy, editMedia, useAI}`). Also re-exports `STUB_USER_DEFS` (the editable definitions, used by `/settings/access` for the grid rows) and a legacy `STUB_USERS` (pre-resolved snapshot, kept for the dev switcher's quick lookup). Delete the file when real auth ships.
- **`audit-stub.ts`** — `ForcePublishEntry` type + `STUB_FORCE_PUBLISH_LOG` (2 seed entries). **Session 5 (F)** added the overlay write path: `appendForcePublishEntry(entry)` pushes new entries into `webnua.dev.audit-log`, `getEffectiveAuditLog()` returns the merged seed + overlay sorted newest-first, `subscribeAudit(cb)` glues to the shared event bus. `publish-stub.publishDraft({ force: { reason } })` calls into this on every break-glass publish. `/settings/access` reads via `useSyncExternalStore(subscribeAudit, getEffectiveAuditLog)` so new entries appear without reload. Replace with backend-resolved entries when publish lands.

### `lib/website/` — website data model + section registry (Phase 6 · Cluster 5 · Session 2)

The data shape every later session builds against. See `reference/builder-design.md` §2 for the full design. Single-shape versioning (drafts and published share `Version`, distinguished by `status`). Section data is `Record<string, unknown>` at the registry boundary — each section type's actual shape is exported from its own module and asserted at the Fields/Preview component boundary.

- **`types.ts`** — `Website` (`{ id, clientId, name, domain, draftVersionId, publishedVersionId | null, createdAt, updatedAt }` — no `brand`, that lives on the Client now), `Page` (`{ id, websiteId, slug, title, type: PageType, sections[], seo, createdAt, updatedAt }`), `Section` (`{ id, type: SectionType, enabled, data, ai? }`), `Version` (`{ id, websiteId, status, snapshot: { pages, header, footer, nav, pageOrder }, createdBy/At, publishedAt/By?, notes?, parentVersionId? }`), `NavLink` (`{ label, target: { kind: 'page', pageId } | { kind: 'href', href } }`), `BrandObject` + `VoiceTone` (kept here for now — both Funnel and Website consume them; backend pass can hoist to `lib/brand/types.ts`), `WebsiteDomain`, `ContainerKind` (`'page' | 'funnelStep' | 'websiteHeader' | 'websiteFooter'` — drives the registry's singleton constraint). **11 section types**: `hero | offer | trust | services | reviews | faq | cta | schedulePicker | thanksConfirmation | header | footer` — the last two are website-level singletons (see registry). **5 page types**: `home | about | services | contact | generic` (funnel page types removed — those live in `lib/funnel/types.ts`). `MAX_NAV_LINKS = 6` const exports the V1 forcing-function cap (design doc §2.5).

- **`registry.ts`** — `SectionTypeDefinition<TData>` (`{ type, label, description, defaultData, Fields, Preview, capabilityHints?, allowedContainers, allowedPageTypes?, implemented }`). `allowedContainers: readonly ContainerKind[]` enforces singleton vs stackable semantics: `'header'`/`'footer'` have `allowedContainers: ['websiteHeader' | 'websiteFooter']` and are not stackable in `Page.sections[]`; stackable types use `['page', 'funnelStep']`; funnel-only types (`schedulePicker`, `thanksConfirmation`) use `['funnelStep']`. `defineSection<TData>(def)` is a type-preserving helper for registrations.

- **`sections/index.ts`** — the canonical const array `SECTION_REGISTRY` (no mutable registry; immutable list at module load). Order: stackable types first (hero / offer / trust / services / reviews / faq / cta), then funnel-only (schedulePicker / thanksConfirmation), then website-level singletons (header / footer). Accessors: `getSectionDefinition(type)`, `getImplementedSections()`, `getPlaceholderSections()`, `getSectionsForContainer(container: ContainerKind)`. Re-exports every section's `*Data` type for typed consumers.

- **`sections/_shared/CopyField.tsx`** (Session 4) — text/textarea field wired with per-field capability gating + AI controls. Wraps `BuilderField`/`BuilderInput`/`BuilderTextarea` in `<CapabilityGate capability="editCopy" mode="request">` so view-only users see the request-change affordance on hover. When `useAI` is held, renders inline rust-soft `✦ Regen` and `↶ Original` buttons in the field's `hint` slot — `Regen` cycles through a hardcoded `alternatives: readonly string[]` (stub-layer; Session 6 wires real AI), `Original` reverts to the `originalValue` prop. The `capability` prop defaults to `'editCopy'` but accepts `'editSEO'` for SEO-bound text. Every implemented section's Fields component uses this helper instead of raw `BuilderInput`.

- **`sections/_shared/MediaField.tsx`** (Session 4) — image-URL field gated on `editMedia`. Sibling to `CopyField`. Stub-layer is URL text input; real upload + crop UI lands when asset management ships.

- **`sections/hero.tsx`** (Session 2 → Session 4) — `HeroData`: eyebrow + headline + sub + 2 CTAs + hero image URL. Fields uses `CopyField` for every text (headline + sub carry AI alternatives) and `MediaField` for the image URL. Preview: 2-col layout with brand-tinted eyebrow + headline + CTAs + image placeholder. `allowedContainers: ['page', 'funnelStep']`.

- **`sections/offer.tsx`** (Session 2 → Session 4) — `OfferData`: tag + title + price label/caption + newline-separated inclusions + scarcity copy + CTA. AI alts on title + scarcity. Preview: single-offer card with brand-tinted price + bulleted inclusions + warn-tinted scarcity + CTA. `allowedContainers: ['page', 'funnelStep']`.

- **`sections/services.tsx`** (Session 2 → Session 4) — `ServicesData`: title + intro + `services: ServiceItem[]` (id/name/priceFrom/durationLabel/description). Add/remove buttons are `editLayout`-gated (`hide` mode on Remove, `disable` mode on the Add row). Per-item text fields use `CopyField` without alternatives (too item-specific to canonicalise). Preview: 2-col grid of service cards. `allowedContainers: ['page', 'funnelStep']`.

- **`sections/trust.tsx`** (Session 4) — `TrustData`: eyebrow + rating value/max + rating source + years label + licence label + guarantee label. Preview: wrap-flex of trust pills, rating tile uses brand colour. `allowedContainers: ['page', 'funnelStep']`.

- **`sections/reviews.tsx`** (Session 4) — `ReviewsData`: title + intro + `reviews: ReviewItem[]` (author/rating/body/age). Add/remove gated like services. Preview: 3-col grid of review cards with brand-coloured star strings. `allowedContainers: ['page', 'funnelStep']`. Real GBP auto-pull arrives when the GBP integration wires in.

- **`sections/faq.tsx`** (Session 4) — `FAQData`: title + intro + `items: FAQItem[]` (id/question/answer). Preview shows all Q/A pairs open (no accordion in the editor preview for scannability — real frontend renders the accordion). Add/remove gated. `allowedContainers: ['page', 'funnelStep']`.

- **`sections/cta.tsx`** (Session 4) — `CTAData`: tag + headline + sub + button label/href. AI alts on headline + sub. Preview: centred dark `bg-ink` callout block with brand-coloured tag, large headline, and a glowing brand-coloured CTA button. `allowedContainers: ['page', 'funnelStep']`.

- **`sections/schedulePicker.tsx`** (Session 4) — `SchedulePickerData`: title + intro + duration label + earliest-slot caption. **Funnel-only** (`allowedContainers: ['funnelStep']`). Preview renders a 4-col mockup of available time slots with a "Mockup · real availability pulls from the calendar integration" footer. Real picker wires in when bookings backend is ready.

- **`sections/thanksConfirmation.tsx`** (Session 4) — `ThanksConfirmationData`: title + body + detail line + referral title/body/CTA. **Funnel-only** (`allowedContainers: ['funnelStep']`). Preview: centred success ✓ + body + optional referral block. Leaving `referralTitle` blank hides the entire referral block in the preview.

- **`sections/header.tsx`** (Session 4) — `HeaderData`: logo text + logo image URL + CTA label/href + `showCta` flag. **Website-level singleton** (`allowedContainers: ['websiteHeader']`). Preview: horizontal bar with brand-coloured logo + dashed-border `[ site nav · from Website.nav ]` placeholder + brand-coloured CTA. **Important:** nav data lives on `Website.nav` (NOT in `HeaderData`); the Session 4 Header editor does NOT wire nav editing — that requires extending the singleton editor's API to thread `Website`-level context into Fields, which is a deliberate punt for a future polish pass. The preview's nav placeholder communicates the boundary.

- **`sections/footer.tsx`** (Session 4) — `FooterData`: brand line + phone/email/address + 3 socials + legal copy. **Website-level singleton** (`allowedContainers: ['websiteFooter']`). Preview: 3-col footer on `bg-ink`, brand-coloured section labels, legal copy under a separator. Empty social handles hide rather than render.

- **`sections/placeholders.tsx`** (Session 4) — **empty in Session 4** (all eight previously-stubbed types graduated). The file remains as the canonical home for any FUTURE V2 placeholder types — `makePlaceholderSection` factory + `PlaceholderFields` + `makePlaceholderPreview` are still exported for that case.

- **`data-stub.tsx`** — three stub websites demonstrating the publish lifecycle, plus the brand-by-client store (brand lives on the Client now per design doc §2.3; no `brand` field on `Website`). **Voltline**: 3 pages (Home + About + Contact populated, Services page exists with empty sections — partial-build state), published. **FreshHome**: 4 pages fully published (Home / About / Services / Contact), draft ahead of published with hero copy edits on Home — demonstrates the Lane B submit-for-review flow when Session 5 lands. **KeyHero**: 1 home page, draft only, never published — first-publish state for Session 8. NeatWorks has no website (empty state in the agency-mode roster). Exports `STUB_WEBSITES`, `STUB_VERSIONS`, `DEFAULT_PREVIEW_BRAND`, plus accessors `findWebsite`, `findWebsiteByClient`, `getWebsitesForClient`, `findVersion`, `getDraftForWebsite`, `getPublishedForWebsite`, and the brand resolver `getBrandForClient(clientId)`. **Intentionally separate from `lib/onboarding/voltline-build.tsx`** — that file is the wizard's pre-registry stub; the Session 7 funnel data-stub (`lib/funnel/data-stub.tsx`) is what re-expresses the same Voltline content as section-registry-backed funnel steps for the editor.

### `lib/funnel/` — funnel data model + stub (Phase 6 · Cluster 5 · Session 3.5 types; Session 7 stub)

> **Architectural note.** Funnel types were locked shape-only in Session 3.5; Session 7 added the first stub data + editor wiring. The funnel data layer lives alongside the website data layer (same `Section` model, separate top-level type). See design doc §2.0 for the funnel-vs-website distinction.
>
> **Naming overlap** with `lib/funnels/` (plural, Cluster 4) is intentional: `lib/funnel/` (singular) is the editable build model; `lib/funnels/` (plural) is the analytics-detail stub for `/funnels/[id]`. Different shape, different concern. The two coexist for the same Voltline funnel — id `emergency-call-out` resolves in both stores. If a third domain joins, consider hoisting to `lib/funnel/{builder,analytics}/`.

- **`types.ts`** — `Funnel` (`{ id, clientId, name, domain, draftVersionId, publishedVersionId, createdAt, updatedAt }` — no `brand`, resolves through Client like Website does), `FunnelStep` (`{ id, funnelId, slug, title, type: FunnelStepType, sections[], seo, createdAt, updatedAt }`), `FunnelStepType` (`'landing' | 'schedule' | 'thanks' | 'optin' | 'upsell'` — distinct from website's `PageType`; design doc §2.0 spells out why), `FunnelVersion` + `FunnelVersionSnapshot` (`{ steps, stepOrder }`), `FunnelVersionStatus` (mirrors `VersionStatus`). Imports `Section` and `WebsiteDomain` from `lib/website/types.ts` — sections and domain shape are shared, everything else is funnel-specific.

- **`data-stub.tsx`** (Session 7) — Voltline's "$99 emergency call-out" funnel built against the section registry. Three steps: `landing` (hero + offer + trust + services + cta), `schedule` (schedulePicker), `thanks` (thanksConfirmation). Section content sourced from the onboarding wizard's stubbed answers (`lib/onboarding/voltline-build.tsx`) so the wizard's published output and the funnel editor's seed agree. One published version + one draft (identical for now). Exports `STUB_FUNNELS`, `STUB_FUNNEL_VERSIONS`, plus accessors `findFunnel`, `getFunnelsForClient`, `findFunnelVersion`, `getDraftForFunnel`, `findStep`, `findStepBySlug`. FreshHome / KeyHero / NeatWorks ship without funnels — the agency-mode roster on `/funnels` renders empty rows for them. When real backend lands, this stub is replaced by Supabase reads against `funnels` / `funnel_steps` / `funnel_versions` tables; the accessor surface stays the same.
- **`generation-stub.ts`** (Session 7) — `generateFunnelStub()`, the onboarding wizard's funnel-generation handler. **STUB:** a deterministic passthrough — returns the registry-backed Voltline funnel (`data-stub.tsx`) after a 4–8s synthetic delay, regardless of input. Real funnel generation composes per-step generations (design doc §5.5) and is a backend pass; Session 6's `generatePageStub` is website-page-shaped and can't emit the funnel-only `schedulePicker` / `thanksConfirmation` sections, so the wizard does not reuse it. The `/clients/new/draft` route calls this on mount; the `GenerationStatusCard` shows during the delay.

### `lib/website/` (cont'd) — autosave + publish lanes + approval store (Phase 6 · Cluster 5 · Session 5)

The save → publish → approval pipeline behind the editor. Three new modules plus a reactive hook layer. All mutable state is localStorage-backed (`webnua.dev.draft.*` keys for drafts; `webnua.dev.versions` / `webnua.dev.website-pointers` / `webnua.dev.approvals` for publish state; `webnua.dev.audit-log` for force-publish). When real backend lands the whole set gets replaced by Supabase reads against `versions` / `approvals` / `audit_log` tables; the consumer hooks (`useAutosave`, `useWebsitePublishState`, `useUserPendingSubmission`, `useAllPendingApprovals`) keep their shape.

- **`draft-stub.ts`** — Session 5 (A) + Session 7 funnel extension. localStorage-backed draft store. Key shapes: **`webnua.dev.draft.{websiteId}.{pageId|header|footer}`** for websites; **`webnua.dev.draft.funnel.{funnelId}.{stepId}`** for funnel steps. **Session 7** refactored `DraftSlot` to carry its scope id inline so a single slot argument is self-contained: `{ kind: 'page', websiteId, pageId } | { kind: 'header', websiteId } | { kind: 'footer', websiteId } | { kind: 'funnelStep', funnelId, stepId }`. Stored shape: `{ sections: Section[], savedAt: number }` so the autosave indicator can render "Saved Ns ago" from the stored timestamp. Public API: `loadDraft(slot)`, `loadDraftSections(slot)` (sugar), `saveDraftSections(slot, sections)`, `clearDraft(slot)`, `clearDraftsForWebsite(websiteId)` (wipes all website slots by prefix — called on publish), `clearDraftsForFunnel(funnelId)` (Session 7 symmetry; funnel publish lands later), `subscribeDraft(callback)`. Custom event bus: `webnua:draft-change`.

- **`use-autosave.ts`** — Session 5 (A). The hook driving section persistence. `useAutosave({ slot, sections, disabled? })` → `{ status: AutosaveStatus, lastSavedAt: number | null, retry }`. (**Session 7** dropped the `websiteId` argument — the `DraftSlot` now carries its scope, which lets the same hook serve funnel-step slots too.) Statuses: `idle | pending | saving | synced | failed`. Behaviour: every change to `sections` schedules a 500ms debounced flush; on flush, status moves `saving → synced` (or `failed` on localStorage quota throw, with `retry()` to reattempt). First-render is skipped (no `idle → pending` on mount). `disabled: true` cancels any pending flush and parks at `idle` (used by the Lane B editor lock — locked submitters mustn't keep writing). The 5s server flush from §3.2 collapses to the same 500ms in the stub because localStorage is synchronous; documented in code. Submit-mid-edit edge case (§3.1) is also docced — server-snapshot defines the pending version's contents; in-flight 500ms edits stay in operator's local React state and fold forward on the next flush.

- **`publish-stub.ts`** — Session 5 (B+C+F). The publish + version + submission overlay store. **All operations dispatch the `APPROVAL_EVENT` custom event** (shared with `website-approval-stub.ts` — they're two halves of one store) so every subscriber re-renders on every mutation. Operations:
  - **`publishDraft(websiteId, actor, options?)`** — Lane A. Builds a snapshot by merging the seed draft Version with any per-slot localStorage drafts (slot wins where present), writes a new `published` Version to the overlay, archives prior published, updates the website's `publishedVersionId` pointer overlay, **clears all draft slots for the website**, resolves any in-flight pending approval to `'approved'` (publishing bypasses or fulfils it), and — when `options.force` is set — also calls `appendForcePublishEntry` with actor / target / reason / new version id. Returns `{ newVersionId }`.
  - **`submitForApproval(websiteId, submitter, note?)`** — Lane B. Same snapshot construction, but writes a `pending_approval` Version + a `WebsiteApprovalSubmission` record. **Does NOT clear draft slots** — submitter retains the draft so recall or reject puts them back in business. Diff vs the currently-live published snapshot is computed at submit-time (shallow JSON-string compare per section.data) and stored on the submission as `{ pagesChanged, sectionsChanged, fieldsChanged }`. Submit-mid-edit edge case (§3.1) handled by the architecture: snapshot is whatever flushed to localStorage at this moment.
  - **`approveSubmission(submissionId, approver)`** — Operator approve. Promotes the pending Version → `published`, archives prior, updates pointer, clears draft slots, marks submission `approved`. Returns `{ newVersionId }`.
  - **`rejectSubmission(submissionId, approver, reason)`** — Operator reject. Archives the pending Version, marks submission `rejected` with the reason. Draft slots untouched — submitter resumes editing.
  - **`recallSubmission(submissionId)`** — Submitter recall. Same end-state as reject but no reason recorded; status `recalled`.
  - **`restoreVersionAsDraft(websiteId, sourceVersionId, actor)`** — Session 8 rollback (design §7). Deep-copies a prior version's snapshot into a fresh `draft` Version, archives any existing draft, and wipes the website's localStorage draft slots so the editor rehydrates from the restored snapshot. Does NOT auto-publish — the restored draft re-enters the normal lanes. **Refuses while a pending submission is in flight** (would silently overwrite the submitter's diff). Returns `{ newDraftId }` or null.
  - **Selectors**: `resolveVersion(id)`, `getEffectivePublishedVersionId(websiteId)`, `getEffectiveDraftSnapshot(websiteId)` (Session 8 — seed draft Version merged with localStorage slots; the review surface runs preflight against this), `getAllVersions()`, `getAllApprovals()`, `getPendingApprovals()`, `findPendingForUser(websiteId, userId)`.

- **`preflight.ts`** — Session 8 (design doc §7). Declarative preflight rule engine. `PreflightRule` is `{ id, title, run(snapshot, context) → PreflightResult[] }` — each rule is a pure function over a `VersionSnapshot` that emits zero-or-more results (one per affected page / section). `PreflightResult` carries `{ ruleId, status: 'pass' | 'warn' | 'fail', title, message, pageId?, sectionId?, fixHref? }`. `PREFLIGHT_RULES` is the const array (13 rules: page-has-sections, SEO title/description, hero content + CTA, offer content, CTA section, trust evidence, reviews/FAQ populated, header logo, footer contact, nav targets). `runPreflight(snapshot, context)` → `PreflightReport` (`{ results, counts, canPublish, allClear }`); `canPublish` is true when there are zero `fail`s, `allClear` adds zero `warn`s. `groupResultsByPage(results)` buckets results by `pageId` (with a `__site` bucket for header/footer/nav rules) for the per-page review cards. **Integration-prerequisite rules** (GBP for reviews, calendar for schedule) are deferred until the integrations data layer can answer "is X connected for this client" — add a composable integration-aware rule set then.

- **`use-publish-state.ts`** — Session 5 (B+C). Reactive hook layer over `publish-stub`. Three hooks: **`useWebsitePublishState(websiteId)`** → `{ publishedVersionId, approvalsForWebsite, livePendingSubmission }` for editor + hub consumers; **`useUserPendingSubmission(websiteId, userId)`** → the submitter's own pending submission if any (drives the Lane B editor lock); **`useAllPendingApprovals()`** → workspace-wide pending list (drives the /tickets approvals tab count + the /settings/access "Pending approvals" tile). All three use `useSyncExternalStore(subscribeApprovals, ...)` with a stable server-side fallback so SSR doesn't crash.

### `lib/tickets/` (cont'd) — website-approval submission shapes (Phase 6 · Cluster 5 · Session 5)

- **`website-approval-stub.ts`** — Session 5 (B+C). The shape + subscription glue for pending website-approval submissions. State (writes / reads) lives in `lib/website/publish-stub.ts`; this module just owns the types and the `APPROVAL_EVENT` event bus (`subscribeApprovals(callback)`). `WebsiteApprovalSubmission` (`{ id, websiteId, pendingVersionId, submitterId, submitterName, submittedAt, status: 'pending' | 'approved' | 'rejected' | 'recalled', note?, diff: WebsiteApprovalDiff, snapshot, rejectionReason?, resolvedAt?, resolvedByName? }`). `WebsiteApprovalDiff` (`{ pagesChanged, sectionsChanged, fieldsChanged }`) — shallow JSON-string compare counts, good for the V1 "X fields in Y sections" summary; per §3.4 there's no field-level diff view V1.
- **`types.ts`** — Session 5 added `'website-approval'` as a new `TicketCategory` enum value with `CATEGORY_LABEL['website-approval'] = 'Website approval'`. `pills.tsx` (`CategoryPill`) and `CategoryTile` got their record entries — both render in rust on rust-soft to distinguish from the default `website` tone. The pills are wired but currently unused by the inbox row paths (the approvals tab uses `WebsiteApprovalRow` directly, not `TicketRow`); they're available if a future surface lists website-approvals alongside regular tickets.

### `admin/tickets/` (cont'd) — website-approval row (Phase 6 · Cluster 5 · Session 5)

- **`WebsiteApprovalRow`** (`tickets/WebsiteApprovalRow.tsx`) — Session 5 (E). Single pending-website-submission row inside the "Website approvals" tab on `/tickets`. Admin-only. 4-cell grid: 36px rust-tinted client-initial avatar + `1fr` body (client name + diff summary + submitter / submitted-at / pages-touched meta) + 90px right-aligned relative age + actions row (Approve & publish ✓ / Open in editor → / Reject ✗). Reject expands inline (decision 3 in the Session 5 plan message): the actions row swaps for a warn-tinted reason `Textarea` + Cancel / Submit-rejection ↩ row. Submit button stays gated on non-empty reason. Approve calls `approveSubmission`; reject calls `rejectSubmission`. "Open in editor" routes to `/website` (per-page deep-link wasn't in the brief — submissions are website-level since any of their pages could have changed).

### `app/tickets/_admin-content.tsx` — augmented in Session 5 (E)

Now `'use client'`. Tab list is the static `adminTicketTabs` with a live `{ id: 'website-approvals', label: 'Website approvals', count }` tab spliced in as the second entry (count tracks `useAllPendingApprovals().length`, re-renders on every approval mutation). `TicketTabsBar` runs in controlled mode (`value` + `onChange`) so the active tab is hoisted state. When the approvals tab is active the regular ticket table + filter bar both hide; an `ApprovalsList` renders the `WebsiteApprovalRow`s (with an empty-state card when the queue is clear). `TicketTabsBar` itself grew an optional controlled `value` + `onChange` API in Session 5 — existing uncontrolled callers continue to work.

### `lib/workspace/` — agency vs sub-account context (Phase 6 · Cluster 5)

The two-tier GoHighLevel-style workspace model. Operator's default view is **agency** (cross-client birds-eye, where triage/oversight happens). Drilling into a client via `AdminClientPicker` switches the context to **sub-account** mode for that client (where the actual managing/changes happen). Surfaces that exist in both modes (currently just `/settings/access`) dispatch on the context. Cross-client-only surfaces (`/tickets`, `/leads`, `/calendar`, `/automations`, `/campaigns`, `/integrations` matrix) stay agency-only for now; per-client sub-account variants land as each surface gets touched in future clusters. URL stays clean — context is state-driven, not URL-driven.

- **`workspace-stub.tsx`** — **STUB.** localStorage-backed React provider + hooks. Active client id lives in `webnua.dev.active-client-id`; absent = agency mode; populated = sub-account mode for that client. Exports `<WorkspaceProvider>` (mounted in `src/app/layout.tsx` inside `<UserProvider>`), `useWorkspace()` → `{ activeClientId, activeClient, hydrated, setActiveClientId, clearActiveClient }`, `useActiveClient()` (sugar), `useIsAgencyMode()` (sugar — true when `activeClientId` is null). Defaults to agency mode (no key) on fresh load. Client-role users never see the picker (hidden in `ClientSidebar`), so the context has no effect for them. Two deletion points when real auth ships: this file, and the `<WorkspaceProvider>` mount.

### `lib/agency/` — policy resolution layer (Phase 6 · Cluster 8 · Session 2)

> The three-layer model's spine (see the `## Agency / sub-account model` section near the top). Layer 2 (agency policy) + Layer 3 (sub-account overrides) + the resolver that merges them. Six policy keys, three wired to UI in Cluster 8, three typed + resolver-ready but deferred. STUB layer — localStorage-backed; backend pass swaps the two stores for Supabase tables and the resolver + hooks keep their shape.

- **`types.ts`** — the closed `PolicyKey` union (6 keys) + `POLICY_KEYS` / `LIVE_POLICY_KEYS` arrays + `POLICY_KEY_LABEL`. Per-key value types: `IntegrationDefaults`, `BrandDefaults`, `AutomationDefaults`, `PricingDefaults`. `PolicyValueMap` maps each key to its value type — the resolver is typed to this, never `Record<string, unknown>`. `PolicyResolution<K>` (`{ effectiveValue, source: 'agency' | 'override', agencyValue }`) + `AllPolicyResolutions`. **Adding a policy key means amending this union AND the CLAUDE.md policy-key table — don't widen it ad hoc.**
- **`agency-policy-stub.ts`** — **STUB.** Layer-2 store. `AGENCY_POLICY_SEED` (frozen, derived from `CLIENT_DEFAULTS` + `adminDefaults*`) + a localStorage overlay (`webnua.dev.agency-policy`). `getAgencyPolicy(key)`, `getAllAgencyPolicy()`, `setAgencyPolicy(key, value)`, `resetAgencyPolicy(key | '*')`, `subscribeAgencyPolicy(cb)`. Snapshot-cached per the CLAUDE.md stability rule.
- **`override-stub.ts`** — **STUB.** Layer-3 store. localStorage (`webnua.dev.policy-overrides`), `Record<clientId, Partial<PolicyValueMap>>`. A key absent from a client's entry = inherit. Carries `SUB_ACCOUNT_OVERRIDE_SEED` (Session 4b) — the per-client overrides that exist at platform start (the migrated former `AdminClient.seatLimit` values: Voltline `3`, KeyHero/NeatWorks `null`; FreshHome inherits). `readStore` falls back to the seed; the first write bakes it in so clears can truly clear. `hasOverride`, `getOverride`, `getOverridesForClient`, `setOverride`, `clearOverride`, `clearOverridesForClient`, `subscribeOverrides`.
- **`resolver.ts`** — pure functions over the two stores. `resolvePolicy(key, clientId)` → `PolicyResolution<K>` (clientId null = agency mode; override present = `source: 'override'`). `resolveAllPolicies(clientId)`.
- **`use-policy.ts`** — `'use client'`. `usePolicy(key)` resolves against the active workspace (`useWorkspace`), reactive to both stores; `useAgencyPolicy(key)` is the raw Layer-2 value. Resolution snapshots cached per `(key, clientId)` against a JSON key (snapshot-stability rule).
- **`integration-providers.ts`** — `INTEGRATION_PROVIDERS` — display metadata (id / name / description) for the providers tracked by the `integrationDefaults` key. The ids must match the `sharedProviders` keys in `AGENCY_POLICY_SEED`. Shared by `/settings/integration-defaults` + the sub-account integrations view.
- Verified by `/dev/agency-policy` (see `app/dev/` convention) — the resolution matrix: every key × agency + each sub-account, with source flags + per-cell test-override toggle.
- **Consuming surfaces:** `/settings/integration-defaults` + `/settings/seats` (Session 3 — agency-level, set Layer 2 via `useAgencyPolicy`/`setAgencyPolicy`). Session 4b — sub-account override UI: `/settings/integrations` sub-account mode (`_sub-account-content.tsx`, per-provider `integrationDefaults` inherit/override via `usePolicy` + `setOverride`/`clearOverride`) and `ClientSeatLimitCard` (the seat limit resolves through `resolvePolicy('defaultSeatLimit', clientId)` — see `lib/clients/seat-limit-stub.ts`).

### `lib/website/` (cont'd) — form-to-page generation (Phase 6 · Cluster 5 · Session 6)

The Q&A-to-Page pipeline that backs `/website/new`. Spec lives in `reference/builder-generation-design.md`. Five locked questions (page type → primary intent → audience → specifics → avoid); stub generator deterministic on `(pageType, intent, audience)` with synthetic 4–8s delay; validation pipeline + fallback log graduates straight to backend.

- **`generation-context.ts`** — `GenerationContext` + the discriminated `PrimaryIntent` (including `'other'` with free-text fallback) + `Audience` + `ExistingPageSnapshot`. Chip-row data tables (`PAGE_TYPE_CHIPS`, `PRIMARY_INTENT_CHIPS`, `AUDIENCE_CHIPS`) + label helpers (`describeIntent`/`describeAudience`/`describePageType`) live here so the Q&A flow + review surface + dev preview share one source of truth.
- **`generation-prompt.ts`** — pure prompt-block construction. Five blocks (`system | brand | page-questions | existing-pages | registry-catalog`). `buildPromptBlocks(ctx)` for the dev surface; `composePrompt(ctx)` glues them with `---` separators. The existing-pages block is omitted for first-page-of-website generation (empty `existingPages[]`). The registry-catalog block filters `SECTION_REGISTRY` by `allowedContainers.includes('page')` + `allowedPageTypes` (if set). Voice tone numeric axes → prose translation via `voiceToneToProse()`. Side-effect-free + no React; this is the piece that graduates to the real backend.
- **`generation-stub.ts`** — handler + recipes + per-section copy library + validation pipeline (§4.4) + fallback log (§4.4a). `generatePageStub(ctx)` runs the synthetic delay then returns `{ generationId, page, fallbackLog, droppedSections }`; `generateSync(ctx)` is the no-delay variant for the dev preview. Per-page-type recipes (`home | services | about | contact | generic`) pick which sections appear and in what order. Three voice variants (`cold-ad-urgent | existing-calm | search-plain`) selected from `audience`. `GENERATION_PHASES` exports the three phase strings the status card cycles through. The free-text `specifics` + `avoid` answers are **received but not consumed** — see the design doc §1 framing rule (the generation card surfaces them as "received, will be applied," not echoed back as the AI consuming them).
- **`generated-pages-stub.ts`** — **STUB.** localStorage-backed overlay store. Key shape `webnua.dev.generated-pages.{websiteId}` → `Page[]`. `addGeneratedPage(websiteId, page)` writes; `mergeGeneratedPages(websiteId, seed)` returns seed-first list with the overlay appended (seed wins on id collision). Both the `/website` hub and the `/website/[pageId]` editor merge in at read time so the new page is visible and editable immediately. When real backend ships, replaced by a normal INSERT against the `pages` table; the merge helper goes away (seed = backend reads).

### `shared/website/` (cont'd) — form-to-page UI primitives (Phase 6 · Cluster 5 · Session 6)

- **`QuestionCard`** (`website/QuestionCard.tsx`) — single Q&A step inside `/website/new`. Composes `BuilderStepHeader` + body slot + `BuilderFooterActions`. Discriminates Continue vs Skip via `required` + `isAnswered`: required & empty disables Continue; optional & empty shows both "Skip →" and "Continue →". Body slot is open — chip rows, textareas, or the chip-or-free-text dual mode used by Q2's "Other" option.
- **`GenerationStatusCard`** (`website/GenerationStatusCard.tsx`) — `'use client'` ink-bg "// GENERATING ✦" card. Rotating ✦ glyph + "Drafting your page…" + phase copy cycling on a 1.6s interval (`GENERATION_PHASES`). When Q4 or Q5 had content, the card mounts a separate "// RECEIVED" footer block with ✓-prefixed lines reading "Got your specifics — will shape the final draft." / "Got your avoid notes — will be honoured." **Framing rule locked in the design doc §1:** never echo Q4/Q5 inline with phase copy as the AI consuming them in real time — that would mislead users into thinking the stub-output ignoring those answers is a bug.
- **`NewPageEntry`** (`website/NewPageEntry.tsx`) — the "+ New page" / "✦ New page" CTA on `/website`. Encapsulates the two-cap gating story (`editPages` + `useAI`): both → rust `Link` to `/website/new`; `editPages` only → hidden (V2 manual-new-page modal lands here later); missing `editPages` → `<CapabilityGate mode="request">` reveals the request-change affordance. Inlining this on the hub would force every reader to re-derive the three-state gating from the JSX; the wrapper keeps the hub readable.

### `app/website/new/` — Q&A generation flow (Phase 6 · Cluster 5 · Session 6)

- **`page.tsx`** — `'use client'` flow root. State lives in URL search params (`?step=N&pageType=...&intent=...&intentOther=...&audience=...&specifics=...&avoid=...`), no localStorage — refresh + back/forward work natively; 90-second form doesn't warrant cross-tab merge complexity. Six logical steps: 1-5 questions (each via `QuestionCard`) → 6 review summary with inline "Edit ✎" links bouncing back to the relevant question while preserving the rest. On Generate, mounts `GenerationStatusCard` with `specifics`/`avoid` passed through, calls `generatePageStub(ctx)`, writes via `addGeneratedPage`, routes to `/website/[newPageId]`. The route doesn't re-check caps (the gated `NewPageEntry` already filtered); the review-card Generate button is disabled with a fallback explainer if the user somehow lacks `editPages + useAI` on this workspace.

### `app/dev/generation-preview/` — Session 6 dev surface

- **`page.tsx`** — Off-nav dev utility per the `app/dev/` convention (deletion-point: same lifecycle as `/dev/capabilities` + `/dev/sections`). Three-column layout: inputs (page-type chips / intent chips + Other input / audience chips / specifics + avoid textareas) → prompt blocks (expandable per-block details) → stub output (Generated page summary + validation log + full JSON). All three columns re-render on every input change — no Generate button, the live linkage is the whole point. Validates the prompt-construction layer (which graduates to real backend) in isolation from the stub recipes (which don't).

### `admin/team/` — team-invite flow (Phase 6 · Cluster 6 · team invite)

> **Architectural note — `/settings/team` is a shared-slug dispatcher (Cluster 6 · client invite).** Both roles now reach a "Team" tab from their own nav, so `app/settings/team/page.tsx` is a `'use client'` role dispatcher (same pattern as `/settings/integrations`): operator → `_admin-content.tsx` (the workspace team-invite flow below); client → `_client-content.tsx` (the client's own-account teammate list — see `app/settings/team/` entry). The 3-step operator modal stays admin-only by virtue of mounting only inside `_admin-content.tsx`.

> The first real management UI for the capability layer. A 3-step `Dialog`-based modal launched from the operator `/settings/team`. Screens 37-39 in the platform prototype. The flow captures org-structure decisions (who / what role / which clients) as a discrete attributable `TeamInvite` record (vision §7). Built as pure composition over `Dialog` / `Button` / `Input` / `Textarea` + the capability layer.
>
> **Prototype disagreements converged:** (1) the prototype's Step 2 allow/deny bullets ("Edit Meta ad campaigns", "See revenue/billing") are off the 13-cap builder model — the preview is driven **purely from the 13 real capabilities** instead (see the parked decision below). (2) The prototype shows a pre-filled readonly demo; the real modal is an empty interactive form. (3) The prototype sizes Step 3 narrower than Steps 1-2 — a single step-switching `Dialog` can't resize per step, so the flow uses one `size="lg"` (920px) throughout.

- **`TeamInviteModal`** (`team/TeamInviteModal.tsx`) — `'use client'` orchestrator. Owns step state (`1 | 2 | 3`) + the `TeamInviteDraft` form state + the committed `TeamInvite` once sent. Modal chrome comes from `shared/invite/InviteModalChrome` (extracted at the second use — see that entry). Step 1 = invite form (email / full name / role pick / client-access selection / personal note); Step 2 = invitee ink card + `PermissionPreview` + "what happens after" box; Step 3 = success state + copyable magic link + "what happens next". Step 1→2 is gated on a non-empty email and (for junior) ≥1 assigned client. On send, builds one `TeamInvite` record and (stub) `console.log`s it. Step-2 invitee card + Step-1 client-access row stay local subcomponents (operator-specific, single-use).
- **`InviteTeamButton`** (`team/InviteTeamButton.tsx`) — `'use client'` trigger: a `size="sm"` "+ Invite member" `Button` + `TeamInviteModal` open-state owner. Mounted on `/settings/team`. Same pattern as `AddBookingButton` / `NegativeReviewAlertButton`.
- **`InviteStepper`** (`team/InviteStepper.tsx`) — generic N-dot progress indicator. Props: `steps` + `current` (1-indexed). Steps before `current` render done (good check), `current` renders rust with a soft ring, the rest paper-2. First stepper primitive in the codebase — kept in `admin/team/` (only consumer); move to `shared/` if a second flow needs it.
- **`RoleSelectCard`** (`team/RoleSelectCard.tsx`) — `'use client'` selectable role card (icon tile + name + description), used 3-up in Step 1. Borrows `ReframeOptionCard`'s `data-selected` + rust-border-on-active idiom but is its own shape (compact icon-tile card vs `ReframeOptionCard`'s 22px display-headline card — reuse would have fought both).
- **`PermissionPreview`** (`team/PermissionPreview.tsx`) — **the cap-wired component.** Iterates `ALL_CAPABILITIES`, partitions allow/deny by membership in `TEAM_ROLE_CAPABILITIES[role]`, labels each with `CAPABILITY_LABEL`, and uses `CAP_EXPLAINER[cap].short` as the deny-line hover title. Never a hand-written list — role → permissions is computed. Renders a one-line note for owner/operator explaining they look identical here because they only diverge on billing/governance (outside the cap model).

### `shared/invite/` — invite-modal chrome (Phase 6 · Cluster 6 · client invite)

> Extracted at the second use (operator `TeamInviteModal` + client `ClientTeamInviteModal`). Deliberately the **intersection** of what both modals need — the structural rhythm + the two in-body form primitives — not the union. Anything one modal needs and the other never does (operator role-pick, client seat-meter) stays composed in that modal, never bolted on here as an optional prop.

- **`InviteModalChrome`** (`invite/InviteModalChrome.tsx`) — exports: `INVITE_DIALOG_CONTENT_CLASS` (the shared `DialogContent` className — each modal still composes its own `Dialog`/`DialogContent size="lg"`), `InviteModalHeader` (rust/good mono tag + `<em>`-rust title + subtitle + close), `InviteModalFooter` (mono info line + action-button slot), `InviteSection` (labelled form section with `last` rhythm flag), `InviteFieldLabel` (mono uppercase field label + optional `required` asterisk), `InviteCopyLinkRow` (the dashed-border magic-link + copy button on both confirmation steps), and the `inviteInitials(fullName, email)` helper.

### `client/team/` — client-side teammate invite (Phase 6 · Cluster 6 · client invite)

> The client-role counterpart to the operator team-invite. A client business owner invites a user into their **own** client account — fixed client (no picker), flat access (no role pick): every invitee resolves to the `CLIENT_DEFAULTS` floor (`viewBuilder` only); per-website edit grants stay an operator concern via `/settings/access`. With no role/access decision to review, the flow is **2 steps, not 3** — inventing a review step for a flat invite would be drift (same logic as Session 3's Step-2 call). The access reality is communicated inline on Step 1. Net-new — the client prototype has no team surface.

- **`ClientTeamInviteModal`** (`team/ClientTeamInviteModal.tsx`) — `'use client'` 2-step `Dialog` modal. Composes `shared/invite/InviteModalChrome`. Step 1 = form (email required / optional full name / default-access explainer / `SeatUsageMeter` + seat-blocked warn block / optional note); Step 2 = success state + `InviteCopyLinkRow` + "what happens next". The send is seat-gated — `canInviteToClient(clientId)` disables the Send button AND re-guards inside `handleSend` (the count can move while the modal is open). On send, commits one attributable `ClientUserInvite` via `addClientInvite`.
- **`InviteTeammateButton`** (`team/InviteTeammateButton.tsx`) — `'use client'` "+ Invite teammate" trigger + modal open-state owner. Resolves the inviter's own `clientId` from `useUser()`; renders null for a user with no client. Sibling of `admin/team/InviteTeamButton`.
- **`SeatUsageMeter`** (`team/SeatUsageMeter.tsx`) — presentational "N of M seats used" indicator (bar + count, or uncapped variant when `limit` is null). Takes a resolved `SeatUsage` — the caller fetches it reactively via `useClientSeatUsage`. Used on the client Team tab, inside `ClientTeamInviteModal`, and inside the operator `ClientSeatLimitCard`.

### `shared/settings/` (cont'd) — client seat-limit control (Phase 6 · Cluster 6 · client invite)

- **`ClientSeatLimitCard`** (`settings/ClientSeatLimitCard.tsx`) — `'use client'` operator control for one client's seat limit, mounted on `/settings/access` in **sub-account mode** as a visually-distinct `SettingsSection` separate from the capability grid (the seat limit is a contract/plan axis — how many users — not the capability axis — what users can do). Number input + "No limit" checkbox + Save (disabled until dirty); shows the live `SeatUsageMeter` and the most-recent `SeatLimitChange`. **Cluster 8 · Session 4b** — resolver-backed: the limit resolves through `resolvePolicy('defaultSeatLimit', clientId)`, so the card shows a `PolicySourceBadge` (inherited from the agency default vs a per-account override) and a "Revert to agency" affordance (`inheritSeatLimit`) when overridden. Admin-only — placed in `shared/settings/` following the `CapabilityToggleGrid` precedent. Calls `setSeatLimit` with the operator's id for attribution.
- **`PolicyOverrideRow`** (`settings/PolicyOverrideRow.tsx`) — `'use client'` Cluster 8 · Session 4b. The canonical "inherited from agency / overridden here" affordance for sub-account settings: a label + an inherited/overridden source badge + the agency value hint when inherited + a "Revert to agency" action when overridden; the control is `children` (the parent owns what "override" means for its policy key). Sibling export **`PolicySourceBadge`** is the standalone pill (used by `ClientSeatLimitCard`). Consumed today by the sub-account integrations view.

### `app/settings/team/` — shared-slug Team tab (Phase 6 · Cluster 6 · client invite)

- **`page.tsx`** — `'use client'` role dispatcher (operator → `_admin-content`, client → `_client-content`).
- **`_admin-content.tsx`** — the operator workspace team page (`AdminSettingsTeamContent`) — team members + role-permission matrix + the `InviteTeamButton`.
- **`_client-content.tsx`** — the client's own-account view (`ClientSettingsTeamContent`) — `SeatUsageMeter` + `InviteTeammateButton`, the current-users list + pending invites (both via the shared `TeamRow`), and a "what teammates can do" explainer. Reactive to the client-invite store so a freshly-sent invite appears immediately.

### `lib/team/` — team-invite data shapes + roles

- **`types.ts`** — `TeamInviteStatus` (now a re-alias of `InviteStatus` from `lib/invites/shared-types` — single-sourced so the operator + client invite siblings can't drift), `TeamInviteDraft` (mutable form state — `{ email, fullName, role, assignedClientIds, personalNote }`), and the committed `TeamInvite` (draft + `id`, `invitedBy` (operator user id — an invite without it is unattributable, vision §7), `invitedAt` / `expiresAt` (ISO strings), `magicLink`, `status`). Stub-becomes-schema-contract: every field is producible from real DB rows + lookups with no narrative interpretation.
- **`roles.ts`** — `TeamRole` (`'owner' | 'operator' | 'junior'` — the operator-tier org role, distinct from `Role` in `capabilities.ts`), `TEAM_ROLES` (the 3 role defs: id / name / icon glyph / description), and `TEAM_ROLE_CAPABILITIES: Record<TeamRole, readonly Capability[]>` — **derived** from the cap layer: owner + operator inherit `ADMIN_DEFAULTS` verbatim; junior is a deliberate subset (`viewBuilder`, `editCopy`, `editMedia`, `editSEO`, `useAI` — `editSEO` is deliberately in, since SEO drafts ≠ SEO publishes and the `publish` gate still requires operator). `getTeamRoleDef(role)` helper.

### `lib/invites/` — invite primitives + client teammate invite (Phase 6 · Cluster 6 · client invite)

> `ClientUserInvite` is a **sibling** of `lib/team/types.ts` `TeamInvite`, NOT a generalization — the dimensions genuinely differ (no Webnua-org role; a single fixed `clientId` not `assignedClientIds[]`; a client-user inviter; different RLS/lifecycle). Two clean siblings beat one record with conditional optional fields. Only the genuinely-common primitives are shared.

- **`shared-types.ts`** — `InviteStatus` (`'pending' | 'accepted' | 'expired' | 'revoked'` — the lifecycle union both invite siblings use; `lib/team` `TeamInviteStatus` re-aliases it), `IsoTimestamp` / `MagicLink` (convention-carrying string aliases), `INVITE_TTL_DAYS` (= 7).
- **`client-invite.ts`** — `ClientUserInviteDraft` (mutable form state — `{ email, fullName, personalNote }`) + the committed `ClientUserInvite` (`id`, `email`, `fullName`, fixed `clientId`, `invitedBy` (client user id — attributable, vision §7), `invitedAt` / `expiresAt`, `magicLink`, `status: InviteStatus`, `personalNote: string | null`). Stub-becomes-schema-contract.
- **`client-invite-stub.ts`** — **STUB.** localStorage-backed invite list (`webnua.dev.client-invites`). `addClientInvite`, `getInvitesForClient` (pending only), `getAllClientInvites` (snapshot-cached for `useSyncExternalStore` stability), `subscribeClientInvites`.
- **`seats.ts`** — seat accounting. `getClientSeatUsage(clientId)` → `SeatUsage` (`usedByUsers` + `usedByInvites` + `total` + `limit`); usage is **derived** (existing client users + pending invites — pending counts so the limit can't be gamed by bulk-inviting). `canInviteToClient(clientId)` → `{ allowed, reason? }` (`reason: InviteBlockReason` — `'seat_limit_reached'` today, union kept open) — backs both the modal's Continue gate and the send guard.
- **`use-seat-usage.ts`** — `useClientSeatUsage(clientId)` hook — `useSyncExternalStore` over both feeder stores (client invites + seat limits), snapshot-cached against a derived key so the reference only changes when the counts change.

### `lib/clients/` — per-client seat limit (Phase 6 · Cluster 6 · client invite)

- **`seat-limit.ts`** — `SeatLimitChange` (`{ changedBy, changedAt, clientId, previousLimit, newLimit }`, limits `number | null`) — every limit change is an attributable event (vision §7); history is an array of these, not a single overwritten value.
- **`seat-limit-stub.ts`** — **STUB.** **Cluster 8 · Session 4b — a facade over the agency policy resolver**, not a standalone store. The seat limit is the `defaultSeatLimit` policy key: the effective limit resolves down the layers (`resolvePolicy('defaultSeatLimit', clientId)`) — agency default (Layer 2) → per-account override (Layer 3, `override-stub`). This module keeps the seat-limit export surface (`getSeatLimit`, `getAgencySeatLimit`, `isSeatLimitOverridden`, `setSeatLimit` = set an override, `inheritSeatLimit` = clear it, `subscribeSeatLimits`) and owns the change-history log (`webnua.dev.seat-limit-history` — `getSeatLimitHistory`, `subscribeSeatLimitHistory`), which the generic override store doesn't track. `getSeatLimit`/`subscribeSeatLimits` keep their old names so `lib/invites/seats.ts` + `use-seat-usage.ts` are unchanged.

> Note: `AdminClient` (`lib/nav/admin-clients.ts`) carried a `seatLimit` field through Cluster 6; **Cluster 8 · Session 4b retired it** — the per-client seed values moved into `SUB_ACCOUNT_OVERRIDE_SEED` (`lib/agency/override-stub.ts`) and the seat limit now resolves through the `defaultSeatLimit` policy key.

---

## Deferred / out of scope until end

The admin **Proof Page / prospecting tool** (the audit → proof-page → outreach
pipeline, sourced from `reference/webnua-proof-page-tool.html` and
`reference/webnua-finding-library.json`) is a real platform feature **but is
deferred until the full platform — front and back end — is working.**

Until then:
- **Do not** scaffold routes, components, lib/types, or data for it.
- **Do not** add it to nav, sidebar, or any role's settings.
- **Do not** treat the prototype HTML or the finding-library JSON as current spec.
- The files stay in `/reference` — they are the spec for the future build, not for now.

If a session has a reason to touch this area, stop and confirm with the user
first.

---

## Open decisions / parked

> Things deliberately not decided yet. Don't silently resolve these — flag them.

- Backend wiring (Supabase) — frontend-first, backend later.
- Error-handling pattern — decide on first real use, document above.
- ~~**`SettingsPanel` — second-use trigger.**~~ **Resolved** (Phase 5 session B): every settings tab page now uses the extracted `components/shared/settings/SettingsPanel`. Don't re-inline the `Card`+`CardContent` pattern in new tab pages — use `SettingsPanel`.
- **`InkHero` — evaluated at six instances (Phase 6 · Cluster 6 · Session 1a); verdict: styling recipe, not a component.** Six ink-hero-style components exist (`IntegrationProgressHero`, `BillingPlanCard`, `TicketsHero`, `LeadsHero`, `IntegrationMatrixHero`, `FunnelHero`). The structure-vs-styling call: only **3 of 6** share the canonical `[tag][headline][subtitle] | [side]` structure (`TicketsHero` + `LeadsHero` are near-identical twins; `IntegrationMatrixHero` matches if its stat tiles are treated as a generic `right` payload). The other three are genuinely different shapes — a progress-bar row (`IntegrationProgressHero`), a plan-meta line + action (`BillingPlanCard`), a back-link + meta rows + version pill + `FunnelAggCard` + radial flourish (`FunnelHero`). A unified `InkHero` component would need optional slots for all of those — the grab-bag this decision exists to prevent. What IS broadly shared is *styling chrome*: the `bg-ink … text-paper` surface and especially the rust mono-pill tag (near-identical in 5 of 6). **So: consolidate as a `cn()` styling recipe (`inkHero*` helpers), NOT a component.** The recipe landed in **Session 1b** as `lib/ink-hero.ts` (`inkHeroSurface()` + `INK_HERO_TAG_CLASS`), first consumed by `ClientHubHero`; existing heroes adopt it opportunistically when next touched — no churn-migration of five shipped surfaces. **Separate still-open item:** `TicketsHero` + `LeadsHero` are true twins — collapsible to one component independently of this verdict; merge whenever one of them is next touched (not urgent, not bundled into Cluster 6).
- ~~**`LeadRailCard` vs `TicketSideCard` vs booking rail — third use of the white-card-rail pattern.**~~ **Resolved** (Phase 6 · Cluster 6, first commit): `TicketSideCard` → `shared/RailCard`, `TicketPropertyRow` → `shared/RailPropertyRow`; `LeadRailCard` / `LeadRailRowItem` merged in (the `rows` shorthand + `tone`/`accent` row vocabulary absorbed). All four old files deleted, no re-export shims; tickets / leads / bookings detail rails migrated. `RailPropertyRow` carries a `labelStyle: 'plain' | 'mono'` knob to cover the divergent label styling (plain for tickets/bookings, mono for leads rows). See the `shared/` rail-primitives inventory entry.
- ~~**`CalendarClientFilterBar` vs `LeadFilterChips` — second use of the rust-active filter-chip recipe.**~~ **Resolved** (Phase 5 · Cluster 3 · Session A): the third use landed (admin `/automations` client filter) and triggered the extraction. `shared/FilterChips.tsx` is the canonical implementation; both `LeadFilterChips` and `CalendarClientFilterBar` were deleted (no re-export shims) and the two callers (`/leads` admin, `/calendar` admin) were migrated to import `FilterChips` directly. The `LeadFilterChip` / `CalendarClientFilter` data types stay in their feature `types.ts` files — they're structurally `FilterChip`-compatible and TypeScript accepts them. Don't reintroduce per-feature wrappers — use `FilterChips` directly.
- **`CalendarClientTone` vs `LeadClientTone` / `AdminTicketClientTone` — three separate client-tone vocabularies now exist.** Leads/tickets pills use muted backgrounded tones (`bg-rust/[0.12]` etc.); calendar uses solid prototype hex colours (`#4a7ba6` etc.) on booking pills + legend swatches + today-panel left borders. The token sets aren't interchangeable (one is a pill background-tint vocabulary, the other is a solid identity-colour vocabulary), so they live separately. **Hold pattern: when the booking-detail screen (Cluster 2 Session 2) lands and needs the same solid-tone vocabulary, extract `lib/calendar/tones.ts` and import from both calendar + booking detail.**
- **`BuilderFooterActions` + `EditorFooterActions` both exist — revisit for a shared core if a third footer-actions variant appears.** Built as siblings (not a single conditional component) because the interaction models genuinely differ — wizards do Back/Continue + step-count progress, editors do Back/Disable/Save + auto-save indicator. Same discipline that just paid off with `FilterChips`: extract on the third data point, not the second.
- **Three `1fr + fixed-rail` detail layouts now exist (`TicketDetailLayout` 320px, the inline lead-detail shape 320px, `AutomationEditorLayout` 340px) — revisit layout-shell consolidation.** Separate from the already-parked rail-card consolidation. The widths and surface treatments diverge slightly (the editor canvas is a white card; ticket/lead detail wrap the main col in their own surface), so this isn't a drop-in extraction. Don't act now — get it on the list for a future housekeeping pass (the `TicketSideCard` → `RailCard` rename it was bundled with is now done; this layout-shell item remains open).
- **Numbered-option-row pattern — `ConflictOptionRow` (booking conflict modal) + `NegativeReviewActionRow` (admin Screen 24) are visually adjacent (Phase 5 · Cluster 4 · Session A).** Both render a numbered ink/rust-light circle + title + sub + a recommended/selected state. The shapes diverge enough to sibling (the conflict row is 2-column with the recommended state colour-only; the negative-review row is 3-column with a visible "Recommended" pill), so they were intentionally not merged. **Third use → extract** a generic `NumberedOptionRow` in `shared/` with a `pill?: string` slot.
- ~~**`CampaignActivityCard` vs `LeadTimelineEventRow` — second activity-log pattern.**~~ **Resolved** (Phase 6 · Cluster 6 · Session 1a): evaluated at the third-surface trigger (the overview hub's activity feed). The flat-feed shape was extracted to `shared/ActivityFeed` + `shared/ActivityRow`; `CampaignActivityCard` became a thin adapter over it. `LeadTimeline` was **deliberately left a sibling** — a timeline (dot-spine, scheduled-future pending events, tinted snippet quotes, gapped rows) is a genuinely different structural concern from a flat feed, and folding it in would re-create the optional-slot grab-bag. Flat feeds consolidate; the timeline stays. Don't reopen this — `LeadTimeline` staying separate is the decided outcome, not an unfinished one.

- **Funnel publish / autosave / approval — DEFERRED past Session 7.** Session 7 wired the funnel-step editor shell (data model, routes, editor mode, autosave to a funnel-keyed `DraftSlot`) but did NOT wire publish, submit-for-review, or the approval queue for funnels. The `EditorToolbar` simply hides both publish actions for `mode.kind === 'funnelStep'`, and `useUserPendingSubmission` short-circuits to null when called with `websiteId === null` (the funnel-mode call). Session 8 also skipped funnels (it covered website preflight + the review surface + rollback only). When funnel publish lands (a Cluster 6 cleanup pass), the work is: a `funnel-publish-stub.ts` parallel to `website/publish-stub.ts`, a `useUserPendingFunnelSubmission` hook, a `WebsiteApprovalSubmission`-shaped `FunnelApprovalSubmission`, a funnel-flavoured `runPreflight` (the rule engine is website-snapshot-shaped today), a `/funnels/[id]/review` surface, and routing the funnel editor's toolbar `reviewHref` through it. The shapes are intentionally aligned with the website lanes so the lift should be mostly typing.

- ~~**Wizard preview (`FunnelLandingPreview`) ≠ funnel editor preview.**~~ **Resolved** — Session 7 (the wizard refactor): `FunnelLandingPreview` + `FunnelPreviewState` + the `previewAfter*` snapshots are deleted. The wizard's Step 5 (`/clients/new/draft`) is a wizard-frame `SectionEditor` walk (`WizardSectionEditor`) rendering the section registry's per-section `<Preview>` components — one preview implementation platform-wide. The wizard now produces an editable `Funnel`; steps 1-4 are single-column Q&A forms, the trust step triggers `generateFunnelStub`. See `builder-design.md §5`. **Still deferred:** the wizard does not yet fold its Q&A answers into a real `GenerationContext` (the stub generation ignores them — same as Session 6's specifics/avoid), and funnel publish/preflight is unbuilt (see the funnel-publish parked item above).

- **Agency vs sub-account two-tier model — `/settings/access`, `/website`, `/dashboard` are the references (Phase 6 · Cluster 5–6).** GoHighLevel-style. Workspace context driven by `lib/workspace/workspace-stub.tsx` and switched via `AdminClientPicker`. **Agency mode** = cross-client birds-eye for triage. **Sub-account mode** = drill into one client to make changes. `/settings/access` (Cluster 5 · Session 1b) and `/website` (Cluster 5 · Session 3) are built native against this — agency mode shows a cross-client overview; sub-account mode renders the active client's content. **Cluster 6 · Session 1a added the third reference: `/dashboard`** — `app/dashboard/page.tsx` dispatches on `useIsAgencyMode()`, agency mode → the cross-client roster (`AdminDashboardContent`), sub-account mode → the single-client overview hub. **The state-driven dispatch pattern (branch on `useIsAgencyMode()` inside the route, no URL change) now has three precedent points — it is the settled platform pattern; a future fourth surface adopts it, it does not get re-decided.** Their agency-level cross-client matrices live at `/settings/access` (agency view) and `/websites` (admin-only). **Surfaces still agency-only that need sub-account variants when next touched:** `/automations` (admin roster — inline groups don't scale past ~5 clients), `/campaigns` (admin roster — same), `/leads` (admin cross-client inbox is correct as triage; but editing/managing a specific lead should land you in sub-account), `/calendar` (cross-client overview correct as triage; booking-edit work should be sub-account), `/reviews` (per-client cards is OK as a hybrid). **Don't refactor the legacy agency-only roster surfaces now** — adopt the pattern surface-by-surface as scope reaches each one. **Cluster 8 (in progress)** formalises the model — see the `## Agency / sub-account model` section near the top: three-layer resolution, the closed policy-key union, and `/settings/*` joining the mode-dispatch pattern. Cluster 8 scope is the IA reshape + the agency settings home; plans/billing is a separate later cluster.

- **Auth + capability mechanism — STUB in place.** The current user boundary is a localStorage-backed React context with four hardcoded stub users (Craig admin / Mark@Voltline editor / Liam@Voltline view-only / Anna@FreshHome view-only). Capability layer is real; user-resolution + publish + version + approval state are all the stub. **Six deletion points** when real auth ships:
  1. `src/lib/auth/user-stub.tsx` + `src/lib/auth/audit-stub.ts` — the provider + `useUser`/`useCapabilities`/`useCan`/`useCanAny`/`useCanAll`/`useIsViewingAs` + the legacy `useRole` shim + the stub users + the force-publish audit stub (seed + overlay path). Replace `audit-stub.ts` with backend-resolved entries; keep `capabilities.ts` and `explainers.ts`.
  2. `src/lib/website/draft-stub.ts` + `src/lib/website/publish-stub.ts` + `src/lib/website/use-publish-state.ts` + `src/lib/website/use-autosave.ts` + `src/lib/tickets/website-approval-stub.ts` — the Session 5 stub data layer. Replace with Supabase-backed reads against `versions` / `approvals` / `audit_log` tables; the consumer hooks (`useAutosave`, `useWebsitePublishState`, `useUserPendingSubmission`, `useAllPendingApprovals`) keep their shape so call sites don't change.
  3. `src/app/layout.tsx` — the `<UserProvider>` import + wrapper (replace with the Supabase-backed equivalent).
  4. `src/components/shared/DevRoleSwitcher.tsx` and its mounts in `(client)`/`(admin)` layouts plus every shared-route layout listed in the architectural notes (settings/tickets/leads/calendar/automations/reviews/dashboard).
  5. `src/app/dev/` — the dev verification matrices: `dev/capabilities/` (cap layer × gate modes), `dev/sections/` (section registry — every registered section type's Fields + Preview against a swappable brand), `dev/generation-preview/` (Session 6 — prompt construction + stub output side-by-side), and `dev/agency-policy/` (Cluster 8 — policy resolution matrix). Delete the whole tree; see `app/dev/` convention below.
  6. `src/lib/agency/agency-policy-stub.ts` + `src/lib/agency/override-stub.ts` — the Cluster 8 policy stores. Replace with Supabase reads against `agency_policy` / `sub_account_policy_overrides` tables; `resolver.ts` + `use-policy.ts` + `types.ts` keep their shape.
  Also: `src/app/page.tsx` (root landing) and `src/app/(auth)/login/page.tsx` both call `useRole()` — replace those with the real auth-resolution flow. **viewAsUser is a mode, not a capability** — when set, `useCapabilities()`/`useCan()` resolve to the impersonated user's caps but `useUser()` and `useRole()` keep returning the actual operator (so sidebar shape doesn't flip). Switching the active user automatically clears any view-as override.
- **`app/dev/` convention — stub-era only.** Routes under `src/app/dev/*` are off-nav developer utilities for inspecting stub state during the no-backend phase. Four surfaces today: `/dev/capabilities` (cap layer × gate modes, Session 1a), `/dev/sections` (section registry × brand, Sessions 2-4), `/dev/generation-preview` (prompt construction + stub output, Session 6), `/dev/agency-policy` (policy resolution matrix, Cluster 8 · Session 2). The entire `app/dev/` tree is gated or wiped when real auth ships — same lifecycle as the rest of the stub layer. Don't add user-facing pages here; don't link to `/dev/*` from production nav.

- **Explainer strings assume the Webnua managed-service relationship.** `src/lib/auth/explainers.ts` hardcodes "your operator" / "your account" in every `disable`-mode tooltip and `request`-mode label. Fine for V1 (single-operator platform). If multi-operator deploys land later (white-label, agency reseller, etc.), swap the hardcoded strings for an `{operatorLabel}` token resolved per workspace before the strings get embedded into hundreds of `<CapabilityGate>` calls downstream. Flag the moment a second deploy scenario shows up.

- **Workspace-governance capabilities (billing, team management, client deletion) are not in the 13-cap builder model.** The capability layer covers page-builder access only; org-governance actions live outside it. This is why the team-invite `PermissionPreview` shows owner and operator as identical — in the builder model they genuinely are; they diverge only on governance. Extending the cap model is **deferred**. When next revisited — likely with the backend pass or the first real billing surface — evaluate adding ~4 new caps (e.g. `manageBilling`, `manageTeam`, `deleteClient`, `viewFinancials`) and update `TEAM_ROLE_CAPABILITIES` in `lib/team/roles.ts` so owner/operator reflect their real divergence. The `PermissionPreview` parity note can then be dropped.

- **Per-client seat limit lives on `/settings/access` sub-account mode — provisional home.** The seat limit is a contract/plan axis (how many users a client's plan permits), distinct from the capability axis (`/settings/access`'s main job). It sits on `/settings/access` sub-account mode because that is the operator's only per-client management surface today — there is no standalone client-record edit surface, and inventing one was rejected as scope creep. It's rendered as a visually-distinct `SettingsSection`, separate from the capability grid, to keep the two axes from blurring. **When a real billing/plan surface lands, the seat limit moves there** — it's a plan lever, and `ClientSeatLimitCard` should relocate with it. The data layer is home-agnostic and won't need to move: as of Cluster 8 · Session 4b the seat limit resolves through the `defaultSeatLimit` policy key (agency default + per-account override) — `lib/clients/seat-limit-stub.ts` is now a thin facade over the resolver, and `AdminClient.seatLimit` has been retired.

- **Client-internal hierarchy is flat for V1 — deferred.** Every client-invited user gets the `CLIENT_DEFAULTS` baseline; the client teammate-invite flow has no in-flow role pick (no co-owner vs member tier). The capability model doesn't currently distinguish a client-internal admin from a regular client user, and inventing a tier to match a UI shape would be the drift the operator-side Step-2 call correctly avoided. When workspace-governance caps land (see the item above), `inviteClientTeammate` becomes a real capability and the client-invite flow can gate it — at which point a client-internal role tier can be designed against a real cap, not a guessed one.
